# Hosted sandbox and live-mode ownership

This records the ownership and browser-artifact portion of the implementation
contract. It does not establish that all owners have their final implementation.
The [support contract](hosted-sandbox-live-mode-support.md) now states the
initial admission, durability, retention, performance, and lifetime policies.
Its configuration/scenario declaration check is partial; gate 0B still needs
complete metadata validation and executable scenario bindings.

| Concern | Owner | Boundary |
| --- | --- | --- |
| Initial target selection | `serve/entries/init-payload.ts`, `serve/entries/worker-runtime.ts` | Resolve initialization before choosing a transport. A failed hosted selection cannot create a different local store. SDK operation families do not select hosting. |
| Application lifetime | App registry and `serve/entries/app-client.ts` | Explicit app deletion releases its client. Transport interruption does not delete the app. |
| Client calls and listener intent | `serve/worker/client/core.ts` and its lifecycle collaborators | Own correlation and subscription records. Pending service/tool records share a count/byte budget by physical port and logical relayed client, refusing operation 257 or aggregate JSON UTF-8 messages above 24 MiB before registration/send. Cleanup messages retain admission at saturation. The correlation-removal helper releases each reservation exactly once for replies, errors, failed posts, timed RPC cancellation and disposal; the final record also retires its budget without a separate client registry. Retain Firestore subscription messages for restoration on the same logical port; never retain one-shot operations for replay. Release failed posts and local cancellations. Preserve the distinction between unsent work, uncertain outcomes, and deletion. Complete host/remote release accounting, other service restoration and failed subscription posting remain unfinished. |
| Operation admission policy | `bridge/operation-budget.ts` | Own one client's count and encoded-byte charges. Reserve before queue insertion, refuse over 256 calls or 24 MiB, and return a release callback for the accepting owner to invoke once on settlement. Browser, native SharedWorker, Service Worker, Node hosted, public remote, bridge worker-relay and served-bridge MCP clients share this policy while retaining their own ordering and session lifetimes. The module owns counters only, not queues, sockets, timers or a global client registry. Rendered tools and resource-template reads also share this policy. Remaining host tool execution admission still needs verification. |
| SharedWorker admission and scheduling | `serve/worker/client-message-queue.ts`, `serve/worker/entry.ts`, and `serve/worker/host/dispatch.ts` | Each physical MessagePort owns queues and operation counters by logical client. Native service/tool admission measures the complete JSON UTF-8 operation message and refuses totals above the shared 24 MiB limit. Accepted service/tool calls release their slot and byte charge in finally; completed tails leave the queue map. Physical disconnect waits for all existing client tails, and later traffic waits for its completion. The shared dispatcher refuses a tombstoned physical port before virtual-client registration or resume. Retirement tracks every accepted task. Virtual consumers can survive peer replacement; physical-port cleanup must not erase their separate session ownership. The browser bridge preserves each MCP caller ID as the existing worker client ID; public MCP/wire checks verify independent progress, same-caller ordering and continued use after a held upstream succeeds or fails. Tool-only callers use temporary reply adapters, while existing remote sessions retain their state. Closed-session retention, byte accounting in other adapters and complete disposal accounting remain open. |
| Service Worker relay admission | `serve/worker/service-worker-relay.ts` | The logical client owns a shared operation budget; each realm owns its port and serialized work. Replacement realms borrow the same budget while teardown waits for the old tail. Refuse count or byte overflow before enqueueing, using the shared policy. Finally releases accepted work, and disconnect keeps its existing queue position without consuming an operation slot. No parallel admission registry is added. Malformed channel envelopes and full startup/error disposal remain open. |
| SDK reference construction | `pyric/firestore/refs.ts`, `pyric/firestore/snapshots.ts`, `serve/worker/client/firestore-reference.ts`, shared value identity registry, and host query resolution | Modular document methods live outside the Admin error-translation proxy. Removing or replacing a converter keeps a usable modular reference and does not mutate the original. The factory registers both backing and modular reference identities for query capture. Decoded references borrow the receiving SDK owner or client port. Host query references use the selected database. Stored values carry reference identity without owning a connection. |
| Physical browser socket | `serve/worker/client/websocket-connection.ts` | Own socket generations, attach deadlines, reconnect backoff, the private resume grant and physical connectivity observers. Its optional ClientPort observation method reports attachment/loss without exposing transport selection to service callers. Ignore replaced-socket callbacks. Cancel timers on close, reject uncertain calls through the existing correlation owner, and ask that owner to restore Firestore listener intent after admission. One browser-owned heartbeat timer sends pings every 15 seconds and enters the same interruption path after 45 seconds without incoming liveness. Replacement invalidates old callbacks before closing the socket; app deletion cancels heartbeat and reconnect timers. Public browser checks cover silent loss without close delivery, no write replay, healthy idle liveness and deletion before/after recovery. Remaining restoration ordering, other liveness owners, queue bounds and terminal listener errors retain their separate requirements. |
| RTDB state replacement | `serve/worker/host/rtdb.ts` and `pyric/database/sandbox/persistence-state.ts` | The host owns per-port disconnect queues. Their single sandbox reset subscription clears registered intent before import/reset/restore replaces data; the sandbox releases the subscription on disposal. Callers do not clear these queues separately. RTDB reset clears data and priorities while preserving rules. In-flight registration/drain work and full metadata/resource transitions remain unproven. |
| RTDB client connectivity | `serve/worker/client/rtdb-connection-lifecycle.ts`, `serve/worker/client/rtdb-listeners.ts`, `pyric/database/connection-lifecycle.ts` and `pyric/database/listeners.ts` | The worker adapter owns the app's explicit network-enabled choice per logical port and combines it with observed physical connectivity. In-page execution observes its existing RtdbConnectionLifecycle rather than adding another online-state owner. Both adapters suppress unchanged deliveries and use existing snapshot construction and listener registries. The in-page registration joins the existing app cleanup owner before its synchronous initial callback. Normal .info/connected and parent .info listeners follow explicit controls; unsubscribe, onlyOnce and deleted-app refusal are reviewed across all three paths. Parent metadata queries use the shared pure projection and retain only the previous selected rows for unchanged-result suppression. The in-page registry records the query scope so off preserves a sibling parent listener. Scalar metadata queries, remaining constraints, reset/import, separate get semantics and complete host online/offline behavior remain required. |
| Admission and authority | `bridge/server/peer.ts`, `bridge/server/bridge.ts` | Bind incoming work to its admitted consumer and current generation. Reserve the hosted authority. A supplied session ID is not authorization. |
| Consumer presence and identity admission | `bridge/server/consumer-registry.ts`, shared `bridge/frame-output.ts`, and `auth/identity.ts` | The registry checks the proposed complete presence frame before registration or lens mutation. The shared identity result carries refusal through both socket callers and targeted MCP tools. Rejected updates preserve the existing record and listeners; native serializer exhaustion is contained at this admission boundary. No new connection, timer or subscription owner is introduced. |
| Retained hosted worker sessions | `bridge/server/worker-sessions.ts` | Own host-issued resume grants, logical consumer identity, connection leases and 60-second interruption expiry. Keep grants out of consumer presence. Socket detach retains identity; explicit deletion retires the grant; mount shutdown closes the owner. Full admission, stale-generation, expiry and resource-bound proofs remain required. |
| Import validation | `pyric/sandbox/persistence/import-bundle.ts` and `serve/worker/host/connection.ts` | Decode and validate replacement input before mutation. Reuse the persistence checksum and value decoder; leave recovery-reader quarantine unchanged. Both host transports return validation errors. Complete raw legacy/schema validation and transactional all-service replacement remain required. |
| Public remote pending operations | `remote/index.ts` | The existing pending map owns correlation, deadlines and event-loop holds. Reserve the complete outgoing request through the shared count/byte policy before registering correlation or a timer. Success/error replies, timeout, send failure and disposal settle the operation promise, whose finally releases its reservation. Existing paths remove correlation records and event-loop holds. No replay queue is introduced. Complete reply validation and lifecycle accounting remain open. |
| Bridge worker-relay pending operations | `bridge/server/bridge.ts` | Pending correlation records for one admitted client share an operation budget. Reserve the complete outgoing JSON UTF-8 worker-op frame before registration or forwarding, with limits of 256 calls and 24 MiB. Each operation promise releases its reservation in finally. Existing response/error/timeout, detach/disconnect and peer-loss paths remove records and cancel timers; removing the final record retires the budget without a separate client registry. Saturation of one consumer preserves another consumer's admission. MCP tool forwarding and worker-port host admission remain separate boundaries. |
| MCP tool admission and forwarding cancellation | `bridge/server/mcp.ts` and `bridge/server/bridge.ts` | Each server built by buildMcpServer owns its session's 256-tool/24 MiB allowance across forwarded and in-process handlers. Charge the normalized call's name and arguments as JSON UTF-8 before executing a handler; release in finally and audit overflow as a refusal. This charge excludes the HTTP/JSON-RPC envelope and later bridge reminting. The separate rendered tool registration owns its own budget; it is not covered by this owner. Forwarded dispatch receives the SDK request AbortSignal, removes the correlation record and timer on cancellation, and removes its abort listener on every settlement path. SDK session deletion aborts the same requests. No separate caller registry is introduced. Cancellation does not undo work already dispatched; cooperative cancellation within in-process tool handlers and broader session lifecycle accounting remain open. |
| Rendered MCP tool/resource admission | `bridge/server/surface-server.ts` | One registration owns a shared 256-call/24 MiB budget across its rendered tools and resource-template reads. Charge normalized name/arguments before execute, release in finally on success or failure, and preserve the canonical operation/action audit and MCP result paths for refusals. Reuse the typed SDK tool-registration boundary from mcp.ts. Real stdio checks verify oversized-call refusal, cross-service count saturation and refill after successful/failed platform reads. Resource reads charge the requested URI and parsed variables before execution through the same module-local reservation helper. Real stdio checks cover shared count refusal and repeated successful/failed resource reads; SDK URI-length refusal occurs before this owner. Aggregate stdio tests admit exactly 24 MiB, refuse one byte above it, refuse resource reads and mutations while full, and verify partial/full refill after success or failure. Active-work shutdown and downstream host execution admission remain open. |
| Node MCP execution ordering | `bridge/server/mcp.ts`, `bridge/server/bridge.ts`, and `serve/hosted/runtime.ts` | One MCP registration mints an execution caller ID carried as optional tool-call metadata, separate from auth. The Node host retains one ordered promise tail and shared operation budget per active caller. It reserves the complete forwarded message before enqueueing, refuses above 256 calls or 24 MiB, and releases in finally when host work settles. Bridge cancellation cannot release the host reservation. The owner is removed when its last work settles. New execution owners are refused at the mounted MCP session ceiling (64), including owners whose MCP session has already closed. The protocol declares this ceiling once for both mounted session and Node execution admission. Independent callers can read while another waits on persistence. Missing caller IDs retain a legacy queue; direct service commands keep their own method chain. Close includes current tails. Real hosted checks cover cross-session read progress, same-session ordering and successful/failed persistence. Real cancellation/HTTP checks cover retained count/byte admission and drain after successful/failed persistence. A public SDK observer verifies every admitted mutation across two 64-owner fill/drain rounds, proving retained-owner capacity returns after completion. Exact/partial-drain byte boundaries, direct-command admission and full close accounting remain required. |
| Hosted remote relay | `serve/hosted/runtime.ts` | Translate published remote operations and subscriptions into the existing host's per-consumer queue. Each admitted consumer owns its port, auth session and cleanup; the adapter forwards results and errors and marks remote provenance. Its queue uses `bridge/operation-budget.ts` to admit at most 256 pending service/tool operations and 24 MiB of encoded operation messages, releasing each reservation in finally; control/subscription messages do not consume those operation slots. Legacy operation routing binds the admitted identity in `bridge/server/peer.ts`. Subscription admission, unbounded event relay and complete service/reconnect/lifetime accounting remain required. |
| Checkpoint storage and restored subscriptions | `serve/worker/host/connection.ts`, `serve/worker/host/subscriptions.ts`, `serve/worker/host-context.ts`, and `serve/hosted/runtime.ts` | Hosted checkpoints reuse `pyric/sandbox/checkpoints/directory`; SharedWorker retains its record backend. The shared subscription owner retains each original data-listener intent, including explicit lenses, and restores it with original page provenance after checkpoint replacement. Auth changes select only app-session intents. Unsubscribe and port cleanup remove retained intent. Hosted restore awaits persistence and refuses execution while unhealthy. Corrupt checkpoint validation, full metadata fidelity, and import/reset restoration remain required. |
| Saved account and object schemas | `pyric/sandbox/internal/state-schemas.ts`, `pyric/auth/seed-user.ts`, and `pyric/storage/persistence.ts` | Shared schemas validate checkpoint and host-state fields. Auth owns the dependency-free seed record type; Storage owns stored metadata. Schema completeness checks import these types directly, without depending on service execution or the sandbox barrel. Existing Auth exports re-export the same seed type. |
| Shared host execution | `serve/worker/host/`, `serve/worker/serve-init.ts` | Own execution, per-consumer ordering, identities, and accepted-work cleanup across host runtimes. Keep sandbox behavior out of transport shells. |
| Atomic Firestore write intent | `pyric/firestore/sandbox/admin-compat/batch.ts`, `pyric/firestore/sandbox/batch-write-executor.ts`, `pyric/firestore/sandbox/transaction-write-executor.ts`, `pyric/firestore/sandbox/atomic-write-pipeline.ts`, `pyric/firestore/sandbox/field-merge.ts`, and `pyric/firestore/sandbox/write-runtime.ts` | Preserve queued storage intent. Shared preparation resolves against the preceding projected state and validates deletion sequences before mutation. Rules methods use original existence and final projected state. Both host transports and write APIs reuse the engine. Queued-input lifetime, complete validation, event/capture/replay intent and modular type alignment remain unfinished. |
| Queued write inputs | `serve/worker/client/firestore-writes.ts`, `pyric/firestore/sandbox/admin-compat/batch.ts`, `pyric/firestore/sandbox/transaction.ts`, and `pyric/firestore/sandbox/document-copy.ts` | Both write APIs capture field masks, nested set/update containers, and native Dates across hosted, SharedWorker and in-page paths. Host adapters encode Dates through the existing timestamp marker; in-page queues copy Dates without resolving transforms and preserve trusted modular references as scalar values. Transaction reads and field merging reuse the same document copier; native structured cloning must not erase stored reference types. Other mutable values, complete own-key/class fidelity, pending single-write options and invalid-input validation still need proof. |
| Timestamp bounds and storage precision | `pyric/firestore/timestamp.ts`, `pyric/firestore/sandbox/document-copy.ts`, and `pyric/firestore/sandbox/converters/user-timestamp.ts` | The SDK value class validates component ranges and retains its original precision. Queued Date copying reuses its factory before accepting the write. The existing write converter applies Firestore microsecond precision to SDK and decoded timestamps before rules and storage. Invalid/non-finite inputs and complete inbound validation remain open. |
| Firestore wire query structure | `serve/worker/host/query-structure.ts` and `serve/worker/host/core.ts` | Validate target nodes, child arrays and composite-filter children before reconstructing queries. Query-source and composite-filter nesting share a 64-layer limit along each path, separate from encoded document depth. Both hosted and SharedWorker reads/counts/aggregates/listeners enter through this guard; SDK validators retain leaf argument semantics. Rejected subscriptions never reach registration. Aggregate replies copy numeric/null fields out of the sandbox's Proxy-backed result into a cloneable wire object. |
| Query operand capture and execution | `pyric/firestore/sandbox/query-operand-equality.ts` and `pyric/firestore/sandbox/query-execution.ts` | Capture normalizes trusted timestamp identities for both comparison and execution without changing the caller's object or interpreting ordinary maps as timestamps. Execution applies the existing normalized order, including the default document key and existing top-k path. No wire decoder or rules-language timestamp representation owns storage precision. |
| Modular transaction document reads | `pyric/firestore/transactions.ts`, `pyric/firestore/snapshots.ts`, `pyric/firestore/state.ts`, and worker client snapshot/read adapters | Each callback receives a modular adapter over a fresh backing transaction. Its bound read retains conflict tracking and the operation-start identity. Document snapshots apply converters lazily and retain the converter on their reference for subsequent reads. Snapshot value decoding uses the receiving owner's reference factory. The existing Admin query-read extension is forwarded unchanged. Public type fixtures cover converted document reads in both published adapters; complete reference-producer coverage, full snapshot/overload declarations and query/listener parity remain unfinished. |
| Converted model writes | `pyric/firestore/transactions.ts`, `pyric/firestore/types.ts`, and `serve/worker/client/firestore-writes.ts` | Apply `toFirestore` before write rules and storage, capturing converted batch inputs at queue time and transaction inputs per attempt. Each adapter shares its set conversion path; update payloads retain their existing direct path. The modular batch owns an explicit SDK-shaped interface over its backing batch and retains the existing per-commit auth override. Public fixtures exercise model writes without index signatures. Converter options, complete overloads, exception timing and other reference producers still need proof. |
| Document root validation | `pyric/firestore/internal/document-data.ts`, `pyric/firestore/plain-object.ts`, modular atomic writes, and worker set adapters | Reuse one plain-object predicate and the shared FirebaseError. Validate after conversion and before queue insertion, preserving previously accepted writes when a caller catches a rejected input. The worker consumes the validator through the existing value-codec internal export without importing the engine. Root checks do not establish nested value validation, complete single-write/update coverage or full public type compatibility. |
| Firestore wire paths and atomic requests | `serve/worker/protocol/firestore-validation.ts` and `serve/worker/host/firestore-writes.ts` | Require string paths before constructing setDoc, atomic-write and transaction-read references. Reuse the shared FirebaseError and leave segment validation with the SDK. One typed atomic writer applies both APIs and rejects invalid descriptor containers and unknown methods. A shared assertion checks read/write list containers, proving only unknown[] rather than validated entries. Transaction reads validate entry/data containers and JSON string/syntax, then use the existing encoding decoder and document-root validator before conflict comparison. Full nested-value schemas, write options/values, other paths, envelopes, ownership and resource limits remain open. |
| Document backing and atomic application | `pyric/firestore/sandbox/local-state.ts`, `pyric/firestore/sandbox/atomic-state.ts`, and `pyric/firestore/sandbox/document-store.ts` | LocalState owns backing storage and document versions. Atomic application validates ordered existence, captures prior state and reports committed version changes. The type-only store contracts retain their original LocalState exports and avoid a runtime dependency cycle. Raw internal resolution-failure atomicity still needs verification. |
| Node host shell | `serve/hosted/runtime.ts` | Supply Node dependencies and wire the shared host. Queue interruption cleanup after accepted work and reuse the RTDB disconnect queue without destroying retained Auth. Cleanup failures reach host stderr. Its current relative-fetch facade and shell-local scheduling are prototype work to replace through shared boot/execution seams; shutdown tracks already-closing port cleanup separately from active ports and waits for it before sandbox disposal. Public wire/CLI checks verify queued work survives app deletion followed by shutdown, including upstream failure; broader startup and shutdown accounting remains open. |
| Hosted CLI project ownership | `serve/hosted/project-ownership.ts` and `cli/serve.ts` | Acquire a non-blocking SQLite write reservation on the canonical state's `host.lock` before CLI startup can load or reset state. Use the SQLite implementation built into the current Node/Bun runtime; the ownership file contains no application data. Keep the file in place, close the reservation after owned runtime resources, and release on startup failure or process termination. A public CLI/browser characterization on Node/macOS verifies SIGTERM and SIGINT during paused startup after port binding: no readiness announcement, then successful replacement and browser write on the same project and port. This is process termination, not in-process startup cancellation. `BEGIN IMMEDIATE` avoids an exclusive-lock upgrade race between simultaneous starters. The hosted CLI, CLI browser persistence, Vite persistence, in-process MCP and one-shot in-process service commands acquire this reservation before state access. MCP holds it through shutdown; one-shot commands hold it through save. Vite teardown is bound to each server's captured generation, so closing an old server cannot release its replacement's state. The complete writer inventory, shared in-process/hosted runtime and storage adoption, full shutdown accounting and authoritative persistence remain required. |
| HTTP state channel ownership | `serve/sandbox-session.ts` and `serve/namespace.ts` | The session declares `browser` or `host` ownership. Hosted state remains readable after Host/Origin/session admission; browser mutations receive 423 before body collection. Browser mirroring remains the default for existing namespace callers. The HTTP lock cannot grant ownership of a hosted store. Separate CLI/Vite process ownership still requires proof. Named-condition review is confined to the edited namespace factory; unchanged legacy functions remain excluded under the existing convention. |
| Hosted operator commands | `serve/hosted/runtime.ts`, `serve/hosted/method-protocol.ts`, `serve/bridge-mount.ts`, and `cli/hosted-method.ts` | The runtime owns the sandbox and CLI method context. Each admitted HTTP connection owns its ordered direct-command tail; the mount supplies this identity outside request JSON. Last-tail settlement removes the execution owner, and host close awaits accepted tails. A separate connection can read while one is stalled. Each connection owns a shared operation budget measured from the complete parsed request envelope before queue insertion. Finally releases the charge after command settlement, including handled failure. Count/byte saturation and partial/full byte reuse after successful or failed execution are verified through real HTTP on the same connection. A separate 64-owner cap bounds active and retained direct-command connections. Closed sockets keep their accepted work's owner until the final call settles; existing owners can still queue within their operation budget at the cap. Two complete churn/drain cycles and accepted mutation completion after caller closure are verified. Broad shutdown remains open. MCP reuses the sandbox tool dispatcher; service CLI calls reuse validated method records and the host project directory. The mount owns HTTP method, Host/Origin, envelope/body-size and instance checks. The runtime rejects a caller outside its canonical project tree before dispatch. The client reports host failures without opening another store. The service CLI reuses declared method effects for durable-mutation admission. CLI and MCP share result flushing even for unsuccessful outcomes that may contain partial changes. MCP reads and mutation admission use effects declared in the existing tool-family records. Returned mutation failures still flush possible partial changes; a failed flush preserves their receipt and adds uncertainty guidance. The canonical held-identity switch remains a memory-only CLI control. Successful reads disclose unhealthy memory; rules refusals retain their error. CLI and bridge MCP lens contexts currently remain distinct; complete service/control classification, partial-failure outcomes, control parity, auditing, queue bounds and shutdown proofs remain open. |
| Stdio MCP discovery and request outcomes | `serve/discovery.ts`, `cli/mcp-proxy.ts`, `serve/mcp-project.ts`, and `serve/bridge-mount.ts` | Discovery returns the canonical directory containing the selected pointer. The proxy carries that directory and the discovered instance on every HTTP request; the endpoint checks both before creating or resuming a session. A copied pointer cannot establish project ownership and health success cannot authorize a replacement instance. Direct endpoint clients retain existing Host/Origin policy. The proxy owns request deadlines and reports unknown outcomes after send failure or deadline expiry without replaying requests. These headers do not authenticate clients; full admission, older-host refusal, request classification, late-response and resource-bound proofs remain required. |
| State representation and storage | `serve/state-file.ts`, `serve/state-store.ts`, `serve/hosted/persistence.ts`, `serve/hosted/runtime.ts`, and `serve/worker/host-context.ts` | The hosted adapter prevalidates the existing record bundle and writes it through the atomic state store. The runtime supplies its acknowledgment policy to the shared host; SharedWorker retains best-effort replies. A hosted SDK flush failure stops subsequent Firestore SDK mutations, and SDK/MCP callers receive explicit committed-state failures. Session/checkpoint records remain separate from the namespace-insensitive blob adapter. Full writer ownership, all-service restoration/admission, health/recovery, legacy import, backup counts and interrupted-commit guarantees remain unfinished. |
| Hosted SDK persistence admission | `serve/hosted/persistence-admission.ts` | One declared policy covers Firestore writes, full reset, RTDB set/remove and Storage upload/delete. Other RTDB/Auth mutations and host controls remain required. |
| Hosted Storage persistence | `pyric/storage/sandbox/persistence-state.ts` and `serve/hosted/persistence.ts` | The Storage foundation snapshots the handle's bucket with original metadata and bytes using existing base64 utilities. The Node adapter restores before admission and writes the optional Storage section through the existing atomic state store. The runtime queues complete explicit flushes so older asynchronous snapshots finish first. The shared Studio reset handler awaits that runtime policy before acknowledgment; SharedWorker retains best-effort persistence. Coherent snapshots during mutations, automatic controller flushes, reset generations, all buckets, full validation and atomic multi-service commits remain unfinished. |
| Browser live execution | `serve/entries/live/` | Use the actual App/Auth/Firestore owner and preserve upstream inputs, results, errors, and observer behavior. Execution cannot fall through to the sandbox. |
| Observation delivery | `serve/live/capture.ts` and host observation consumers | Own recording and eventual bounded delivery. Failure here must not change an upstream operation's outcome. Real upstream credentials never enter observations. |
| Browser value representation | `pyric/firestore/internal/value-codec`, `pyric/firestore/timestamp.ts`, `pyric/firestore/bytes.ts`, `pyric/firestore/geo-point.ts`, `pyric/firestore/vector-value.ts`, value wrappers, and value identity registries | Encode timestamps, bytes, locations, vectors, and registered references in modular writes, where inputs, and value cursors. Restore the existing public SDK scalar constructors for client reads while preserving rules values through the unary persistence decoder. Reuse scalar JSON validation for the SDK vector marker. The shared codec owns map escaping and encoding validation; operation/query descriptors and snapshot envelopes carry its declaration outside document fields. Legacy persistence retains its previous interpretation until migration. Reconstruct references with their receiving owner. Keep the codec free of rules and sandbox engines. Complete scalar, write-intent, qualified-reference, and collision handling remains required. |

Paths in the table are relative to `packages/cli/src` unless prefixed with
`pyric/`. A new responsibility needs a declared owner; it must not be hidden in
an SDK operation or appended to a generic manager merely because that manager
already has access to the required state.

The process lock relies on SQLite's [single-writer and immediate-transaction contract](https://www.sqlite.org/lang_transaction.html), through the built-in [Node 22.15 driver](https://nodejs.org/download/release/v22.15.0/docs/api/sqlite.html) or [Bun driver](https://bun.com/docs/runtime/sqlite). It adds no application schema or persistence format. Keep its connection alive and its pathname intact for the entire ownership lifetime.

## Browser leaf budgets

These are raw minified ESM bytes emitted by the installed esbuild version with
`platform: browser`, bundling enabled, and no Node shims. Each entry is built
independently, so the live entry includes its real Firebase SDK dependencies.
The budgets apply to these leaves, not to the entire served app, Studio, or
the existing in-page fallback bundle.

| Entry relative to the repository root | Initial measured bytes | Maximum bytes |
| --- | ---: | ---: |
| `packages/cli/src/serve/worker/client.ts` | 48,248 | 98,304 |
| `packages/cli/src/serve/worker/client/websocket-connection.ts` | 4,573 | 16,384 |
| `packages/cli/src/serve/worker/client/rtdb-listeners.ts` | 12,887 | 32,768 |
| `packages/pyric/src/firestore/internal/value-codec.ts` | 12,260 | 24,576 |
| `packages/cli/src/serve/entries/live/firestore.ts` | 387,843 | 524,288 |

Run `bun packages/cli/scripts/check-browser-boundaries.ts <entry> <max-bytes>`
after building workspace packages. The report names modules contributing bytes
to the output. The check rejects sandbox/rules engine contributors, CLI host
contributors, imports left outside the inspected bundle, and exceeded budgets.
Node imports fail the browser build instead of receiving shims.

The RTDB listener leaf has its own 32 KiB ceiling, declared before measuring
the query-projection integration. The older worker barrel does not re-export
these listeners, so its report cannot measure their cost. The shared
`pyric/database/internal/query-projection.ts` owns pure ordering, bounds and
window projection for both backend queries and browser-local metadata.
It imports only types and owns no data store, rules evaluator or resources.
The projection also owns its query types; the backend query module retains
its existing exports through a re-export. A scoped import audit includes
type-only edges and finds no cycle among the backend facade, projection and
JSON value definitions. This does not establish the whole repository graph;
the browser boundary checker and its engine exclusions are unchanged.

Shared Firebase errors, app-handle deletion state, value wrappers, browser
attribution helpers, and the two value identity registries are intentional
browser dependencies. Their directory/file exceptions are explicit in the
checker. Tree-shaken dependencies do not count as shipped engine code; the
remaining source import/cycle audit must still examine those dependencies.

Changing a budget or boundary requires an explained contract or dependency
change and new measurements. A failing gate alone does not justify changing
its limit. These artifact checks do not complete U4's source ownership,
transport-selection, and cycle requirements.
