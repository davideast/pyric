# Hosted sandbox hardening

Base: `6b0728b830faa788b53ea97fc764f5c3397fe14d` on `hosted-live-mode`.
This bounded milestone leaves the broader hosted/live-mode goal incomplete.
Use the already approved S1–S6 seams, vertical TDD, and applicable universal gates.
Do not modify the manual demo project. Commit and push each verified slice.

## Ordered queue

1. Anonymous UID uniqueness — verified locally. Deletion/restart cannot transfer UID-owned data to a new identity; retained accounts preserve their UID, claims, creation time and last-login time.
2. Session retention expiry — verified locally. The original app obtains fresh admission after the retention window, with Auth restored before listeners. Expired/invalid grants remain refused; app deletion cancels recovery; uncertain writes never replay.
3. Checkpoint restoration — verified locally. Account/Storage metadata, Firestore typed values and literal maps survive the scoped round trips and host restart. Corrupt service inputs refuse before reset; legacy records and branch consumers retain their value semantics. Broader atomicity/concurrency gaps remain explicit.
4. Restoration diagnostics — verified locally. Startup text and readiness JSON count bucketed documents and controller Auth accounts correctly, including Auth-only and fresh state.
5. Interrupted recovery — verified locally. A second interruption restores the original identity and one listener; delayed old-socket events cannot overwrite or disconnect the recovered app, and deletion during Auth restoration cancels recovery.
6. Reset/import with active apps — verified locally. Hosted and default SharedWorker listeners follow replacement without reviving unsubscribed listeners; paused transactions retry against replacement state. Complete portable imports restore Storage bytes/metadata and await durable persistence, including an older asynchronous save and hard restart.
7. Persistence-failure recovery — verified locally. Failed writes report uncertainty; mutations remain refused even after permissions are repaired. Restart restores durable documents/Storage bytes, the original app does not replay its uncertain increment, and new writes succeed.
8. Malformed requests — in progress. Invalid outer JSON/envelope kinds, worker envelopes and malformed browser/legacy relay correlation IDs reject before dispatch. Malformed remote identity-lens containers, modes, impersonation fields and request/session IDs refuse before lens mutation or buffered writes. Legacy operation/subscription envelope shapes also refuse; unsupported handshake versions cannot admit consumers or replace the SharedWorker peer, and buffered frames cannot operate after server refusal. Browser and public remote consumers reject incompatible host acknowledgment versions before readiness or queued writes. Hosted browser acknowledgments require a string capability list containing worker-port, rejecting malformed lists before queued SDK writes. Incompatible peer acknowledgments disconnect the bridge while local SharedWorker SDK access remains usable. Healthy apps remain usable and recoverable protocol errors permit reconnect. Mounted serve/Vite and standalone bridge input enforce the 12 MiB encoded-message limit, including fragments, while mounted runtimes preserve supported 8 MiB Storage transfers. Peer handshakes also validate identity, tools and capabilities before registration. Standalone outer JSON/envelopes now refuse before buffered work, using the same parser as the mounted handler. Firestore write preparation, transaction read-set canonicalization and filter/cursor operand decoding enforce 64 encoded containers before recursive decoding, with public SDK boundary, atomicity, retry, count and listener checks in hosted and default SharedWorker. Other known-frame/reply fields, detailed targets/service arguments, outbound size limits and depth limits on other decode paths remain open.
9. Lifecycle cleanup — pending. Repeated startup failures, interrupted initialization, reconnect, deletion and shutdown release resources and ownership.
10. Sleep/resume with delayed disconnect notification — pending. An expired host session recovers even when the browser observes the interruption late; invalid grants remain refused and uncertain writes never replay.
11. Identity and tenant isolation across clients — pending. Switching or signing out in one app cannot change another app's identity, tenant, claims or Rules access, including after recovery.
12. RTDB disconnect behavior — pending. Connectivity signals and registered disconnect operations follow the declared session-lifetime contract across transient loss, expiry and explicit app deletion.
13. Concurrent transactions and atomic writes — pending. Two clients contend through normal SDK calls without lost updates; rejected batches remain atomic and ambiguous acknowledgments do not trigger transport replay.
14. Packed installation and runtime selection — pending. An isolated consumer uses the built package through served imports and Vite cold/warm startup, reload and HMR; default SharedWorker, explicit hosted and in-page select their intended implementation.
15. Origin and project admission isolation — pending. An unrelated browser origin or another project's discovery/session credentials cannot attach to, inspect or mutate this host; intended clients still connect.
16. Slow-client isolation and bounded event delivery — pending. A stalled Studio/event consumer cannot exhaust retained queues or starve another app's SDK operations; overflow and lost history remain explicit.
17. Rules hot reload with active apps — pending. Valid Rules changes update enforcement and active listeners according to the existing service contract; invalid edits report their failure without falsely claiming successful enforcement.
18. Client/host version compatibility — pending. Actual packed consumers within the declared compatibility range work against the candidate host; unsupported combinations refuse clearly before mutation and never silently select another runtime.
19. Fault diagnostics and redaction — pending. Existing CLI, runtime and Studio diagnostics distinguish connection, restoration and persistence failures while excluding credentials and private document contents from default support output.
20. Combined verification and morning handoff — verified locally for the completed milestone slices. See `docs/hosted-hardening-handoff.md`: 216 affected browser scenarios, 1,138 regressions, strict types, 123-file code form, scoped import review, matching browser budgets and four rebuilt copied-standalone checks. Remote backup is complete following direct user authorization; item 8 is partial and 9–19 remain open.

### Added follow-on gates

These five additions extend the ordered queue; they do not promise overnight completion or expand it into hosted live-mode implementation. Finish the current verified slice before advancing. Reuse existing evidence when its inputs and assertions cover the requirement; reproduce a failure before changing implementation.

| Item | Prerequisites | Required evidence before completion |
| --- | --- | --- |
| 10. Delayed disconnect | Session expiry and interrupted recovery (2, 5) | Separate host expiry from browser notification in a public lifecycle fixture. Prove recovery of the original valid identity and listeners, a subsequent authorized write, invalid-grant refusal and absence of mutation replay. An observed-interruption clock test alone is insufficient. |
| 11. Identity isolation | Identity, recovery and active-app replacement (1, 5, 6) | Two independent browser contexts with distinct users/tenants exercise allowed and denied SDK operations before and after one changes identity and reconnects. Assert each client's resulting user/token and downstream Rules decision; compare SharedWorker behavior where applicable. |
| 12. RTDB disconnect | Recovery and cleanup (5, 9, 10) | Public SDK observers and a second client verify `.info/connected`, disconnect execution timing, cancellation and absence of duplicate effects across transient loss, expiry and deletion. Resolve expected behavior against the existing support contract before writing assertions. |
| 13. Concurrent writes | Persistence failure and request validation (7, 8) | Two clients produce a real transaction conflict and the expected committed result through SDK retry semantics. A rejected batch changes no documents. A lost-acknowledgment fixture proves the transport does not replay a non-idempotent mutation. Reuse existing engine semantics rather than adding another scheduler. |
| 14. Packed consumer | Completed source fixes and cleanup (9) | Install the actual package in an isolated directory outside the workspace. Verify selected runtime and an SDK write/listener round trip through served imports and Vite, including reload/HMR without duplicate delivery. Run applicable minimum-Node and browser-boundary checks; retain package identity and commands so final verification can reuse this evidence. |

Every item also requires the advancement checks below. Broader redesigns discovered by these probes are recorded separately, with the failing scoped requirement left open. At the stopping point, perform the final verification and handoff for completed slices even if later queue items remain untouched.

### Second five additions

Items 15–19 extend the existing queue at the user's request; final verification moves to item 20. They audit existing hosted behavior and do not expand live-mode implementation. Current work and its prerequisite order remain intact. These are completion conditions, not claims that failures have already been reproduced.

| Item | Prerequisites | Required evidence before completion |
| --- | --- | --- |
| 15. Admission isolation | Request validation and packed consumer (8, 14) | Through real browser/socket and CLI discovery boundaries, try an unrelated origin, another project's discovery record and a grant issued by another host. Prohibited attempts refuse before exposing state or changing data; an authorized client still completes a write/listener round trip. Record the existing admission policy and test supported configurations without broadening network exposure. |
| 16. Slow-client isolation | Request bounds and cleanup (8, 9) | Stall one event consumer while another app performs SDK operations. Before implementation, record workload, configured queue/history bounds, duration sufficient to reach those bounds, and numerical memory/latency budgets. Measure bounded retention and healthy-client responsiveness, assert the declared overflow/gap signal, then verify disconnect cleanup. A short happy-path run or undefined budget cannot pass. |
| 17. Rules hot reload | Active-app replacement and identity isolation (6, 11) | Edit real fixture Rules files while two clients retain listeners. Check allowed and denied SDK operations before and after valid changes, listener behavior, an invalid edit and subsequent repair. Preserve each service's established invalid-source policy; do not assume every service retains its last valid Rules. Assert diagnostics agree with actual enforcement and compare supported SharedWorker behavior. |
| 18. Version compatibility | Packed installation (14) | Record the supported client/host version range first. Install representative actual artifacts in isolated consumers, retaining versions and package hashes. Supported combinations pass Auth, SDK write/listener and reconnect checks. Unsupported combinations produce an actionable compatibility error before mutation or runtime fallback. Malformed-version frame coverage from item 8 alone is insufficient; this does not create a new backwards-compatibility promise. |
| 19. Fault diagnostics | Restoration, persistence recovery and event delivery (4, 7, 16) | Induce connection refusal, restoration failure and persistence failure in disposable fixtures. Existing public diagnostics identify the selected runtime and failing stage without contradicting SDK outcomes. Seed recognizable credential and private-data markers; verify default diagnostic/support output excludes them and recovery clears stale failure status. Reuse existing surfaces rather than introducing a new telemetry system. |

Apply the same approved seams, vertical TDD and universal gates to these additions. Reuse matching existing evidence; a characterization that already passes needs no invented fix. If the night ends earlier, prepare item 20's handoff for completed slices and explicitly list untouched items. Production Firebase and the manual demo remain outside this work.

## Advancement requirements

Each behavioral change needs an actual public-boundary red and unchanged-fixture green, separate review, current affected regression results, strict production/fixture types, and changed-code form. Runtime and browser-boundary checks apply to the touched owner. Existing passing coverage may be reused only with matching inputs and scope. Larger architectural gaps remain explicitly open; they do not waive a failed scoped acceptance test.

## Evidence

The previous planning turn changed no implementation. Initial inspection confirms the clean worktree and exact pushed base above. Task 1 begins at normal served SDK imports plus the public remote admin and real host lifecycle (S1/S2/S3).

### U3 tooling prerequisite

Editing one Auth method exposed a checker mismatch with U3's modified-function scope: the checker rechecked every untouched method in the enclosing class. The checker now explicitly records unchanged class members only when the class header is unchanged. Modified members remain checked in full; new classes and changed headers retain whole-class checking. This changes no U3 code-form rule and adds no source-specific exemption. The broad Auth backend split remains separate from the user-authorized narrow repair.

The S6 scope regression failed before the correction (`/tmp/pyric-hardening-class-scope-red.log`: 12 pass, 1 fail) and passed unchanged afterward (`/tmp/pyric-hardening-class-scope-green.log`: 13 pass). Review covers constructors, field initializers, accessors, static blocks, changed headers, duplicate members and added suppressions. Final checker/API/CLI/CI tests pass 28 cases in 8.61 seconds; strict tool types and the current changed-code check pass. Reports: `/tmp/pyric-hardening-class-scope-review.log`, `/tmp/pyric-hardening-class-scope-types.log`, `/tmp/pyric-hardening-uid-code-form-review.json`.

### Task 1 verification

The normal SDK regression twice reproduced both `anonymous-1` reuse and access to the deleted user's owner-only document. A public Auth probe reproduced the repeated UID in two independent sandboxes without browser or persistence involvement. UUID allocation in the existing shared anonymous factory passes the unchanged original regression (4.6 seconds total); the retired counter is removed. No new runtime branch, dependency, persistence schema or resource owner is introduced.

An initial retained-account fixture incorrectly reused its saved browser session; explicit SDK sign-out corrected that setup. The corrected test then exposed a real persistence defect: creation time changed and last-login time became null. A second unchanged-fixture red/green cycle now preserves both through the existing export/seed contract and state codec. The new fields are optional so legacy seeds remain readable. Existing exact-shape export tests now explicitly expect the additional timestamp fields and retain their other assertions.

Final scoped verification, all terminal and without retries:

| Check | Result | Report |
| --- | --- | --- |
| Identity, restart/admission, Auth durability, legacy seeds, checkpoints, three runtime paths | 32 passed in 1.1 minutes | `/tmp/pyric-hardening-identity-final-browser.log` |
| All six identity cases under Node 22.15.0 | 6 passed in 16.3 seconds | `/tmp/pyric-hardening-identity-minimum-node.log` |
| Auth, app registry, persistence, worker identity and state codec | 487 passed, 42 isolated files, zero skips | `/tmp/pyric-hardening-identity-final-regressions-corrected.log` |
| Pyric, CLI, hosted fixture strict types | Passed | `/tmp/pyric-hardening-identity-final-{pyric,cli,fixture}-types.log` |
| Code form from milestone base | Eight TypeScript files, zero findings | `/tmp/pyric-hardening-identity-final-code-form.json` |
| Browser client / live entry | 54,943/98,304 and 387,843/524,288 bytes; zero boundary findings | `/tmp/pyric-hardening-identity-browser-{client,live}.json` |

Red/green logs, fixture copies and current input hashes are archived in `ignored/hardening/uid/`. The initial Chromium sandbox refusal, incorrect saved-session fixture, and nonexistent state-test path are retained as setup/review failures, not behavioral red evidence. The corrected final regression invocation uses `packages/cli/test/serve/state-store.test.ts`.

No new resource owner, Buffer/browser fallback, or conformance registry row was added. Existing Rules evaluation is exercised through both allowed and denied public SDK reads/writes. The copied standalone artifact and whole feature/release gates remain unverified; final milestone integration will cover applicable packaging. The manual demo was not changed.

## Remote backup

Resolved: the user explicitly authorized pushing after the QA handoff. The push to `origin/hosted-live-mode` succeeded from `6b0728b8` through `3f3a4ed6`, backing up all 43 verified commits. The earlier blocked-state record below is historical.

Tooling commit `506c0383` is local. Automatic approval review rejected its push twice, requiring direct user authorization despite the active goal's explicit push instruction. An asynchronous approval question is pending for this and subsequent verified hardening commits. Continue independent local work; do not bypass the rejection or claim the branch is remotely backed up beyond `6b0728b8`.

Identity task commit: `9b418216627fa53d892dbc0580d7e51b0db2616b`, also local pending push authorization.

## Task 2 verification

`bun x playwright test --config packages/cli/test/e2e/hosted/playwright.config.ts session-expiry.pw.ts` failed at the intended SDK listener assertion: expected `After expiry`, received `Before expiry` after the real 60-second host retention window. The isolated fixture drops attach frames for 62 seconds, keeps another remote consumer writing, then permits reconnection. It also requires a subsequent owner-authorized write, the original UID, and no new anonymous account. Report: `/tmp/pyric-hardening-session-expiry-red.log`, terminal exit 1 (`4cd3d4`). The unchanged fixture passed after the repair (`/tmp/pyric-hardening-session-expiry-green.log`, terminal exit 0 `381fc1`). The original fixture and reports are preserved in `ignored/hardening/session-expiry/`.

The client previously retried the expired grant and treated its refusal as terminal. It now measures the observed interruption with the monotonic browser clock across connection attempts, discards the resume grant after the shared 60-second policy window, and explicitly requests fresh admission. The existing restoration owner configures Auth before observers and operations. A failed fresh attempt keeps retrying; a protocol refusal remains terminal. The host's resume validation and resource-expiry behavior are unchanged. No secrets are persisted and no second session owner is introduced.

Review added a direct old-grant refusal after the real window and four short browser-clock cases: deleted user, deleted app, invalid grant, and a failed first fresh-admission attempt. The clock cases exercise client decisions; the real-clock case separately proves the host actually expires the old grant.

Final current checks: 26 focused browser cases in 2.0 minutes, four short cases on Node 22.15.0 in 10.3 seconds, 59 session/relay regressions in six isolated processes, strict CLI/fixture types, four-file code form with zero findings, and three browser boundaries. Socket: 7,259/16,384 bytes; worker client: 54,943/98,304; RTDB listeners: 12,930/32,768. Reports use `/tmp/pyric-hardening-session-expiry-`; archived input hashes bind them to the source and emitted CLI files. The minimum-Node run did not repeat the long real-clock scenario. Whole-feature and copied-standalone checks remain reserved for the final milestone join.

## Task 3: Auth refresh slice

The all-service checkpoint probe reached restore after correcting two fixture assumptions: use modular `reload(user)` and the supported RTDB snapshot `priority` property. Its actual restore assertion exposed stale Auth claims and regenerated Storage metadata. These are separate failures; the combined round trip remains open.

Minimization showed that claim reads without reload already worked. The worker's shared refresh helper wrote the session's old token claims into the account before re-minting it. Thus reload could undo both an admin revocation and a restored checkpoint. The helper now reads the stored account and keeps the port tenant, removing the stale write and conditional spread. Email/password refresh use the same correction. No new service, adapter, dependency, resource owner or runtime decision is introduced.

`bun x playwright test --config packages/cli/test/e2e/hosted/playwright.config.ts checkpoint-auth.pw.ts` reproduced expected `saved`, received `changed` after restore and reload (`/tmp/pyric-hardening-checkpoint-auth-red.log`, exit 1). The exact fixture then passed unchanged in 3.9 seconds (`/tmp/pyric-hardening-checkpoint-auth-green.log`, exit 0). Both fixture and logs are archived in `ignored/hardening/checkpoint/`. Earlier passing minimization probes and the pre-restore reload failure are diagnostic evidence, not the final red/green pair.

That review labeled all three refresh operations as hosted, default SharedWorker and in-page, but the runtime-selection correction below withdraws the old in-page claim (those cases ran SharedWorker): the SDK sees the revoked claims, retains its tenant, fails a protected write, and the public admin account retains the revocation. Final affected checks pass 33 browser cases in 54.4 seconds, ten minimum-Node cases in 17.2 seconds, and 57 regressions in four isolated files. CLI and fixture strict types pass; changed-code form has zero findings. Reports use `/tmp/pyric-hardening-checkpoint-auth-`. The source change is confined to the host helper; browser leaves and shared SDK implementation are unchanged. Applicable packaging remains at the final join. This slice does not close the all-service checkpoint or corruption requirements.

Auth refresh commit: `87c7e951`, local pending push authorization.

## Task 3: Storage round-trip slice

After the Auth repair, the original all-service test still failed only on Storage generation and timestamps. The minimized supported-SDK probe also showed that `cacheControl` disappeared. Capture retained only a few upload fields, and apply used a new upload that regenerated object metadata. The shared full-state owner now uses the existing lossless Storage persistence functions. Optional extra metadata and Blob type preserve current records; legacy records retain their upload-based compatibility path. Existing path ordering is unchanged. This adds no runtime selection, new persistence engine, service owner or base64 implementation.

The minimized regression failed at the metadata equality assertion (`/tmp/pyric-hardening-checkpoint-storage-minimal-red.log`, exit 1). Both it and the original four-service regression passed unchanged in `/tmp/pyric-hardening-checkpoint-storage-green.log` (two cases, 10.8 seconds). The first attempted minimized fixture called the explicitly unsupported served `updateMetadata`; that setup failure is not the behavioral red. The final fixture uses supported upload metadata.

Review verifies exact metadata and bytes, removal of newer objects, legacy checkpoint records, all three runtime paths, exact public account metadata immediately after restore, and SDK state after SIGKILL/restart with credential sign-in. Final checks pass 30 affected browser cases in 51.6 seconds, five minimum-Node cases in 12.9 seconds, and 117 full-state/checkpoint/branch/Storage regressions in eleven isolated files. Strict Pyric/CLI/fixture types pass; three-file source form and client/live browser boundaries have zero findings (54,943/98,304 and 387,843/524,288 bytes). Reports use `/tmp/pyric-hardening-checkpoint-storage-`; current hashes and original fixtures are archived in `ignored/hardening/checkpoint/`.

The full-state contract covers the default Storage bucket. Arbitrary-bucket checkpoint coverage, coherent concurrent capture, all-service atomic apply and branch-promotion metadata fidelity are not established by this slice. Corrupt-state refusal is still the next scoped requirement; task 3 and the milestone remain incomplete. The manual demo is unchanged.

Storage round-trip commit: `0c834446`, local pending push authorization.

## Task 3: Corruption refusal slice

The first public SDK regression proved a failed restore had already erased the current document: malformed `auth.users` reached the service only after the sandbox reset. Seven sequential red/green cycles cover account arrays, other service containers, account/object records, corrupt bytes/Storage rules/RTDB envelopes, empty paths and incorrect byte lengths, invalid checkpoint counts, and a slash-only path that normalizes to the bucket root. Original fixtures and each red/green pair are retained in `ignored/hardening/checkpoint/corruption-*.{log,pw.ts.txt}`; their commands are in the reports.

The restore owner now validates the serialized state before reset. The existing host account and object metadata schemas moved unchanged into `sandbox/internal/state-schemas.ts`, used by both host state and checkpoints. The 78-line checkpoint validator composes those schemas with the existing Storage path normalizer, byte decoder and rule compiler. Envelope validation requires finite timestamps and nonnegative integer counts. This adds no dependency, persistence owner, rollback engine or new runtime branch.

A diagnostic initially treated invalid Firestore syntax as corrupt state. Source inspection showed this is deliberately allowed during rule editing: Firestore installs the source and reports lint errors. That probe remains archived as `content-contract-probe.*`; it is not counted as a valid corruption red. A separate compatibility test saves and restores an intentionally invalid Firestore source. Storage differs: its normal setter compiles before installation, so an unparseable saved Storage source must refuse before state replacement.

Final verification is terminal, with no retries or skipped cases:

| Check | Result | Report |
| --- | --- | --- |
| Corruption, all-service round trip, Auth, Storage durability, legacy state and three runtimes | 45 passed in 1.2 minutes | `/tmp/pyric-hardening-checkpoint-corruption-final-browser.log` |
| Scoped corruption and checkpoint cases under Node 22.15.0 | 20 passed in 32.5 seconds | `/tmp/pyric-hardening-checkpoint-corruption-minimum-node.log` |
| State codec, checkpoints, branches and Storage persistence, 13 isolated files | 131 passed, zero skips | `/tmp/pyric-hardening-checkpoint-corruption-final-regressions.log` |
| Strict Pyric, CLI and fixture types | Passed | `/tmp/pyric-hardening-checkpoint-corruption-{pyric-build,cli-build,fixture-types}.log` |
| Changed code form | Eight TypeScript files, zero findings | `/tmp/pyric-hardening-checkpoint-corruption-form.json` |
| Client/live browser boundaries | 54,943/98,304 and 387,843/524,288 bytes; zero findings | `/tmp/pyric-hardening-checkpoint-corruption-browser-{client,live}.json` |

Every corruption case checks the healthy SDK document, the unchanged public account list, and a successful subsequent write. One also kills/restarts the host and checks the durable document. The SharedWorker path verifies empty bytes and all base64 padding remainders. Malformed-file fault injection is hosted; valid restoration is checked across all three runtimes. No production Firebase or manual demo data was used.

These checks establish structural corruption refusal and the specified object consistency checks, not detection of arbitrary edits that remain valid data. Atomic rollback after a later resource/I/O failure, concurrent capture, general input resource limits and arbitrary buckets remain separate gaps. Task 3 stays open until ordinary Firestore special values are checked; the existing full-service fixture currently uses plain document fields. Applicable packaging remains at the milestone join.

## Task 3: Firestore value fidelity — verified locally

The original SDK checkpoint tests lost Timestamp methods after restore. Full-state capture now uses the existing declared Firestore value encoding before JSON removes wrapper identity, and restoration decodes that declaration. Review reproduced a literal timestamp-shaped map being converted into a Timestamp; escaping ordinary maps with the existing codec fixes that without a second scalar codec. Full-state branch storage retains the declaration, and promotion decodes documents before writing them.

Two further public-boundary failures were reproduced and repaired. Malformed encoded bytes erased healthy state before restore failed; checkpoint validation now decodes Firestore inputs before reset. A plain SDK write followed by host restart converted a literal map into a Timestamp even without checkpoint operations; newly written persistence buckets now declare their value encoding individually. Legacy buckets retain their prior decoding contract, and bucket-level declarations allow old and newly flushed records to coexist.

The published in-process MCP branch interface exposed `literal.fields.note` in its diff instead of the actual field `literal.note`. Diff now decodes documents before walking fields. The first hosted MCP probes used operations that its tool surface does not expose and count only as setup failures. Review also corrected an invalid promotion expectation: promotion applies branch changes relative to its base, so an unchanged branch must preserve a later live edit. The field-path assertion is unchanged and passes; this fixture does not prove promotion of an actual changed typed value.

Current terminal checks: 42 affected browser cases passed in 1.1 minutes; the cases then labeled in-page actually selected SharedWorker, as corrected below; eight focused cases passed on Node 22.15.0 in 16.1 seconds; 107 regressions passed in eight isolated files; strict Pyric/CLI/fixture types, changed-code form and client/live browser boundaries passed. Reports use `/tmp/pyric-hardening-checkpoint-values-`, with source-bound copies in `ignored/hardening/checkpoint-values/`. The byte-corruption, literal-map restart and branch-path red fixtures are retained there. The minimized restart test is now named `checkpoint-literal-restart.pw.ts`; its behavioral assertions are unchanged.

Final compatibility review passes legacy full-state maps without an encoding declaration, mixed old/new persistence buckets after actual host restart, and changed-value promotion through persisted MCP branches. The promotion check uses a real unauthenticated read with installed Rules requiring a Timestamp at the specified instant and a literal map with its original fields. An initial Rules simulation had no seeded resource and was unsuitable for this assertion; the corrected read evaluates the actual promoted document. These are review checks, not additional claimed red/green fixes.

The three added review cases pass individually; the two branch cases plus legacy/mixed-bucket cases pass together under Node 22.15.0 (four cases, 7.0 seconds). Final fixture types and fifteen-file code form pass. Production and emitted input hashes match the preceding 42-case combined browser run, eight-case minimum-Node run, 107 regressions, production typechecks and browser boundaries, so that evidence is reused. `ignored/hardening/checkpoint-values/verified-inputs.json` binds the final inputs and reports. All processes are terminal, without retries or skipped cases.

Task 3's scoped round trips and corruption refusal are verified. Current readers accept legacy records; downgrade compatibility with older readers is not established. General checkpoint atomicity, coherent concurrent capture and arbitrary buckets remain separate. Applicable packaging remains at the milestone join. Later queue items are untouched; the manual demo has not been changed.

## Task 4: Restoration diagnostics — verified locally

The S1/S3 restart regression read both saved documents through the SDK while readiness reported zero documents and zero users. Startup summary code only inspected the older top-level document and Auth sections; the current controller stores documents in buckets and accounts in its service metadata. The original fixture passed unchanged after the shared state-count helper learned both representations (`/tmp/pyric-hardening-restoration-diagnostics-{red,green}.log`).

`state-summary.ts` counts document paths without rehydrating values or serialising the keyspace. Controller account counts take precedence over the older separate Auth section, including an empty account list. Startup diagnostics and seed labels use that summary, and the existing state-store recovery-backup check reuses its document count. This adds no persisted fields, runtime owner or transport change.

Separate review verifies both the human-readable startup line and JSON, empty/fresh startup, and Auth-only restoration checked against the public remote account list. The affected matrix passes 18 browser cases in 35.6 seconds (restart, reset, seed and runtime parity); 34 state-store/persistence/session regressions pass in four isolated files. Both diagnostics cases pass on Node 22.15.0 in 6.7 seconds. CLI/fixture strict types and five-file code form pass. Reports and source/emitted hashes are archived in `ignored/hardening/restoration-diagnostics/verified-inputs.json`; every process is terminal. Browser runtime modules were unchanged; applicable packaging remains at the final milestone join.

Checkpoint value commit: `d5f1c10775d063bbdd80d4f3d5fe34b94e17b57c`, local pending push authorization. Task 5 is next. The manual demo remains untouched.

## Task 5: Interrupted recovery — verified locally

Three S1/S2/S3 characterization tests pass without a production change. A real host restart followed by a second socket cut during Auth restoration retains the UID, permits an owner-authorized write and delivers each subsequent document value once. Deleting the app while its restoration request is held closes the socket; advancing the browser retry clock produces neither another restoration of that UID nor a resume of its grant, and SDK app enumeration stays empty. A browser platform interceptor retains a real document snapshot and close event, then delivers them after recovery: neither changes the current document nor prevents a subsequent SDK write/listener round trip.

Review corrected fixture assumptions rather than weakening product expectations. Same-host retained admission does not run Auth restoration, so the interruption fixture requires a real restart. Runtime panel and app sockets both use worker ports; selecting the first socket or counting every attachment is not an app-specific oracle. The final delayed-event fixture selects an actual document snapshot and intercepts before SDK delivery. The deletion fixture identifies the app by its public UID and admission grant. Initial setup failures, the intermittent all-port count failure and diagnostic runs are retained separately; none is claimed as a product red/green pair.

Final evidence: 13 affected recovery browser cases pass in 36.6 seconds; the three new cases pass on Node 22.15.0 in 12.2 seconds. Strict fixture types, three-file source form and whitespace checks pass. The long real-clock expiry case is reused: its fixture, protocol, session owner, socket source and emitted owner hashes match the task 2 evidence. No production source changed, so this slice requires no new runtime parity, bundle or package build claim. Applicable packaging remains at the final join. Reports, reviewed input hashes and setup evidence are under `ignored/hardening/interrupted-recovery/`. Task 6 is next; the manual demo is untouched.

Recovery verification commit: `08a3fdc9`, local pending push authorization.

## Task 6: Active-app reset slice — verified locally

The public two-context reset fixture failed after a successful reset acknowledgment: an active SDK listener still displayed `Before reset` instead of the missing document. The unchanged fixture passed after the worker reset handler reused `restoreSubscriptions` following restoration of project Rules. This is the same retained-intent owner used by checkpoint restore; no second listener registry or runtime selection policy was added.

Review extends the fixture to default SharedWorker tabs, sharing a browser context only for that runtime. Both apps observe cleared data and subsequent SDK writes. A secondary listener unsubscribed before reset stays stopped while another active listener follows both updates. An ordered SDK read confirms the unsubscribe was processed before the control request. Existing checks cover reset with an in-flight Storage save, unhealthy-persistence refusal, checkpoint listeners, RTDB disconnect invalidation and SharedWorker checkpoint controls. The old in-page checkpoint label is withdrawn by the runtime-selection correction below.

Current checks: 11 affected browser cases pass in 22.4 seconds; 17 reset/RTDB cases pass on Node 22.15.0 in 32.6 seconds; seven worker reset/subscription regressions pass in two isolated files. Strict CLI/fixture types, two-file source form, whitespace checks and the worker-client browser boundary pass (54,943 / 98,304 bytes, zero findings). The original red/green pair and reviewed input hashes are archived under `ignored/hardening/active-app-replacement/`. No full task 6 completion is claimed: active Firestore import, remaining stale work and the final milestone packaging/integration checks are open.

Reset repair commit: `bb3bd157`, local pending push authorization.

## Task 6: Active-app import slice — verified locally

The unchanged hosted SDK import fixture failed after `{ ok: true }`: the document listener still showed `Before import` instead of `Saved value`. The import handler now invokes the existing `restoreSubscriptions` after loading the snapshot. The original fixture passes; no new registry, generation owner or transport branch was introduced.

Review covers two independent hosted contexts and two default SharedWorker tabs. Active document and collection-query listeners show the imported value, a document created after export disappears, subsequent SDK writes reach both apps, and the listener unsubscribed before import stays stopped. The original red/green fixture and results are preserved separately from this expanded review.

Current evidence: 28 affected replacement browser cases pass in 52.4 seconds, including reset, checkpoint, RTDB disconnect/refusal and SharedWorker checks. The then-labeled in-page checkpoint case actually used SharedWorker; see the correction below. Both reviewed import scenarios also pass on minimum Node; exact timing is retained in the archived report. Fifteen existing connection/checkpoint/subscription regressions pass across four isolated files. Strict CLI build, fixture types, two-file source form and whitespace checks pass. The client source and its prior boundary result are unchanged by this host-only call. Inputs and logs are archived under `ignored/hardening/active-app-replacement/import-verified-inputs.json`. Pending transaction work and asynchronous persistence across replacement remain to be probed before task 6 can close.


## Runtime-selection correction and in-page token refresh

The parity fixture's `--inpage` flag did not select in-page mode. An explicit runtime assertion failed with expected `in-page`, actual `shared-worker`. This withdraws the earlier in-page claims derived from `checkpoint-auth.pw.ts`, `checkpoint-storage.pw.ts`, `checkpoint-values-runtime.pw.ts`, and the first transaction-replacement probe. Their historical raw reports remain preserved, but are not in-page evidence.

A shared fixture helper now selects in-page mode through the established browser setting before imports and supplies the expected public runtime diagnostic. Corrected runtime assertions cover the affected parity and active-app replacement fixtures. The genuine in-page peer does not expose the worker relay: checkpoint, restore, export/import and reset requests through that relay return `unimplemented`. The new capability test verifies this refusal while Firestore typed values/literal maps and Storage bytes/metadata remain intact through normal SDK reads. Worker-control round trips remain verified for hosted and SharedWorker only. No in-page checkpoint/restore parity is claimed.

In-page Auth administration uses its existing public MCP tools. This exposed a real failure before the first protected write: `getIdTokenResult(true)` returned the new role, but Rules still saw old session claims. The shared Auth backend now updates the matching current session's claims on forced refresh, preserving its tenant and identity. Both token APIs use the same cache/refresh path. The existing transition guard preserves notification semantics; refreshing a held user cannot alter a different current user or undo sign-out. The unchanged in-page Auth/Rules fixture now passes.

Reviewed evidence: 31 affected browser cases pass in 52.3 seconds, including four hosted/SharedWorker transaction-replacement characterizations. Seven genuine in-page cases pass on Node 22.15.0 in 10.9 seconds. Forty-two existing Auth tests pass across five isolated files; strict Pyric/CLI/fixture types and source form pass. Worker-client and live-Firestore browser boundaries remain 54,943 / 98,304 and 387,843 / 524,288 bytes with zero findings. Explicit runtime assertions for active-app reset/import have a separate minimum-Node result. Reports and source/artifact hashes are archived under `ignored/hardening/inpage-token-refresh/`; misselection and unsupported-relay evidence remain under `ignored/hardening/runtime-selection/`.

A separate tenant-token issue was observed: the in-page `User.tenantId` is `tenant-blue`, but its `IdTokenResult.claims.firebase` lacks `tenant`. The diagnostic is retained in `ignored/hardening/runtime-selection/inpage-auth-identity-probe.log`. This remains open for item 11; the current repair proves role propagation and retained Rules tenant scope, not complete tenant-token parity. Asynchronous persistence across import remains open for item 6. Final combined/package verification and later queue items remain required.

## Task 6: Complete transfers and stale work — verified locally

The Storage overlap probe first exposed a transfer omission: export/import used persistence records whose Storage snapshot/restore hooks deliberately carry no object data. A minimized ordinary SDK round trip reported a successful import but returned the later bytes. New portable exports now reuse the existing complete checkpoint envelope, and imports reuse its validation and restore path. Legacy record imports remain readable with their historical service coverage. No second service codec or persistence owner was introduced.

After that repair, the unchanged overlap test reached a separate durability failure: the object disappeared in memory but returned after hard restart. Import now awaits the existing ordered persistence flush before acknowledging success, matching named checkpoint restore. The unchanged original round-trip and overlap fixtures pass. Both red stages, intermediate sources and final reports are retained under `ignored/hardening/import-storage/`.

Separate review verifies hosted and default SharedWorker metadata/bytes, removal of later objects, valid legacy import, corrupt complete-import refusal before data loss, and subsequent SDK writes/listener delivery. Four transaction characterizations pause a normal SDK transaction before reset/import; the callback retries against replacement data and commits without resurrecting obsolete fields. These already pass without a transaction implementation change.

Final checks: 42 affected browser cases passed in 1.3 minutes; ten new import/transaction cases passed under Node 22.15.0 in 20.2 seconds. Twenty regressions passed in five isolated files. Strict CLI and fixture types, eight-file changed-code form and whitespace checks passed. Worker-client and live-Firestore boundaries remain 54,943/98,304 and 387,843/524,288 bytes with zero findings. Final fixture formatting was verified by the minimum-Node run and refreshed type/form checks. No full browser-suite rerun was needed.

This closes the scoped active-app replacement and stale-work checks for item 6. New complete exports require a reader supporting checkpoint envelopes; legacy record exports cannot recover Storage bytes they never contained. Downgrade compatibility, coherent concurrent capture and rollback after a later service/I/O failure are not established. In-page worker controls remain explicitly unsupported; ordinary in-page SDK/MCP support is preserved. Item 7 and later items, final packaging/integration verification and the morning handoff remain open. The manual demo is untouched.

## Task 7: Persistence repair and restart — verified locally

Two new characterization scenarios pass without a production change. Both establish durable data, make the state directory unwritable, observe `committed-but-not-durable`, and prove a later mutation is refused without changing the in-memory result. Repairing permissions while the host is alive does not clear its unhealthy admission state. After hard restart, Firestore and Storage read their earlier durable values and accept new writes. The final Firestore scenario retains the original app across restart and verifies that its uncertain increment is not replayed.

The affected persistence-failure group passed 36 browser cases in one minute across SDK, Auth, RTDB, Storage, MCP and service CLI entry points. That run included the earlier fresh-reader version of the document recovery test; the stronger retained-app review and Storage recovery then passed under Node 22.15.0 in 7.8 seconds (two cases). The other 34 checks and all production inputs are unchanged. Final strict fixture types and one-file source form pass. No implementation fix, new red claim, runtime selection change or resource owner was needed. Existing production build and browser-boundary evidence from `52524c56` remains applicable.

Reports and source/artifact hashes are retained under `ignored/hardening/persistence-repair/`. The failure model is an unwritable state directory followed by permission repair and a hard process restart; this does not prove crash atomicity across arbitrary partial multi-service writes or recovery from every disk fault. Those broader architectural gaps remain explicit. Item 8 is next; final combined/packaging verification and the morning handoff remain open.

Complete-import and stale-work commit: `52524c5671c0661504b9783bbb69e3a9b807f408`, local pending push authorization.

## Task 8: Worker envelope rejection — verified slice

A null worker message escaped the consumer handler as an uncaught `TypeError`. The CLI kept the process alive, but the SDK write stayed pending. The retained real-host diagnostic identifies the null `.t` read. A socket-boundary check makes the unchanged regression pass by closing the affected connection before dispatch. Review found that an object with an unknown message type was silently ignored and also left the request pending; the boundary now recognises the declared worker message types before dispatch. The type inventory is checked against `InboundMessage['t']`. This recognises envelope kinds, not complete service payloads.

Review also caught an error in the initial repair: close code 1008 is terminal in this client's admission policy, so an affected app could not recover. The final implementation uses a protocol-error close for malformed envelopes while preserving 1008 admission refusals. The unchanged recovery review then passes: a separate app writes successfully, and the affected app resumes its listener and performs a subsequent valid write. Earlier failing review output and intermediate source are retained rather than counted as successful evidence.

Final verification: 22 affected browser cases passed under Node 22.15.0 in 48.1 seconds, including eight malformed-envelope variants, invalid-grant refusal, hosted restart recovery, SharedWorker import/write validation and explicit in-page control refusal. Thirty-one existing bridge peer/session regressions pass in three isolated files. Strict CLI/fixture types, two-file source form and staged whitespace checks pass. The new runtime check is confined to the Node socket boundary; no browser runtime owner or bundle dependency was added. Reports and source/artifact hashes are under `ignored/hardening/worker-envelope/`.

Item 8 remains in progress. Next: correlation-field validation, outer JSON/envelope handling, remaining method payloads, protocol-version refusal and declared size/depth boundaries. Passing these eight malformed worker-body cases does not establish general request validation or resource bounds. The later queue, final packaging/integration verification and manual handoff remain open.

Persistence-repair characterization commit: `39f2facdc61e463f716d79363e661f7be107b499`, local pending push authorization.

## Task 8: Browser worker correlation IDs — verified slice

The null-request-ID regression reproduced both a pending SDK promise and an already-applied write visible in the document listener. The socket validator now requires string IDs for correlated worker requests before dispatch, so an invalid ID cannot cause a mutation with an unmatchable reply. The unchanged original test passes; review covers missing, numeric, Boolean, array and object IDs, unchanged data before the healthy client's write, and subsequent recovery of the affected app.

A separate subscription regression reproduced an original listener that never received its first snapshot after its subscription ID was replaced with null. Subscription and unsubscribe envelopes now require string subscription IDs before dispatch. The unchanged regression passes, and review adds a missing subscription ID. The fixture's document-target guard was refined to distinguish Firestore targets from RTDB/AI target objects after strict typechecking caught the missing narrowing; no cast or suppression was added. Existing string-ID semantics are retained; this slice introduces no ID length or value policy.

Final verification: 31 affected browser cases passed under Node 22.15.0 in 1.1 minutes. The group includes the eight new request/subscription-ID checks, previous malformed-envelope checks, admission refusal, deletion during recovery, hosted restart, SharedWorker import/write validation and explicit in-page control refusal. Thirty-one existing peer/session regressions pass across three isolated files. Strict CLI/fixture types, three-file changed-code form and staged whitespace checks pass. No browser implementation or resource owner changed. Reports and source/artifact hashes are retained under `ignored/hardening/request-id/`.

This closes the scoped browser worker correlation checks, not all of item 8. Outer JSON/envelopes, legacy relay correlation, remaining method payloads, protocol-version refusal and size/depth limits remain to be verified. Later items and final packaging/integration/handoff are still open.

Envelope rejection commit: `9787eac1d150676d9d9a05143cb5247893b37d4d`, local pending push authorization.

## Task 8: Outer frame rejection and shared fault fixture — verified slice

Two separate public-wire regressions reproduced pending SDK writes when the outer frame contained invalid JSON or an unknown message type. The socket parser previously returned silently on both paths. It now closes the offending connection with the existing protocol-error behavior before dispatch. Each unchanged original regression passed after its correction. Review covers null, array, numeric and string roots, missing frame type and a non-string type, along with no mutation, an independently usable app and subsequent recovery of the affected app.

The three malformed-write scenario files had identical setup and assertion bodies. Review extracted those bodies into `malformed-write-fixture.ts`; the tests now supply just their wire transformation. Before extraction, the normalised bodies were compared and matched exactly, including refusal, isolation, listener recovery, subsequent writes and the absence of uncaught host exceptions. The original files are retained in the evidence archive. Subscription-specific tests retain their distinct fixture instead of adding modes to this helper.

Final verification: 39 affected browser cases passed under Node 22.15.0 in 1.4 minutes, including all extracted callers, eight outer-frame cases, correlation/subscription validation, admission refusal, deletion/restart recovery, SharedWorker behavior and explicit in-page control refusal. Thirty-one peer/session regressions passed in three isolated files. Strict CLI/fixture types, five-file source form and staged whitespace checks pass. The production change adds only explicit rejection on the existing parser's two failure paths; no runtime owner or browser dependency changed. Reports and source/artifact hashes are retained under `ignored/hardening/outer-frame/`.

Item 8 remains open for validation of known outer-frame payloads and legacy relay correlation, remaining method payloads, protocol-version refusal and size/depth bounds. This does not claim full protocol validation or cross-version compatibility. Later queue items, final packaging/integration verification and the morning handoff remain open.

Correlation-ID commit: `8308ea40abc53694a4a06824db189cfbc7f8a2b7`, local pending push authorization.


## Task 8: Protocol admission and buffered-frame refusal — verified slice

At the approved S1/S2/S3 seams, an unsupported consumer handshake (`protocol: 999`) received a real worker-session grant. A separate unsupported peer handshake replaced the default SharedWorker peer and received consumer presence data. Both now refuse with close code 1008 and an expected-version diagnostic before registration. Protocol 1 remains the existing wire contract; this does not establish package-version compatibility.

A third public-boundary regression sent an invalid handshake followed immediately by a valid attach and a write. Despite receiving refusal, the SDK read the forbidden document: already-buffered frames were still dispatched while the socket was closing. The socket handler now checks its existing ready state before parsing/dispatching. No parallel connection state, dependency or resource owner was added. The production change is ten lines in `bridge/server/peer.ts`.

The initial consumer red passes unchanged. The peer fixture's first post-fix run reached the follow-up write but correctly failed Rules because its remote control was unauthenticated. That fixture now waits for public peer readiness and uses explicit admin control. The corrected fixture was rerun against the missing peer-version check, failed at the same refusal assertion, and then passed unchanged with the fix restored. The buffered-write regression also passes unchanged after the socket-state check. Original fixtures, intermediate production sources and all reports are archived under `ignored/hardening/protocol-admission/`; the initial fixture mistake is retained separately from valid red/green evidence.

Separate review extracted repeated socket refusal assertions and verifies that no reply/presence/grant precedes closure. Eight invalid version values (future, zero, absent, null, string, Boolean, array and object) are refused for worker-port consumers, legacy consumers and SharedWorker peers: 24 handshake probes within two browser scenarios. Buffered-frame review also covers invalid session grants and invalid JSON. Normal SDK writes/listeners and public remote writes still work; runtime diagnostics explicitly identify hosted and default SharedWorker execution.

Final terminal checks: 37 affected browser scenarios pass in 1.4 minutes on Node 22.15.0; 31 peer/session/relay regressions pass in three isolated files; strict CLI and hosted-fixture types pass; changed-code form checks two TypeScript files with zero findings. Commands and hashes are archived in `verified-inputs.json`. No retries or skips. This edit changes only the Node socket handler, so existing browser dependency/bundle evidence remains applicable; the in-page capability and SharedWorker active-import scenarios pass in the affected group. No SDK return shape, cross-runtime codec or conformance registry row changes. Final packaging remains at the milestone join.

Known-frame and legacy relay payload validation, reply-version validation, size/depth limits and later queue items remain open. This slice proves refusal of invalid wire versions; item 18 still requires actual packaged client/host combinations. The manual demo is untouched. Local commits remain pending the existing push authorization question.


## Task 8: Legacy relay correlation and payload envelopes — verified slice

Five S1/S2/S3 red/green cycles cover legacy `worker-op`, `worker-sub` and `worker-unsub` envelopes. A null operation ID acknowledged a write with an unusable correlation ID; a null subscription ID registered a listener and delivered an uncorrelated snapshot. Null operation/subscription bodies reached service dispatch and produced internal `startsWith`/`__ref` errors. A malformed unsubscribe ID was silently ignored. Each original assertion passes after refusal was added at the socket boundary; all five pass together in 9.1 seconds before review.

Separate review moved worker and relay envelope validation into `bridge/server/request-envelope.ts` (75 lines), leaving socket admission, closure and lifetime ownership in `peer.ts`. One object predicate replaces repeated container checks. Existing worker tag/ID rules and close reasons remain unchanged; the new relay checks require string correlation IDs, an object operation with a string method, and an object subscription with a string or object target container. This introduces no dependency, resource owner, service codec or runtime-selection branch. It does not validate every detailed target or service argument.

The review fixture retains refusal/no-mutation/healthy-app assertions and checks a newly attached public remote consumer can write. It also rejects any intermediate reply before closure. Forty malformed frames run within five host lifecycles: six ID variants for each relay frame type, and eleven malformed body variants for operations and subscriptions. This avoids starting a host for every scalar case. The original five fixtures, intermediate production sources, unchanged green reports and final review evidence are archived under `ignored/hardening/relay-frames/`.

Current terminal checks: 41 affected browser scenarios pass in 1.6 minutes on Node 22.15.0; 31 existing bridge/session regressions pass in three isolated files; strict CLI/fixture types and three-file changed-code form pass. Eight supplemental public valid-relay/checkpoint scenarios pass on Node 22.15.0 in 14.1 seconds, covering remote Auth isolation, normal document subscriptions/unsubscribe, explicit unsupported event subscriptions and default SharedWorker checkpoint restoration. All processes exited zero without retries or skips. Commands, source/test hashes, toolchain identity and emitted artifact digests are recorded in `verified-inputs.json`. Full browser-suite repetition is unnecessary for this server-only edit; current affected scenarios exercise hosted recovery, SharedWorker active imports and genuine in-page capability refusal. Applicable packaging remains at the milestone join.

The remainder of item 8 is explicit: other known-frame metadata/lens fields, detailed targets/service arguments, reply versions and input size/depth bounds. This slice neither closes item 8 nor establishes packaged version compatibility. The manual demo is untouched and no push retry was attempted.


## Task 8: Consumer-side host protocol refusal — verified slice

Two S1/S2/S3 regressions reproduced consumers accepting an incompatible host acknowledgment. The browser released a queued SDK write and reported success after a protocol-999 reply. The published `connectRemoteSandbox` returned a usable connection through a WebSocket proxy that changed only the real host's acknowledgment version. Both now reject with `unavailable` and an expected-version diagnostic before completing attachment. Existing terminal failure handling clears queued/pending work and closes the connection; no second connection state or retry policy was added.

The browser's original regression passed unchanged. Strict compilation of the remote fix caught that the parsed value needed a constant binding for TypeScript's aliased-guard narrowing. The pre-correction test run is retained but is not final evidence. After a clean build, both unchanged regressions passed together in 5.1 seconds. Red fixtures, pre-fix sources, the narrowing failure and corrected reports are archived under `ignored/hardening/host-protocol/`.

Code-form review named the edited remote factory's existing URL/discovery decisions and replaced its unchecked double assertion for the underlying socket with a Node Socket check. A real process using the public remote API stays alive while subscribed, receives a later browser write, unsubscribes and exits naturally without an explicit connection close. This is a passing lifecycle characterization, not an additional claimed bug fix. The existing core ref/unref, lazy connection and version-skew guidance regressions also pass.

Review covers eight invalid acknowledgment versions in both browser and remote consumers (future version, zero, missing, null, string, Boolean, array and object). Browser cases assert zero transmitted SDK writes and read the absent document from an independent healthy app; the remote cases retain real host traffic through the proxy and keep another browser usable. Supported protocol-1 remote subscriptions, Auth isolation, restart recovery, malformed JSON, deletion during attach/recovery, SharedWorker active imports and genuine in-page capability refusal pass in the affected group.

Final terminal checks: 31 affected browser scenarios pass on Node 22.15.0 in 1.0 minute; 47 regressions pass in five isolated files; strict CLI/fixture types and six-file code form pass. The initial regression invocation lacked loopback access for an existing lazy-client test; its log is retained separately and the complete authorized invocation passes. Browser socket and client boundaries pass at 7,366/16,384 and 54,943/98,304 bytes respectively, with no forbidden imports. The Node-only remote entry adds only the built-in `node:net` check; no browser entry imports it. Commands, source/test hashes and emitted artifact digests are recorded in `verified-inputs.json`.

This enforces the existing wire protocol-1 contract. Actual packaged-version compatibility (item 18), peer `hello-ack` validation, other malformed reply fields, remaining request fields/arguments, size/depth bounds and later queue items remain open. The manual demo is untouched and no push retry was attempted.

## Task 8: Peer acknowledgment protocol — verified locally

An actual SharedWorker page accepted an incompatible host `hello-ack` instead of disconnecting its bridge. The final corrected S1/S2/S3 fixture failed with no close observed and passed unchanged after the existing connection owner checked protocol version 1 and entered its terminal disconnected state. Local SDK reads/writes remain available, a healthy tab takes the peer slot, and a public remote write reaches both tabs after the incompatible tab's retry clock advances. No second reconnect owner or runtime fallback was introduced.

The initial test/implementation attempts are not the claimed red/green pair: the Playwright close callback takes positional arguments, browser-initiated close code 1008 is forbidden, and the proxy's second close notification must not overwrite its first. Those failures remain archived separately. The final red and green use the same corrected fixture and browser-permitted code 1000 with the explicit incompatibility reason. Review covers eight incompatible values, preserves a valid version-1 relay fixture, removes its unchecked non-null assertions, and applies named decisions throughout the modified connection function. URL discovery and standby endpoints now share a 34-line module, keeping the connection source below 600 lines. Strict types caught mutable-binding narrowing errors during review; constant snapshots fix them.

Final checks pass 24 affected browser scenarios on Node 22.15.0 (43.7 seconds), 37 regressions in five isolated files, strict CLI/public-browser/relay-fixture types, four-file source form and whitespace checks. The peer entry bundles for the browser with no external imports (648,818 minified bytes); this is an import-boundary check, not a new size budget or a worker-client engine-exclusion claim. Reports, corrected TDD inputs and source/artifact hashes are archived in `ignored/hardening/peer-protocol/verified-inputs.json`. All processes are terminal. Other peer frame fields, buffered peer commands, detailed service payloads, bounds, compatibility packaging and later queue items remain open. The manual demo is untouched; no push retry was attempted.

## Task 8: Hosted acknowledgment capabilities — verified locally

Two S1/S2/S3 cycles cover malformed capabilities through a queued normal SDK write. A numeric value left the result at `Starting` instead of rejecting; an array guard fixes that failure. A subsequent mixed array containing `worker-port` and a number still allowed the write to complete; requiring string entries fixes that second failure. Both original fixtures pass unchanged after their respective minimal changes. The existing connection failure path rejects queued requests and closes the socket; no additional decoder, session owner or fallback was introduced.

Review reuses the host acknowledgment fixture for eight invalid versions and eleven malformed or unsupported capability values. It checks the SDK error, absence of page errors, zero transmitted writes and a separate healthy browser's write/listener round trip. This validates the hosted browser's required worker-port admission; it does not claim all optional fields in every protocol message are validated. Unknown string capability names remain compatible when worker-port is present.

Final evidence: 40 combined browser scenarios pass on Node 22.15.0 (1.3 minutes), covering both current handshake fixes, host restart/tenant restoration, interrupted recovery, stale events, deletion during attach/recovery, public remote consumers, default SharedWorker and genuine in-page access. Eleven connection/RTDB/session regressions pass in three isolated files. Strict CLI/fixture types, two-file source form and whitespace checks pass. Browser socket and worker-client entries pass their existing budgets at 7,425/16,384 and 54,943/98,304 bytes, with zero forbidden-import findings. Reports, two TDD pairs and reviewed input/artifact hashes are in `ignored/hardening/host-capabilities/verified-inputs.json`; all processes are terminal. Other frame fields, buffered peer commands, detailed service arguments, size/depth limits and later queue items remain open. The manual demo is untouched and no push retry was attempted.

## Task 8: Buffered peer commands — characterised locally

A real browser wire probe sends a worker write immediately after the peer acknowledgment. With version 1, an SDK read observes the injected document; with an incompatible version, the peer disconnects and the SDK read confirms the document is absent. Normal SharedWorker SDK writes remain usable in both cases. Chromium already discards buffered delivery once the socket closes, so this slice changes no production code and claims no new red/green fix. It verifies immediate buffered wire delivery, not every possible delayed callback or browser engine.

The two controls and eight existing peer-version cases pass together on Node 22.15.0 (10 cases, 17.9 seconds). Strict fixture types, one-file source form and whitespace checks pass. Matching production/artifact identities retain the preceding combined browser, regression, type and boundary evidence; no package rebuild is needed for this test-only slice. Reports and input hashes are in `ignored/hardening/peer-refused-work/verified-inputs.json`. Other frame fields, detailed payloads, bounds and later queue items remain open. The manual demo is untouched; no push retry was attempted.

## Task 8: Remote identity-lens envelope — verified locally

Five S1/S2/S3 red/green cycles cover lens containers, unknown modes, missing impersonated identity, malformed request IDs and malformed target-session IDs. Each previously failed the required protocol refusal; each unchanged fixture now observes close code 1002, no successful lens/event reply, no buffered SDK-visible write and a healthy app that can still write. The validation belongs to the existing request-envelope boundary, before consumer or peer dispatch. Impersonation UID, tenant and token checks call `validateAuthState` from `pyric/sandbox`, preserving the shared identity contract rather than duplicating it in the bridge.

Review consolidates lens shape checks in one small predicate. Two actual runtime fixtures each send 31 malformed frames through fresh sockets while the same SDK app stays active. Positive controls retain all four lens modes, a tenant/claim-bearing identity and omission of the optional acknowledgment ID; wire lens events preserve those fields. This is envelope validation and supported-control propagation, not completion of the separate cross-client identity-isolation requirement.

Final evidence: 19 affected browser cases pass on Node 22.15.0 (1.2 minutes), including 62 malformed lens frames across hosted and default SharedWorker, valid controls, legacy relay refusal, handshake admission, buffered peer commands, public remote consumers and genuine in-page SDK access. Forty-two consumer/relay/session/Auth-lens regressions pass in six isolated files. Strict CLI/fixture types, two-file source form and whitespace checks pass. The changed validator is imported only by the Node bridge peer boundary; browser leaves and the shared Auth implementation are unchanged, so their applicable prior build/boundary evidence is retained by matching inputs. Reports, all five original fixture pairs and reviewed source/artifact hashes are in `ignored/hardening/lens-frames/verified-inputs.json`. All processes are terminal. Other frame/reply fields, detailed service payloads, size/depth limits and later queue items remain open. No manual-demo changes or push retry were made.


## Verification repair: Vite host lifecycle — verified locally

The inbound-frame regression join exposed a pre-existing fixture failure: all three Vite HTTP/MCP/peer assertions passed, but teardown timed out when Vite ran inside Bun. The unchanged HEAD mount exhibited the same close timeout. Increasing the hook deadline only let the fixture catch and print its own timeout while exiting zero; that result is not passing evidence. A real Node 22.15.0 Vite host with the same peer lifecycle closed cleanly.

The fixture now starts Vite through the public package export in a Node child while Bun drives the existing HTTP and WebSocket interfaces. Shutdown awaits natural child exit after `server.close()`; a deadline kills the child and fails the test. Errors are no longer swallowed by teardown. Runtime schemas replace unchecked response casts, JSON-RPC errors fail explicitly, and edited conditions use named local decisions. No production lifecycle workaround or new test seam was added.

All three reviewed integration scenarios pass in 1.204 seconds on Node 22.15.0, with zero failures or skips and clean process exit. Strict fixture types and changed-code form pass. Baseline failures, the misleading caught-timeout report, final reports and source/artifact hashes are retained in `ignored/hardening/vite-node-host/verified-inputs.json`. This verifies the Node plugin path; it does not claim the Bun-hosted Vite teardown defect is fixed. Item 9's broader lifecycle work remains open.


## Task 8: Mounted inbound frame limit — verified locally

A public wire regression sent a message one byte above the specified 12 MiB encoded limit; the server accepted it instead of refusing it before dispatch. The existing mounted WebSocket server now sets `maxPayload` to the shared protocol limit. The unchanged original fixture passes with close code 1009 and no SDK-visible write. The limit applies to complete reassembled messages, including their JSON envelope and base64 data. This changes no service codec, runtime selection or resource owner.

Review checks below/equal/above boundaries with multibyte text, plus equal/above fragmented messages, through both hosted and default SharedWorker. SDK reads and listeners retain the last accepted value after refusal, and an ordinary write still succeeds. Separate browser and public remote Storage round trips preserve the full supported 8 MiB payload in both runtimes. The mounted server serves both CLI and Vite; the separate standalone bridge and outbound/depth bounds remain open.

Final evidence: 18 affected browser cases pass on Node 22.15.0 in 36.7s; 49 bridge, Vite, relay and Storage regressions pass in five isolated files with no skips. Strict CLI/fixture types, seven-file source form and whitespace checks pass. Browser socket/client boundaries remain within their existing budgets at 7,425/16,384 and 54,943/98,304 bytes, with no forbidden imports. The Vite fixture repair is a separate commit (`99847f16`); its original skipped/timeout attempts are not counted as passing. An older review fixture hash predated final condition cleanup, so the 18-case browser group was rerun after the final build. Current source/artifact hashes, exact commands and the original red/green pair are retained in `ignored/hardening/inbound-frame-limit/verified-inputs.json`.

Item 8 is still in progress. No broader frame-validation, standalone, lifecycle or release completion is claimed. The manual demo is untouched and no push retry was attempted.


## Task 8: Standalone inbound frame limit — verified locally

The actual `pyric bridge` CLI accepted an oversized peer handshake instead of closing that connection. Applying the shared 12 MiB constant at its existing WebSocket server passes the unchanged regression: refusal uses code 1009, a normal MCP write still reaches the original healthy peer, a public Firestore SDK read observes that write, and SIGTERM exits cleanly. No private inspection or mocked dispatcher supplies the result.

Review adds below/equal/above boundaries with multibyte text and fragmented equal/above messages. The touched server function now uses named local decisions and explicit configuration precedence. Session construction uses real transport/server handles instead of placeholder casts; HTTP values are narrowed without non-null assertions. Existing MCP session-cap, health, tool round-trip, reconnect, origin admission, audit and signal/shutdown checks pass. The existing idle-timeout regression checks continued health only; it is not evidence that item 9's full disposal requirement is complete.

Final verification: 14 affected runtime cases pass in 25.2 seconds on Node 22.15.0, including five standalone cases, mounted hosted/SharedWorker input limits and Storage capacity, public remote consumers and genuine in-page access. Twenty-five existing regressions pass in five isolated files; their CLI signal subprocesses use Node 22.18.0. Strict CLI/fixture types, three-file code form and whitespace checks pass. The only production edit is the Node standalone module; unchanged browser-leaf inputs and Pyric artifacts retain their applicable previous boundary evidence. Original red/green inputs, the review narrowing failure and final reports are recorded in `ignored/hardening/standalone-frame-limit/verified-inputs.json`.

The standalone module still has a separate peer-admission handler. Its protocol/schema validation needs its own scoped evidence; this frame-limit fix does not establish parity with the mounted handler. Outbound frames, depth/detailed payload validation, later queue items and final verification remain open. The manual demo is untouched; no push retry was attempted.


## Task 8: Standalone protocol admission — verified locally

The standalone CLI accepted an incompatible peer and failed to close its connection. The unchanged public regression now refuses that handshake and the valid handshake buffered immediately after it: close code 1008 arrives, and an ordinary MCP write still reaches the original healthy peer and reads back through the public Firestore SDK. The existing socket state prevents dispatch after closure begins.

Review moves the mounted handler's version policy into `requestProtocolError` beside the existing request-envelope validation and reuses it for standalone peer handshakes. This shares the admission rule without enabling additional standalone consumer capabilities or changing peer/resource ownership. Eight invalid versions (future, zero, missing, null, string, Boolean, array and object) are refused with the exact reason and no intermediate acknowledgment; the healthy write/read control passes after each. Edited conditions are named. A constant parsed binding provides TypeScript narrowing without assertions; the failed intermediate typecheck remains archived.

Final evidence: 18 affected runtime cases pass on Node 22.15.0 in 32.2 seconds; 36 existing MCP, CLI shutdown, worker relay and peer-standby regressions pass in four isolated files. Strict CLI/fixture types, four-file source form and whitespace checks pass. The changes stay in Node server modules; browser sources and their applicable previous boundary evidence remain unchanged. Reports, original red/green inputs and final hashes are in `ignored/hardening/standalone-admission/verified-inputs.json`. Malformed standalone peer fields and outer JSON/envelopes, other request/reply fields, detailed payloads, outbound/depth bounds and later queue work remain open. No manual-demo change or push retry was made.


## Task 8: Peer handshake fields — verified locally

Three public red/green cycles cover tool lists, sandbox identity and capabilities. `tools: null` and `sandboxId: null` reached unchecked field accesses and produced abnormal close code 1006 instead of bounded refusal; a numeric capability list was accepted. Each unchanged original fixture now receives close code 1002, retains the healthy peer's MCP-to-SDK write and leaves the host usable. The existing shared request-envelope boundary validates these fields before either handler registers a peer.

Review exercises 21 malformed variants through each real CLI topology (standalone and mounted): 42 refusals, each followed by a unique MCP write that reads back from the original peer through public Firestore SDK calls. Buffered valid handshakes cannot register after refusal, and no intermediate acknowledgment is sent. Positive controls preserve omitted optional capabilities, empty lists, string identities including the existing empty-string case, and unknown string tool/capability names. The fixtures use public `connectBridge` peers; the combined group separately exercises actual hosted, default SharedWorker and genuine in-page browser paths.

The mounted handler's obsolete array coercion is removed. Its previous regression expected malformed lists to be admitted as empty lists; that expectation now requires refusal, no registered peer and no acknowledgment, matching the approved malformed-request contract. No resource owner, service codec, private test hook or additional standalone consumer capability was introduced.

Final evidence: 24 affected runtime cases pass on Node 22.15.0 in 54.9 seconds; 39 MCP, relay, peer-standby, session and CLI shutdown regressions pass in five isolated files. Strict CLI/fixture types, five-file source form and whitespace checks pass. Browser-leaf sources and Pyric artifacts remain unchanged, retaining their applicable prior boundary evidence. Reports, all three original red/green pairs and final source/artifact hashes are in `ignored/hardening/peer-handshake-fields/verified-inputs.json`. Standalone outer JSON/envelopes, other request/reply fields, detailed payloads, outbound/depth limits and later queue work remain open. No manual-demo change or push retry was made.


## Task 8: Standalone outer-frame refusal — verified locally

Two public red/green cycles reproduced malformed JSON and unrecognised envelope kinds being ignored by the standalone handler. Both original fixtures now receive close code 1002 before the buffered valid handshake can replace the healthy peer. A normal MCP write still reaches that peer and reads back through the public Firestore SDK; CLI shutdown remains clean.

Review consolidates the identical outer parsing rules into `parseBridgeMessage`, returning an explicit message/invalid union. Each connection owner retains its own closure and lifetime decisions. Eleven invalid inputs cover empty/truncated/malformed JSON, scalar values, arrays, missing/invalid types and an unknown type. Each refusal has the exact diagnostic and no intermediate reply, followed by a unique healthy write/read control. Repeated peer/MCP setup is extracted into one test fixture used by the outer-frame, version and field-validation scenarios; all three callers retain their public assertions and run in the final group.

Final verification: 27 affected runtime cases pass on Node 22.15.0 in 1.0 minute; 39 MCP, relay, peer-standby, session and CLI shutdown regressions pass in five isolated files. Strict CLI/fixture types, seven-file source form and whitespace checks pass. Only Node server parsing changes; browser sources, wire values and Pyric artifacts retain their applicable previous boundary evidence. Original red/green pairs and final report/input/artifact hashes are recorded in `ignored/hardening/standalone-outer-frame/verified-inputs.json`.

This closes the known standalone outer-parsing divergence, not all of item 8. Detailed request/reply fields, service arguments, outbound frames and nesting bounds remain open, as do the later queue and final verification/handoff. The manual demo is untouched and no push retry was attempted.


## Task 8: Encoded document write depth — verified locally

An S1/S2/S3 regression replaced a normal SDK write's data with 65 nested containers. The write incorrectly reported Written. A 19-line codec leaf now counts encoded containers before the shared host write-preparation path recursively decodes values or resolves sentinels. It raises the existing FirebaseError with invalid-argument above 64 containers. The unchanged original regression passes, verifies no mutation in an independent app and confirms both clients remain usable. The payload root counts; the RPC envelope does not.

Separate review drives public SDK writes in actual hosted and default SharedWorker runtimes. Ordinary maps, alternating maps/arrays and marker-shaped maps pass at encoded depths 63 and 64 and refuse at 65 and 256. Escaped maps include their added fields container. Rejected set/update/add operations preserve existing data; a valid first write followed by an over-depth write in a batch or transaction changes neither document. Each runtime ends with an ordinary SDK write/listener round trip. These are 32 boundary/write-family probes within two host lifecycles.

The first review fixture stalled when Playwright transferred a 256-container argument, after its 63/64/65 map checks had passed in both runtimes. Passing the same document as JSON text and reconstructing it in the page resolves the fixture transfer problem without changing production code or the SDK input. Both complete review scenarios pass in 4.3 seconds. The initial timeout report and fixture remain archived separately; they are not behavioral red evidence for a product fix.

Final terminal checks: 120 affected browser scenarios pass on Node 22.15.0 in 3.0 minutes, including merge/atomic failure semantics, transaction snapshots, value encoding, checkpoint values, restart and genuine in-page controls. Sixteen existing codec/write/atomic regressions pass in five isolated files. Pyric and CLI builds, strict fixture types, five-file changed-code form and all five browser boundaries pass. The codec is 18,638/24,576 bytes; the live and worker-client entries remain 387,843/524,288 and 54,943/98,304 bytes. No retries or skips. Reports, original TDD inputs, source/test/lockfile identities and rebuilt artifact digests are retained in ignored/hardening/document-depth/verified-inputs.json.

Review confirms one shared codec leaf and existing cross-service error identity, with no new resource owner, Node/Buffer branch or conformance registry change. This completes only Firestore write-payload depth validation. Query values, transaction read sets, response/import decoding and outbound size bounds still need their applicable checks; item 8 and later queue items remain open. The manual demo is untouched and no push retry was attempted.


## Task 8: Transaction read-set depth — verified locally

The existing malformed-atomic fixture now includes a serialized read document with 65 encoded containers. Both hosted and SharedWorker returned aborted, treating invalid input as a read conflict after decoding. A single call to the existing shared depth validator now precedes read-set rehydration. Both original fixtures pass unchanged (5.2 seconds), refusing with invalid-argument before document reads or writes. No new decoder, bound or runtime owner was added.

Separate review asserts that malformed read depth invokes the application's transaction callback once, rather than entering the SDK conflict retry loop. Twelve public SDK boundary probes across two runtime lifecycles read ordinary maps, mixed arrays/maps and escaped marker maps at encoded depths 63 and 64. Each transaction commits its companion document in one attempt, returns the original data and leaves the read document unchanged. The existing write-depth review now shares its unchanged document builder through document-depth-fixture.ts; both callers pass after extraction.

Final terminal checks pass 45 affected browser scenarios on Node 22.15.0 in 1.2 minutes, covering malformed atomic/read inputs, write depth, transaction snapshots in all three runtimes and transactions interrupted by reset/import. Sixty-five existing host, write-rehydration and remote-session regressions pass in three isolated files. Strict CLI/fixture types, five-file changed-code form and whitespace checks pass. The preceding five browser-boundary reports are reused: only an existing host call changed, with no import change; the host file is absent from all five contributor lists, and Pyric artifacts, lockfile and package/compiler configuration identities match. Reports, original red/green fixture copies and source/test/artifact identities are retained in ignored/hardening/transaction-read-depth/verified-inputs.json. No retries or skips.

This closes transaction read-set document depth, not all validation. Query values, reply/import decoding, detailed method payloads and outbound bounds remain open along with later queue items. Existing shared errors and the common host path preserve hosted/SharedWorker parity; no Buffer fallback, resource owner or conformance registry change applies. The manual demo is untouched and no push retry was attempted.


## Task 8: Query operand depth and served count queries — verified locally

Public SDK queries with a 65-container filter operand incorrectly returned an empty result. The unchanged original regression passes after a preflight check in the host constraint resolver. A second cycle reproduced and fixed the same failure for startAt. Review then reproduced it for startAfter, endAt and endBefore; all five original refusal cases pass after validation. Refusal reaches the requesting app with invalid-argument, while a second app can write and both apps' original listeners continue receiving changes.

Separate review consolidates cursor validation and decoding into a local helper. Every cursor operand is validated before any cursor value is decoded; the cursor tuple and query envelope do not add to each operand's container count. The resolver parameter now names the constraint. No new codec, limit constant, transport state or resource owner was added.

The review drives 160 query operations across two real runtime lifecycles: ordinary maps, mixed arrays/maps and escaped marker maps at encoded depths 63, 64, 65 and 256 through filters and all four cursor directions, plus count/listener rejection and recovery. At depth 63, the stored value matches exactly; inclusive cursors retain the boundary and exclusive cursors exclude it. Valid operands at depth 64 are accepted and over-deep inputs refuse. Normal SDK writes/listeners remain usable afterward.

The first count review exposed a separate served-export gap: getCountFromServer existed in both underlying implementations but was absent from the served SDK. A dedicated three-runtime regression reproduced the missing function. One export through the existing runtime selection fixes it, with the original three cases passing unchanged. Review confirms collection counts, filtered counts, deletion, stable prior results and Rules denial after sign-out in hosted, default SharedWorker and genuine in-page modes. This restores the existing count result contract; it does not claim the entire Firebase aggregation surface is implemented. This fix is committed separately as 0536b8ad.

Final terminal checks pass 35 affected browser scenarios on Node 22.15.0 in 50.5 seconds, including query-depth/cursor review, count routing/Rules, reference operands and timestamp precision in all three runtimes. Sixty-three host, composite-query and subscription regressions pass across four isolated files. Strict CLI/fixture types, six-file source form and whitespace checks pass. The five previous browser-boundary reports are reused after matching Pyric artifact, lockfile and compiler/package identities and confirming the two edited production files are absent from their contributor lists. The actual served entry is freshly built and exercised; those leaf budgets do not purport to bound the full served SDK bundle. No retries or skips. All cycle fixtures, source snapshots, reports and current input/artifact identities are retained in ignored/hardening/query-value-depth/verified-inputs.json.

This completes filter/cursor operand depth validation and the scoped count export. Query descriptor/composite-tree validation, reply/import decoding, other detailed method payloads and outbound bounds remain open, as do later queue items. No Node/Buffer fallback or conformance registry change applies. The manual demo is untouched and no push retry was attempted.

## Task 8: Mounted consumer output bounds — verified locally

The mounted bridge now checks encoded UTF-8 output against the declared 12 MiB frame limit before sending to consumers. Oversized hosted worker replies and snapshots, and legacy relay replies and snapshots, return a correlated `resource-exhausted` error. A frame that cannot be reduced within the limit closes only that connection with code 1009. The sender shares the protocol limit and existing error envelopes; existing peer/session teardown retains resource ownership.

Four public-boundary cycles establish the change: a seven-document SDK collection read previously returned an oversized result; a listener snapshot initially closed the app connection; a legacy reply over the exact byte boundary closed instead of returning a request error; and a legacy subscription did the same. During listener verification, a second close came from its optional observation sample. Size-only diagnostics identified a single `snapshot_delivery` event above 14 MiB. The bounded output now omits optional snapshot samples from oversized event batches while retaining every event and its metadata. Temporary diagnostics were removed. This is not evidence of bounded observation history, queue retention or slow-client behavior.

All original failing assertions and corresponding unchanged-fixture passing runs are retained in `ignored/hardening/outbound-frame-limit/`. The initial source type failure, a runtime run started before that type correction, and the listener follow-up close are preserved as non-final evidence. Separate review consolidated typed frame reduction and error construction, preserved correlation fields, and added multibyte UTF-8 payloads, retained observation count/sample assertions and an end-of-scenario frame-size check. Public wire checks prove replies at limit minus one and exactly the limit succeed, while limit plus one refuses; both clients remain usable after refusal.

Final verification on Node 22.15.0: 19 affected browser scenarios passed in 1.2 minutes, including restart/admission, exact inbound boundaries, supported 8 MiB Storage transfers in hosted and default SharedWorker, and served SDK controls in hosted/SharedWorker/genuine in-page runtimes. Another 36 relay, peer-standby, remote-session isolation and binary regressions passed in four isolated files. Strict CLI build and fixture types, five-file changed-code form and whitespace checks passed. Five prior browser boundary budgets are reused after verifying unchanged contributors, package/compiler inputs and Pyric artifacts. Exact commands, reports and input/artifact hashes are in `ignored/hardening/outbound-frame-limit/verified-inputs.json`.

Scope is the mounted consumer output callback. Standalone and sandbox-peer senders, browser-to-host outgoing bounds, detailed request/reply validation and other decode paths still need their applicable proofs. Other large event kinds can still take the connection-close fallback; queue/history limits remain item 16. No new browser/Buffer fallback or registry rows were introduced. Item 8 and later queue items remain open. The manual demo is untouched and no push retry was attempted.

## Task 8: Unsupported Firestore target kinds — verified locally

A public legacy wire request with `__ref: 'unknown'`, a valid collection source and an empty constraint list executed successfully. The shared target resolver treated every unrecognized kind as a query. It now explicitly requires `query` after resolving document, collection and group descriptors, returning a correlated `invalid-argument` error otherwise. The unchanged original wire fixture passed after the fix. Separate review removed an unnecessary target cast and named the mapped constraint.

Review exercises seven malformed discriminator values at both top-level and nested source positions through reads, counts, aggregates and subscriptions: 112 refusals across hosted and default SharedWorker. Each connection then completes a valid query, and its normal SDK app can still write and receive a listener update. Final verification: 30 browser scenarios passed in 54.4 seconds on Node 22.15.0, including valid reference/cursor queries and genuine in-page controls; 63 shared-host/composite/subscription regressions passed in four isolated files; strict CLI/fixture types, two-file code form and whitespace passed. Five unchanged browser boundary reports are reused after contributor, compiler/package and Pyric artifact checks. Red/green fixtures, exact commands, reports and input/artifact hashes are in `ignored/hardening/query-target-refusal/verified-inputs.json`.

This establishes target discriminator refusal, not complete target/constraint shape validation or composite-tree bounds. Remaining item 8 work and later queue items stay open. No new resource owner, browser/Buffer fallback or registry row was added. The manual demo is untouched and no push retry was attempted.

## Task 8: Invalid query order directions — verified locally

The served hosted SDK accepted `orderBy('message', 'sideways')` and completed its read, because the shared query builder retained the unsupported direction and execution treated it as ascending. A guard in the existing admin-compatible query builder now requires `asc` or `desc` after its default argument resolves. It throws the existing `FirestoreCompatError` with `invalid-argument`, which the modular SDK/worker path already preserves. This avoids a separate transport validator and applies to hosted, default SharedWorker and genuine in-page queries.

The original public SDK fixture failed before the change and passed unchanged after rebuilding Pyric and CLI. Separate review covers nine malformed directions through reads, counts and listeners in all three runtimes (81 refusals), then omitted/ascending/descending ordering through those paths (27 valid cases). Both apps retain their own SDK write/listener round trip; in-page stores remain independent. Final checks: 33 affected browser scenarios passed in 1.0 minute on Node 22.15.0; 153 query, cursor, aggregate, Rules, listener and worker regressions passed in ten isolated files. Pyric/CLI builds, strict fixture types, two-file code form, whitespace and five fresh browser boundary budgets passed. Exact reports, original red/green fixture hashes and source/artifact identities are in `ignored/hardening/query-direction-refusal/verified-inputs.json`.

This closes direction validation only. Other constraint fields, target shapes, structural nesting and outstanding outbound paths still need their applicable proofs; item 8 and later tasks remain open. Existing shared error conversion and resource ownership are unchanged, with no browser/Buffer branch or conformance registry edits. The manual demo is untouched and no push retry was attempted.

## Task 8: Query limit values and zero tail counts — verified locally

Both `limit('1')` and `limitToLast('1')` previously executed by coercing the string into a count. Separate served-SDK red/green cycles now require finite numbers through one shared assertion in the existing query builder. Review retained the repository's explicit `limit(0)` behavior and found that `limitToLast(0)` returned every document: `slice(-0)` starts at the beginning. A third named public regression confirms that failure; shared query execution now produces an empty tail for zero while retaining the existing explicit-order requirement.

The original head-limit, tail-limit and named zero-tail fixtures each have unchanged passing counterparts. The first zero-case test selector matched no tests; that setup failure is retained separately from the corrected red. Separate review consolidates the finite check, uses literal expected result rows, and tests missing/scalar/container/non-finite counts through reads, counts and listeners in hosted, default SharedWorker and genuine in-page runtimes. It proves 180 malformed refusals and 72 valid-count cases, followed by healthy SDK write/listener round trips in both apps. Other finite numerical policies, including negative/fractional counts, were not changed or established by this slice.

Final verification: 17 affected browser scenarios passed in 28.4 seconds on Node 22.15.0; 163 execution/top-K/query/cursor/aggregate/Rules/listener/worker regressions passed in twelve isolated files. Pyric build, strict CLI and fixture types, three-file code form, whitespace and five fresh browser boundary checks passed. CLI source/emitted artifacts remain unchanged from their preceding verified build; the runtime fixtures exercised the newly built Pyric artifacts. Exact commands, reports, TDD pairs and current input/artifact hashes are in `ignored/hardening/query-limit-refusal/verified-inputs.json`.

Other payload shapes, structural bounds and outstanding outbound paths remain open alongside later queue work. The shared error conversion and existing subscription/resource owners are retained. No browser/Buffer branch, registry row, manual-demo modification or push retry was introduced.

## Task 8: Unsupported filter operators — verified locally

A served SDK query using the unsupported `===` operator returned an empty result instead of refusing the request. Shared filter construction now checks the operator before capturing its operand or matching any rows. The original public SDK red fixture passes unchanged after the fix. Separate review moves the supported operator list into one small shared module; runtime validation, the existing exported type and the Firestore tool schema derive from that list. The tool schema's members/order remain unchanged. The touched filter constructor uses named decisions and no longer needs its result cast.

Review exercises eight malformed operators through reads, counts and listeners, against populated and empty collections, both directly and inside false-AND/true-OR composites. All 432 cases refuse in hosted, default SharedWorker and genuine in-page runtimes, independent of row matching or short circuits. All ten supported operators retain literal expected rows/counts in another 90 cases, and the SDK app still completes a write/listener round trip. Fixture review exposed narrow-variable, SDK-overload and generic-inference type errors; these are retained separately. Final fixture code selects the appropriate SDK overload with named decisions and preserves the source query's document type.

Final checks: 11 affected browser scenarios passed in 17.0 seconds on Node 22.15.0; 142 filter/equality/composite/Rules/worker/tool-surface regressions passed in eleven isolated files. Pyric build, strict CLI/fixture types, five-file source form, whitespace and five fresh browser boundary checks passed. CLI source/emitted artifacts still match the preceding verified build; browser fixtures consumed current Pyric artifacts. Original red/green fixtures, non-final review failures, exact commands and current source/artifact hashes are in `ignored/hardening/query-operator-refusal/verified-inputs.json`.

This closes operator-kind validation, not all filter operand/field shapes or structural nesting. Other item 8 boundaries and later tasks remain open. Query construction validation does not change Rules expression error absorption or introduce a resource owner, browser/Buffer fallback or registry row. The manual demo is untouched and no push retry was attempted.

## Task 8: Array-membership operand shapes — verified locally

A served SDK `in` query with a scalar operand returned an empty result without an error. The existing shared filter validator now checks that `in`, `not-in` and `array-contains-any` receive an array before operand capture and query execution. The original public SDK fixture failed before the guard and passed unchanged afterward. Two characterization expectations explicitly recorded the old silent behavior as-is; their invalid-operand assertions and explanatory text now reflect refusal, retaining their valid-array assertions.

Separate review reuses an extracted SDK filter fixture for both operator and operand checks. Membership review performs 1,296 malformed-input refusal checks across the three operators, populated/empty collections, plain/false-AND/true-OR filters, read/count/listener paths and hosted/SharedWorker/in-page runtimes. This includes values that the existing codec already refused; it is not a claim that every case was newly broken. Another 81 checks preserve the current empty-array and null-element semantics, and both active apps complete SDK write/listener round trips after refusal. These are existing Pyric array semantics, not a new assertion of upstream equivalence for empty membership lists.

Final verification: 14 affected browser scenarios passed in 27.2 seconds on Node 22.15.0; 142 query/composite/characterization/Rules/worker/tool regressions passed in eleven isolated files. Pyric build, strict CLI/hosted-fixture types, a separate strict check of the changed characterization file, six-file source form, whitespace and five fresh browser boundary checks passed. CLI source/emitted artifacts remain unchanged from their preceding verified build; runtime checks consume current Pyric artifacts. The original red/green fixture, prior characterization and helper source, exact commands, reports and current hashes are in `ignored/hardening/query-array-operand-refusal/verified-inputs.json`.

Other operand/field policies, structural nesting and remaining item 8 boundaries stay open alongside later tasks. Existing error conversion and resource ownership are retained, without a browser/Buffer branch or registry-row change. The manual demo is untouched and no push retry was attempted.


## Final integration review: schema type dependencies — verified locally

The U4 source audit found three newly cyclic edges through the shared state-schema barrel: its completeness checks imported Auth backend types and the Storage snapshot implementation. These are type-only dependencies, not a reproduced runtime failure. Review moved the unchanged `SeedUser` interface to a dependency-free Auth leaf, retained its existing re-export, and pointed Storage schema completeness directly at `StoredMetadata`. The checks still require every account and metadata field; no schema, runtime behavior or resource owner was removed. The ownership map records the type boundary.

The before/after static import audit includes type-only edges, literal dynamic imports and workspace exports mapped to source. It resolves all local edges in 1,017 CLI/Pyric production and generated source files; none of the 48 added edges closes a cycle after review. Third-party internals and computed dynamic imports are outside this scoped graph. Before/after graphs are retained in `ignored/hardening/handoff-audit/`. This is an architecture review refactor, not another behavioral TDD fix.

Strict Pyric build and downstream CLI types pass. All 1,027 previously emitted JavaScript files match their pre-review bytes; the new leaf emits only an empty export and has no runtime importer. The unchanged public runtime evidence and five browser-budget reports are reused with matching compiler/package/lock/checker inputs. Three-file review form and the full 123-file milestone form have zero findings. The combined affected checks passed 216 browser scenarios in 9.6 minutes and 1,138 regressions in 123 distinct isolated files. A freshly rebuilt standalone binary passes four copied-artifact checks in 16.7 seconds. Exact commands and reports are retained for the handoff; full packed installation/Vite/HMR remains item 14, and item 8 remains partial. No push retry or manual-demo change was made.

## Section 1 acceptance: request bounds and lifecycle cleanup

Start: `5432686c` on `hosted-live-mode`, after the completed overnight QA checkpoint. This section is active and ends at its own manual QA checkpoint. It does not complete the hosted sandbox release or begin section 2. The initial reconciliation below is a finite gate inventory derived from queue items 8–9, original gates 1C/2A–C/3A–C/3E/5B–C and universal gates U1–U7. A row closes only after its whole stated boundary is covered. Existing passing slices are evidence for their exact assertions, not blanket completion of a row.

| ID | Remaining acceptance requirement | Existing evidence to reconcile | Status |
| --- | --- | --- | --- |
| S1-R1 | Enumerate known request and reply frame kinds from the protocol; validate required fields, correlation and discriminators before dispatch/resolution in browser worker, sandbox-peer and published remote paths. Malformed replies settle or explicitly terminate affected work without stranding calls; another client stays usable. | Outer/envelope/correlation/admission fixtures; peer and remote host-protocol fixtures. | Open: detailed known-frame fields and replies. |
| S1-R2 | Cover advertised service/method payload families and nested Firestore target/query/composite descriptors. Invalid structural inputs refuse before state mutation, registration or recursive evaluation. Use existing service validators; unsupported methods refuse explicitly. | Atomic-write, transaction-read and query-kind/direction/limit/operator/operand fixtures. | Open: remaining descriptor/argument shapes and structural nesting. |
| S1-R3 | Enforce 12 MiB encoded UTF-8 frames before every supported socket dispatch/send: browser worker, public remote, sandbox peer, mounted serve/Vite and standalone bridge. Exercise below/equal/above limits, fragmentation on input, multibyte data, correlated refusal and subsequent healthy work. Preserve supported 8 MiB Storage transfers. | Inbound mounted/standalone and mounted consumer-output fixtures. | Open: exact browser outbound boundaries, server-to-peer senders, and unreducible peer output. Peer reply and remote/browser size-admission evidence is recorded below. |
| S1-R4 | Apply the declared 64-container encoded-document bound before recursive decoding in requests, replies/snapshots, remote consumers and import/checkpoint paths. Preserve ordinary/escaped maps and special values at the boundary; rejected replacement leaves state intact. | Write, transaction-read, filter/cursor depth fixtures and scoped checkpoint corruption cases. | Open: remaining decode paths; structural query recursion belongs to R2. |
| S1-R5 | Enforce 256 pending operations and 24 MiB queued operation bytes per client before acceptance. Saturating either bound refuses new work without dropping accepted work, transport replay or starving a separate client. Release accounting on completion, failure and disposal. | Existing per-consumer ordering and relay regressions do not prove numerical bounds. | Open. |
| S1-L1 | Startup failure at each allocating stage and close during startup release sockets, partial runtimes, listeners and project ownership. Close completion cannot precede cleanup; repeated close shares completion; reopening uses one owner. | CLI ownership, failed startup, Vite lifecycle and copied standalone fixtures. | Open: stage inventory and repeated external resource accounting. |
| S1-L2 | Failed/slow browser init and attach respect the five-second stage deadlines, reject dependent work and release physical connections without fallback storage. Deletion during each stage cancels late readiness and registration. | Existing startup/timeout, protocol refusal and delete-during-recovery scenarios. | Open: full initialization/cancellation accounting. |
| S1-L3 | Reconnect has one owner with declared backoff; deletion stops attempts. Heartbeat loss (15-second cadence/45-second liveness limit) uses the existing interruption transition. Replaced socket callbacks cannot affect the replacement. Session retention expiry releases retained resources. | Interrupted recovery, stale callbacks, real retention-expiry and remote natural-exit evidence. | Open: heartbeat/backoff and resource accounting; identity restoration semantics remain section 2. |
| S1-L4 | App/remote deletion and host/bridge shutdown with active subscriptions and pending work finish or explicitly cancel accepted work under its contract. Release connections, timers, listener intent, pending requests and writer ownership; repeated disposal has no late deliveries or leaked child processes. Compare SharedWorker and genuine in-page where applicable. | Remote process lifecycle, unsubscribe, active replacement and ownership tests. | Open: complete owner/resource inventory and repeat-cycle accounting. |
| S1-J | Review all rows against the candidate; run combined affected checks once, strict production/fixture types, changed-code form, scoped import/browser budgets and applicable artifact checks. Commit/push verified slices and deliver a disposable two-browser manual QA script; wait for user feedback. | Prior combined 216-browser/1,138-regression report can be reused only where unchanged inputs and coverage warrant it. | Pending prerequisites. |

The protocol, worker operation/subscription unions and their service dispatchers define R1/R2's finite method inventory; record uncovered families when auditing those owners rather than adding unrelated SDK fidelity policies. For L1–L4, name each allocating stage/resource owner and its external observation before writing its fixture. Use approved seams S1–S3 and existing diagnostics; do not treat private registry sizes or a single RSS reading as disposal proof.

Observation history/capture retention and slow-consumer performance remain queue item 16/original gate 6B. Full packed-install/Vite/HMR, real version combinations, Rules reload, identity/tenant semantics, RTDB disconnect effects and general persistence atomicity stay in their later sections. This does not defer request queues, socket buffers or cleanup required by R1–R5/L1–L4. New findings must map to one of these rows or be recorded separately; a failed required assertion cannot be waived to close the section.

### S1-R3: bounded SharedWorker peer responses

SharedWorker sandbox-peer output now uses the same browser-safe UTF-8 frame encoder as mounted hosted replies. Oversized worker results and listener snapshots retain correlation and return `resource-exhausted` before socket send; optional observation samples may be omitted while their summaries remain. The existing close fallback remains for frames that cannot be reduced. This does not yet establish the remaining outbound request, standalone peer-command or remote/browser input boundaries.

The original public remote read test failed with `unavailable` after the oversized peer reply disconnected, then passed unchanged with the correlated size error (6.0 seconds). Review added a remote listener case, multibyte payloads, actual sent-frame bounds, no socket closure, and successful subsequent operations from both clients. The encoder has no engine/Node imports or new resource owner; SharedWorker selection and in-page operation are unchanged.

Integration review found a weakness in the existing hosted observation test: its Playwright network observer still saw snapshot summaries removed by a WebSocket route, so that negative control incorrectly passed. The test now observes messages delivered to the actual browser socket and waits for a later write event on the same stream before checking the summary. Dropping only that summary now fails the intended assertion after the later event arrives. The initial intermittent missing-summary failure had no retained causal trace; retries alone are not counted as a fix. Temporary fault injection and diagnostic loops are removed.

Final checks: 19 affected browser scenarios pass on Node 22.15.0 in 57.6 seconds, with zero skips/retries; strict fixture types, six-file source form and whitespace checks pass. The unchanged production slice retains its passing CLI build and 36 regressions in four isolated files. The scoped import audit covers 1,018 files with four added edges and no added cycles or unresolved imports. All five declared browser budgets pass; the new encoder also bundles alone to 910 bytes without external imports (a diagnostic, not a new release budget). Reports, red/green fixtures, negative-control evidence and source/artifact hashes are retained under `ignored/section1/peer-response-limit/`. S1-R3 and section 1 remain open.

### S1-R3: remote request admission and refusal cleanup

The published remote client's socket adapter now checks the full encoded UTF-8 frame before sending. Oversized operations and subscriptions deliver a correlated local `resource-exhausted` refusal through the existing response owner. That owner removes pending requests/timers or terminal listeners and updates its event-loop hold. No reconnect or replay is introduced. The adapter reuses the shared frame encoder rather than duplicating serialization limits.

The real-host operation test first failed with `unavailable` from the disconnected socket and then passed unchanged. The subscription test next failed because the size error threw without its terminal callback, then passed unchanged after routing refusal through the listener owner. Review verifies no rejected-write mutation, successful subsequent operations and subscriptions, exactly one terminal error and idempotent unsubscribe. A wire fixture accepting up to 24 MiB independently observes that 12 MiB minus one byte and exactly 12 MiB are sent, while 12 MiB plus one byte is refused locally; multibyte payloads count the complete envelope. A subsequent response establishes that no refused request reached the wire. Three operation/subscription refusal cycles in a child process permit natural exit without unsubscribe, close or forced exit.

Final checks: 14 affected browser/wire/process scenarios pass on Node 22.15.0 in 23.4 seconds, zero retries/skips. Thirty-eight remote lifecycle/session regressions pass in four isolated files. Strict CLI and fixture types, four-file source form and whitespace pass. The scoped 1,018-file import graph adds one noncyclic edge with no unresolved imports. No browser entry or shared encoder changed, so the previous browser-budget results remain applicable. Evidence and source/artifact identities are retained under `ignored/section1/remote-request-limit/`. This verifies remote outbound op/sub size admission and its refusal cleanup, not remote inbound frame/decode bounds or arbitrary transport-failure cleanup. Those and the remaining section rows stay open.

### S1-R3: remote receive frame bound

The published remote socket now supplies the shared 12 MiB bound to `ws`, which applies it to the assembled message before the JSON handler receives it. A public-channel wire test first demonstrated acceptance of an oversized fragmented reply; the unchanged test then passed with the call rejected and the offending connection closed with code 1009. Review covers exactly below/equal/above the limit using multibyte fragmented replies, continued work after accepted frames, refusal of later work on the closed connection, and a separate healthy client.

Seventeen affected wire/browser/process cases pass on Node 22.15.0 in 23.4 seconds, with no retries/skips, including both hosted and SharedWorker 8 MiB Storage transfers. CLI build, strict fixtures, two-file code form and whitespace pass. No new import edge, resource owner or browser contributor was added; the preceding scoped graph, browser-budget evidence and unchanged core regression evidence remain applicable. Exact reports and source/artifact hashes are in `ignored/section1/remote-reply-limit/`. This closes the remote transport's encoded receive-size gap; reply field/decode validation and broader disconnect notification/resource accounting remain in R1/R4/L4. S1-R3 remains open for browser sockets and server-to-peer paths.

### S1-R3/L4: browser request admission and failed listener registration

The browser socket sender uses the shared frame encoder and refuses oversized requests before sending. Startup queue admission applies the same bound before retaining the request. A failed snapshot-subscription post now removes its registration before rethrowing, so refused listener creation cannot leave intent for later restoration. These changes reuse existing error and correlation owners; no new resource owner or runtime selector was introduced.

Three public SDK red/green pairs establish the behavior. An oversized connected write originally returned `unavailable` after socket loss; it now returns `resource-exhausted` while both browsers continue writing. Holding app attachment left an oversized startup write pending; it now rejects while attachment is still held, and accepted startup work completes after release without the refused write appearing in shared state. Finally, a refused listener was retained and its replay closed the app on reconnect; removal on failed posting preserves successful reconnect and subsequent writes. Synchronous listener-creation failure remains synchronous.

Fifteen affected browser scenarios pass on Node 22.15.0 in 33.7 seconds, zero skips/retries, covering startup cancellation, interrupted recovery, default SharedWorker peer responses and both hosted/SharedWorker Storage capacity. Thirty-three client/port/lifecycle regressions pass in six isolated files. CLI build, strict browser fixtures, five-file source form and whitespace pass. The scoped graph adds one noncyclic edge across 1,018 files, with no unresolved imports. Affected browser budgets pass: client 54,983/98,304 bytes; socket 8,446/16,384; RTDB listener 12,969/32,768, with no forbidden imports. Unchanged codec/live leaf evidence remains applicable. Reports and hashes are under `ignored/section1/browser-request-limit/`.

This slice does not close R3/L4: browser receive validation and exact browser boundary characterization, server-to-peer senders, failed event-subscription posting, terminal-error cleanup, and other lifecycle/resource rows remain open. Numerical startup queue bounds remain R5.

### S1-R3: browser receive bound and browser-valid refusal code

Hosted browser input is measured as UTF-8 before JSON parsing or SDK dispatch. Oversized input closes the affected connection and rejects pending work through the existing owner. The original real-host wire-injection test demonstrated acceptance of an oversized reply. A first implementation attempted close code 1009 and stranded the call: a direct Chromium check produced `InvalidAccessError`, explaining that browser code may initiate only 1000 or application codes 3000–4999. The browser refusal contract therefore uses 4009; Node keeps 1009. The corrected original scenario passes, and exact-boundary review covers 12 MiB minus one byte, exactly 12 MiB, and one byte over with multibyte content and a healthy second browser. This changes the invalid browser-code assumption, not the size or cleanup requirement.

The test cleanup now stops its host even if context cleanup fails. Process inspection after the failed diagnostic found no surviving disposable host from this worktree; the manual server was left untouched. A review run was launched before an optional-close-code fixture type error was noticed; it is archived as diagnostic evidence only. After fixing the type and passing strict fixtures, the final 18 affected browser/wire scenarios pass on Node 22.15.0 in 32.9 seconds, zero retries/skips. CLI build, three-file source form and whitespace pass. The socket bundle passes at 8,544/16,384 bytes without forbidden imports; the encoder's separate 953-byte check is diagnostic, not a new release budget. The scoped 1,018-file graph has no new edges or unresolved imports. Unchanged core regressions and other browser leaves retain their preceding evidence. Reports and source/artifact hashes are under `ignored/section1/browser-reply-limit/`.

The support contract explicitly distinguishes browser-initiated 4009 from Node 1009 and acknowledges that native browser message assembly precedes this guard. The browser peer's pre-existing unreducible-output fallback still attempts 1009 and needs its own public reproduction/fix. Server-to-peer sends, exact browser outbound characterization, and other R1–R5/L1–L4 requirements remain open; this is not section completion.
