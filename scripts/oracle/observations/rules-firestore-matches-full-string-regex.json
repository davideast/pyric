{
  "name": "rules-firestore-matches-full-string-regex",
  "matrixRow": "",
  "rowIds": [],
  "description": "Firestore Rules Test API verdicts for corpus pack \"matches-full-string-regex\" (RULES-B4). matches() is an anchored full-string RE2 match; a pattern matching only a substring must be false (JS partial-match said true).",
  "observedAt": "2026-07-10T22:41:19.540Z",
  "fbSdkVersion": "12.13.0",
  "projectId": "digame-mas",
  "behavior": {
    "'hello world'.matches('world') → DENY (no partial match)": "DENY",
    "'hello'.matches('hello') → ALLOW": "ALLOW",
    "'hello world'.matches('.*world') → ALLOW": "ALLOW",
    "'abc123'.matches('[a-z]+[0-9]+') → ALLOW (full consume)": "ALLOW",
    "'abc123!'.matches('[a-z]+[0-9]+') → DENY (trailing char)": "DENY",
    "alice@acme.com vs full-string email pattern → ALLOW": "ALLOW",
    "1alice@acme.com vs same pattern → DENY (substring would partial-match)": "DENY"
  }
}
