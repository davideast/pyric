{
  "name": "pyric",
  "description": "Drive a local Firebase sandbox (pyric serve --bridge) from Claude Code — auto-wires the MCP endpoint via a stdio proxy, no manual `claude mcp add`.",
  "version": "0.0.0",
  "homepage": "https://github.com/davideast/pyric"
}
