{
  "id": "experiment/admin-claim-write",
  "skill": "firebase-security-rules",
  "description": "Any signed-in user reads /config/{id}; only users with an admin custom claim may write.",
  "prompt": "Write Firestore rules so any signed-in user can read /config/{configId}, but only users whose auth token has a custom claim admin == true can create, update, or delete it.",
  "successSpec": {
    "name": "experiment/conformance",
    "args": {
      "cases": [
        { "method": "get", "path": "config/app", "auth": { "uid": "alice" }, "expect": "ALLOW" },
        { "method": "update", "path": "config/app", "auth": { "uid": "admin1", "token": { "admin": true } }, "expect": "ALLOW" },
        { "method": "update", "path": "config/app", "auth": { "uid": "alice", "token": { "admin": false } }, "expect": "DENY" },
        { "method": "update", "path": "config/app", "auth": null, "expect": "DENY" }
      ]
    }
  }
}
