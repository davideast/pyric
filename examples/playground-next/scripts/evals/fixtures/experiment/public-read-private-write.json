{
  "id": "experiment/public-read-private-write",
  "skill": "firebase-security-rules",
  "description": "Posts are world-readable; only signed-in users create; only the author updates. Unauth writes denied.",
  "prompt": "Write Firestore rules for a /posts/{postId} collection: anyone (even signed-out) can read posts, but only a signed-in user can create a post, and only the author (request.auth.uid == resource.data.authorId) can update or delete it.",
  "successSpec": {
    "name": "experiment/conformance",
    "args": {
      "cases": [
        { "method": "get", "path": "posts/p1", "auth": null, "expect": "ALLOW" },
        { "method": "create", "path": "posts/p1", "auth": { "uid": "alice" }, "data": { "authorId": "alice", "body": "hi" }, "expect": "ALLOW" },
        { "method": "create", "path": "posts/p1", "auth": null, "expect": "DENY" },
        { "method": "update", "path": "posts/p1", "auth": { "uid": "mallory" }, "resource": { "authorId": "alice" }, "expect": "DENY" },
        { "method": "update", "path": "posts/p1", "auth": { "uid": "alice" }, "resource": { "authorId": "alice" }, "expect": "ALLOW" }
      ]
    }
  }
}
