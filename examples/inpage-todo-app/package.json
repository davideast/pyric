{
  "name": "inpage-todo-app",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "description": "Reference example of an in-page Pyric sandbox: clean architectural seams decoupling Firebase Web SDK mirrors, Pyric simulation drivers, and application UI.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^12.12.0",
    "pyric": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "typescript": "^5.7.0"
  }
}
