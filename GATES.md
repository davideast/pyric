# Gates: hosted sandbox and live mode

OWNS: packages/**, scripts/**, .github/workflows/build.yml, docs/code-conventions.md, docs/hosted-sandbox-live-mode-*, GATES.md

Scope: Complete the user-approved hosted sandbox and live-mode contract in the sequence, premortem, and gate specification. Start with the hosted browser tracer bullet; no stage-level result represents completion of the full goal.

Execution: sequential vertical TDD slices. S1–S6 and their documented scope were explicitly approved in the active goal. SharedWorker remains the default; production writes and publishing are excluded. No agents have been dispatched.

Pending seam clarification: permission was requested to extend S3 from CLI commands to the exported BridgeMount lifecycle API, using the real host and its existing public readiness diagnostic. No reply has arrived and no test at that additional interface has been written. The existing S1–S6 approval remains in force for independent work.

## Current milestone: hosted hardening for manual QA

The active milestone is the ordered reliability queue in `docs/hosted-hardening-progress.md`, extended to twenty items at the user's request; the full hosted/live-mode goal stays incomplete. Tasks 1–5 are verified locally: unique identities, observed session-expiry recovery, bounded checkpoint fidelity/corruption refusal, restoration diagnostics and interrupted recovery. Task 6 is verified locally: reset/import retain active listeners, paused transactions retry against replacement state, and complete portable imports restore Storage data durably through the existing checkpoint and persistence foundations. The import join passes 28 affected browser cases, two minimum-Node cases, fifteen regressions, strict types and source form. The final replacement join passes 42 affected browser cases, ten minimum-Node cases, twenty regressions, strict types, eight-file source form and browser boundaries. A runtime-selection audit withdrew old in-page checkpoint parity claims: the selector actually ran SharedWorker. Corrected fixtures assert runtime mode; in-page worker controls are explicitly unavailable. Genuine in-page MCP/SDK tests exposed and now verify a shared Auth token-to-Rules claim propagation fix. Its tenant-token namespace omission remains open for item 11. Earlier slice evidence is recorded below and in the progress document; later tasks and final combined/package verification remain open.

Item 7 is verified locally with characterization only: an unwritable directory produces explicit uncertainty and mutation refusal, repairing permissions keeps admission closed until restart, and durable Firestore/Storage values recover. The original app does not replay its uncertain increment. The affected group passes 36 cases; the final two recovery scenarios pass on minimum Node in 7.8 seconds, with strict fixture types and source form. Item 8 and later items remain open.

Item 8 has a verified envelope slice: malformed worker bodies and unknown message types close the offending socket before dispatch, rejecting pending work without an uncaught exception. Protocol-error closure permits later recovery; admission refusals remain terminal. Final verification passes 22 affected browser cases on minimum Node (48.1 seconds), 31 bridge regressions, strict CLI/fixture types and two-file source form. Correlation fields, other payloads, versions and size/depth boundaries remain open.

The next item 8 slice validates browser worker correlation IDs before dispatch: malformed request IDs previously allowed a write with an unmatchable reply, and malformed subscription IDs stranded the initial listener. Final evidence passes 31 affected browser cases on minimum Node (1.1 minutes), 31 peer/session regressions, strict CLI/fixture types and three-file source form. Outer/legacy relay validation, method payloads, versions and size/depth limits remain open.

Item 8 now also rejects invalid outer JSON and unrecognized envelope kinds rather than silently stranding SDK requests. The shared malformed-write fixture removes three identical setup/assertion bodies. Final evidence passes 39 affected browser cases on minimum Node (1.4 minutes), 31 peer/session regressions, strict types and five-file source form. Known-frame and legacy relay payload validation, protocol versions and size/depth bounds remain open.

A separate S6 tooling commit corrects class-member scope to match U3's modified-function rule. Modified members stay checked in full, with explicit unchanged-member reporting and whole-class checking for new classes or changed headers. Twenty-eight checker tests and strict types pass. Pushes are blocked by automatic approval review pending the direct authorization question; verified local commits remain available.

Task 3 has a verified Auth slice: reload and email/password refresh no longer write stale session claims over an admin change or restored checkpoint. The unchanged SDK checkpoint fixture is red/green. Separate review passes 33 affected browser cases, ten minimum-Node cases, 57 worker regressions, strict types and source form. Storage metadata round trip and corruption refusal remain open; the combined all-service probe is not yet passing.

The subsequent Storage slice now preserves complete object metadata through the existing persistence foundation. Its unchanged minimized regression and the original four-service/restart probe pass. Review passes 30 affected browser cases, five minimum-Node cases, 117 state/checkpoint/branch/Storage regressions, strict types, source form and unchanged client/live browser budgets. Legacy checkpoint records and default SharedWorker/in-page restore remain covered. Corruption refusal is next; task 3 and the milestone are still open.

## Previous repair: hosted app recovery after process restart

Manual QA reproduced a permanent closed connection in existing pages after restarting the host. A new S1/S2/S3 regression reproduced the reported error twice. Process identity now distinguishes fresh admission to a replacement host from resuming the same host; app configuration and existing Auth restoration precede observer restoration and readiness. A separate red/green case preserves invalid-grant refusal when process identity is missing or malformed.

Current scoped evidence is in `docs/hosted-restart-recovery.md`: 11 focused browser cases, 14 cross-runtime RTDB cases, four minimum-Node cases, 79 isolated regressions, strict CLI/fixture types, seven-file code form, and three browser entry budgets. The broad implementation JSON ledger remains a historical checkpoint and is not current proof for this repair. No full feature or release gate is closed.

The user-authorized restart fix is separate from continued feature expansion. Same-process retention expiry, anonymous UID reuse after explicit sign-in, misleading startup counts, and the previously recorded full-plan gaps remain open.

## Previous slice: checkpoint persistence and restore subscriptions

The preceding Auth slice was verified progress and its evidence was revalidated before implementation. Three S1/S2/S3 red/green cycles now persist hosted checkpoints in the existing project directory backend, restore app and explicitly lensed subscriptions without resurrecting unsubscribed listeners, and await durable restore before acknowledgment. SharedWorker keeps its IndexedDB checkpoint backend. The shared intent registry and original page provenance are reused; review preserves guard semantics and names changed teardown decisions.

The final focused run passes 37 browser scenarios in 1.0 minute, including SharedWorker checkpoint restore, Auth, remote consumers, reconnect and RTDB replacement. Existing checkpoint/subscription/Auth/cleanup suites pass 164 tests in ten isolated processes. Strict CLI/fixture types and 217-file code form pass. Collection finds 454 scenarios in 57 files; this is not a full-suite execution. Minimum-Node and standalone were not rerun for this candidate. Reports use `/tmp/pyric-checkpoint-final-`; all three TDD pairs are recorded separately.

The first storage-only attempt exposed a stale browser listener even though a direct SDK read showed restored data. That failure and diagnostic are preserved. The admin-listener fixture initially expected decoded data from the raw relay; its corrected encoded-shape run supplies the valid red. None of these setup/diagnostic failures is claimed as green evidence.

After the user raised the 27-hour duration, new implementation expansion stopped while preparing the verified state for manual QA and dependency-based PR review. The full goal remains incomplete. Checkpoint corruption validation, complete service metadata/restore fidelity, import/reset listeners, atomic persistence, resource bounds and live-mode/release work remain open. No goal completion or release readiness is claimed.

## Previous slice: Auth durability and mutation admission

After the question-only turn, the prior candidate was independently revalidated: 220 source files, current artifacts/contracts, 213 code-form inputs, 19 report hashes and two TDD pairs. Implementation resumed at approved S1/S2/S3. An account creation already survives immediate host termination; the new characterization checks the restored UID, selected tenant session and downstream rules authorization.

Three red/green cycles exposed twelve Auth operations that still changed accounts after persistence became unhealthy. The existing Node admission classifier now refuses account creation, profile/email/password changes, deletion, anonymous/provider sign-in, identity acceptance and admin user CRUD/clear before dispatch. SDK checks reauthenticate the unchanged account; remote checks compare the public user list before and after refusal. Each cycle retains its fixture digest between red and green. Review adds no resource owner, abstraction or browser-side mode branch.

Verification: 26 focused browser scenarios pass in 43.8 seconds, including default SharedWorker and in-page identity; 13 Auth scenarios pass on minimum Node in 19.8 seconds; 47 existing worker regressions pass in three isolated processes. Strict CLI/fixture types, 214-file code form and scenario collection pass. The collection contains 450 tests in 56 files and is not a full-suite execution. CLI artifacts were rebuilt; unchanged Pyric/site artifacts and browser budgets are explicitly reused. Standalone and whole-suite results remain historical.

The account restart proof does not establish tenant account-pool isolation: existing detached sessions choose tenants over a shared pool. Complete Auth metadata/checkpoint restoration, provider settings, session bookkeeping, remaining RTDB/control admission, coherent persistence, lifecycle, live execution and release gates remain open.

## Previous slice: in-process MCP and service-command ownership

The preceding CLI/Vite candidate was independently revalidated against 218 source files, all artifact identities, 211 code-form inputs, 23 reports and three TDD pairs. Two new S1/S3 cycles establish the remaining CLI entry guards: missing MCP discovery previously connected to a second persisted sandbox while the Node host still owned the project, and a one-shot service command acknowledged a competing write while in-process MCP owned it. In-process MCP now holds the existing canonical reservation across startup and shutdown; service commands hold it across load, method execution and save. Explicit selection still selects in-process execution and cannot override another persistence owner.

Verification: 18 focused browser cases (42.1s), six existing MCP attachment/admission cases (14.7s), five minimum-Node cases (10.9s), four rebuilt copied-standalone cases (16.1s), 55 affected regressions in five isolated processes, strict CLI/public-fixture types and 213-file code form. Characterizations cover graceful MCP persistence/reopening, failed startup, relative/symlinked project selection, a separate project, and standalone in-process MCP ownership. Collection confirms 437 hosted scenarios in 55 files; collection is not full execution. Reports use `/tmp/pyric-in-process-owner-final-`.

This turn makes implementation progress. The in-process runtime, snapshot and Storage sidecar remain distinct from hosted state; this establishes exclusion, not adoption or migration. Reusing the hosted runtime/dispatcher, complete writer inventory, all-service persistence/admission/recovery, full lifecycle/resource accounting, live behavior and the release matrix remain required. No full subsection or feature gate is complete.

## Full objective remains open

- [ ] F0: all applicable universal, TDD, section, and release gates in docs/hosted-sandbox-live-mode-gates.md have current evidence
  EVIDENCE: pending; includes early real SDK proof, wire validation, ordering/reconnect, authority/persistence, browser sockets, bounded observations/Studio, browser live execution, hosted credentials, and packaging/runtime verification.

- [ ] F1: the final implementation satisfies every requirement in the sequence and premortem without weakening the supported feature set
  EVIDENCE: pending; requires a final requirement-by-requirement audit against the actual artifacts.

## Evidence log

- 2026-09-12: execution started at c3555ac5. Only the three planning documents were untracked; no implementation edits existed. Beginning dependency installation and baseline verification.
- 2026-09-12: installed locked dependencies and built all packages. Combined baseline exposed a test-realm collision; all 72 baseline files passed in isolated processes. H0 then failed at its intended cross-browser assertion; H1 passed unchanged after the minimal hosted implementation. Full gates remain open.
- 2026-09-12: review-stage verification retained the browser behavior and passed 503 worker regressions. Added an S6 source-form checker through four red/green cycles. The first slice's remaining startup, lifecycle, and structural checks are still required; no section or release gate is marked complete.
- 2026-09-12: HTTP init failure now rejects hosted startup without opening a local worker (red/green observed). The served live read passes against an owner-protected emulator document and records one observation; the Vite entry proof is in progress. Existing evidence hashes are historical after these edits. Full gates remain open.
- 2026-09-12: both live entry proofs passed after review; both hosted browser scenarios remained green. The broad Vite regression run's skipped Studio/bridge scenarios were executed after building site assets and enabling the integration suite. Evidence is recorded as a new snapshot; universal and full-objective gates remain open.
- 2026-09-12: added and verified the hosted init deadline, delayed-success characterization, conflicting-host refusal, bridge project identity, and whole foreign-init refusal. Added S6 parser/scope regressions and a runnable source-form report. Reviewed hosted/live scenarios and all affected regression runs pass; no section or release gate is complete.
- 2026-09-12: reviewed peer routing, bridge dispatch, mount/session construction, and sandbox-session conditions. Source-form findings fell from 182 to 80. A new S3 diagnostics test failed on the browser-only banner; hosted startup now names Node execution and memory-only storage. All eight hosted scenarios and the final combined regressions pass. The full goal remains open.
- 2026-09-12: reviewed CLI configuration, diagnostics, watchers, and shutdown without changing the supported feature set. Source-form findings fell from 80 to 10. Added an isolated-test CLI at S6 with three red/green cycles and two characterization cases; replaced the failing combined baseline command and executed its exact replacement successfully. Full implementation and release gates remain open.
- 2026-09-12: added shared state-file decoding and CLI refusal of malformed auth arrays and unsupported fixture versions. The SDK identity proof exposed a missing getIdTokenResult export; the existing user-method delegation now serves all three sandbox runtime modes. Worker review preserved replacement listeners and removed nullable assertions. The scoped code-form command now reports zero findings. Full implementation and release gates remain open.
- 2026-09-12: resumed after a prompt-only turn. Added S6 command characterization and required CI enforcement with red/green evidence; conventions now state the named-condition rule for changed TypeScript. Added a real socket fault test at S1/S2: a committed increment with a lost acknowledgment previously left the SDK promise pending. Connection failure now rejects outstanding requests through the existing correlation owner, and app deletion reuses that cleanup. The unchanged browser assertion and affected regressions pass. Post-close calls, listener recovery, reconnect, and full resource accounting remain unfinished.
- 2026-09-12: added S1/S2 red/green evidence for structured post-close request errors and safe repeated unsubscribe after losing the host. Failed posting now removes its pending request entry. Added S6 red/green evidence for semantic boolean decisions and build-before-check CI ordering; imported, nullable, unknown, implicit-any, and narrowed/constrained cases characterize the implemented behavior. Current U3 evidence passes. Reconnect, subscription recovery, interrupted boot, and the remaining implementation/release gates stay open.
- 2026-09-12: resumed after a prompt-only turn. Completed browser-boundary tool verification and required CI wiring; strict typechecking exposed and fixed widened issue literals. Added S1/S2 red/green evidence for malformed JSON replies: the request previously hung and now rejects without harming a second browser. The reviewed suites and four bundle checks pass. Full frame validation, host boot/disposal, support policies, and the remaining implementation/release contract stay open.
- 2026-09-12: recorded the missing support/lifetime policy inputs and six-configuration scenario inventory. Added six S6 red/green cycles for declaration validation; the reviewed 71-test tool suite, strict tool types, and 63-file code-form check pass. No runtime source changed in this continuation; prior product/artifact identities are checked and their browser/baseline results remain historical. The requested additional mount-lifecycle seam is pending; independent work proceeds under the existing approval. Full 0B and implementation/release gates remain open.
- 2026-09-12: worked at the existing S1/S2 seam while the additional mount-API seam remained pending. SDK deletion during attach previously waited for an acknowledgment despite having sent no SDK work. It now cancels locally; the test observes socket close and reads the absent queued write through a replacement app. Added three S6 red/green cycles for ownership metadata, scenario metadata, and required CI enforcement. The 18-hosted, five-worker-browser, 539-baseline, and 74-tooling checks pass, with strict types, code form, and bundle budgets. Full support, lifecycle, and release gates remain open.
- 2026-09-12: resumed after a prompt-only turn. Two S1 red/green cycles now preserve stored reference values and reference equality queries. Each also passes through SharedWorker. Review preserved the existing unary persistence decoder and named conditions. Final verification passes 22 hosted-fixture, five worker/Studio, 539 baseline, 21 value, and 74 tooling cases, strict types, code form, and browser budgets. This is partial 2A evidence; the full goal remains open.
- 2026-09-12: seven further S1 red/green cycles covered addDoc reference creation and input, updateDoc, batch set/update, and transaction set/update. Review retained five-attempt conflict retries and named all transaction decisions, recomputing retry availability after the awaited conflict; focused and final regressions pass. SharedWorker characterization brings the fixture suite to 36 passing scenarios. Five worker/Studio, 539 baseline, 21 value, and 74 tooling tests also pass; code form checks 76 files with zero findings. Full 2A and implementation/release gates remain open.

- 2026-09-12: resumed implementation after a prompt-only turn. Two timestamp and four reference-cursor S1 red/green cycles now preserve SDK Timestamp identity/precision and prevent cursor operands from cloning their MessagePort. Review reuses the existing Timestamp class and one encoder; persistence keeps rules wrappers. Final checks pass 47 hosted-fixture, five Studio/SharedWorker, 539 baseline, 56 value, and 74 tooling tests, with strict builds/typechecks, zero code-form issues in 81 files, and all browser budgets. Full 2A and implementation/release gates remain open.

- 2026-09-12: two S1 byte-value cycles now expose Bytes through the served SDK, reconstruct its public class on reads, and encode it before SharedWorker transport. Eight further padding/constructor cases are characterization. The reviewed class retains JSON validation order; strict types and 101 focused regressions pass. Rebuilt Pyric, CLI and Studio before final verification: 57 hosted-fixture, five Studio/SharedWorker, 539 baseline, 153 value and 74 tooling tests pass; 85 TypeScript files have zero code-form findings and all browser budgets pass. Full 2A/2D and implementation/release gates remain open.

- 2026-09-13: four S1 GeoPoint/vector cycles now expose and reconstruct the existing SDK classes and encode them before SharedWorker cloning. Separate review preserves validation and property-access timing; each mechanical move and review passes 101 focused tests. Twelve additional boundary/constructor cases began green. Rebuilt Pyric, CLI and Studio before final checks: 73 hosted-fixture, five Studio/SharedWorker, 539 baseline, 153 value and 74 tooling tests pass; 89 TypeScript files have zero code-form findings and all browser budgets pass. Full 2A/2D and implementation/release gates remain open.

- 2026-09-13: three S1/S2 cycles now preserve marker-shaped user maps, retain unversioned consumer semantics through an external encoding declaration, and reject unsupported encoding before mutation. Eleven further map cases began green. Review retained snapshot access timing and named transaction decisions; the original modular batch/transaction adapter assertions remain pending an API/type correction. Rebuilt packages and Studio before final checks: 87 hosted-fixture, five Studio/SharedWorker, 539 baseline, 233 value/persistence/remote and 74 tooling tests pass; 97 TypeScript files have zero code-form findings and all browser budgets pass. Full wire, compatibility, persistence, implementation and release gates remain open.

- 2026-09-13: resumed after the requested goal-prompt turn. Three S1 red/green cycles fixed batch replacement retaining obsolete fields, replacement rules receiving merged data, and getAfter merging old fields back into sibling projections. Shared batch/transaction execution retains set intent; nine additional transport/transaction cases are characterization. Review names decisions and gives event/error records their existing concrete types. Final verification passes 99 hosted, five Studio/SharedWorker, 539 baseline, 233 value/persistence/remote, 174 atomic-engine and 74 tooling tests. Strict types, 103-file code form and all browser budgets pass. Full 2B and implementation/release gates remain open.

- 2026-09-13: three S1 red/green cycles now preserve nested fields with batch merge:true, honour selected mergeFields, and reject missing mask fields without mutation. Both transports pass the six-case fixture, including merged request rules and sibling getAfter. The shared field-merge engine and shared FirebaseError are reused. One existing Admin test was corrected from silent skipping to invalid-argument plus unchanged data, matching the installed Firebase 12.13.0 source; no scenario was dropped. Final verification passes 105 hosted, five Studio/SharedWorker, 539 baseline, 233 value/persistence/remote, 174 atomic-engine and 74 tooling tests. Strict types, 106-file code form and all browser budgets pass. Full 2B and implementation/release gates remain open.

- 2026-09-13: resumed after the requested goal-prompt turn. Revalidated the prior transaction merge and simple composition cycles, then completed an S1 red/green cycle for transaction mergeFields. Separate review names transaction decisions, preserves callback/retry behavior, corrects misleading option documentation, and verifies selected-mask request rules/getAfter and missing-field refusal across both transports. Final verification passes 113 hosted-fixture, five Studio/SharedWorker, 539 baseline, 233 value/persistence/remote, 174 atomic-engine and 74 tooling cases, with strict types, 111-file code form and all browser budgets. General queued-write composition, mask/transform validation, public types, event/capture propagation and the full implementation/release contract remain open.

- 2026-09-13: completed three S1 cycles for ordered masked writes, final-document rule evaluation, and nested-update projection. Shared preparation now resolves the original queue in order; transaction result summaries preserve their existing per-path shape. Reused the shared update engine for nested/dotted projections. An independent actual-SDK/local-emulator run confirmed final request rules and repeated transforms; default Java setup failure was resolved with the existing Java 21 runtime. Review passes 46 atomic browser and 174 engine cases. Final verification passes 133 hosted-fixture, five Studio/SharedWorker, 539 baseline, 233 value/persistence/remote and 74 tooling cases, strict types, 111-file code form and all browser budgets. Sequential preconditions, deletion sequences, queued-input lifetime, public types and event/capture/replay intent remain open alongside the full implementation/release contract.

- 2026-09-13: resumed implementation after the prompt-only turn. Verified sequential storage preconditions, final-state rule selection and shared deletion-sequence validation; retained aborted transaction diagnostics. Expanded the atomic fixture to both APIs/transports. Fast-forwarded the disjoint origin/main change to e207262f with all 133 local files preserved byte-for-byte. Rebuilt and verified the integrated candidate; full implementation and release gates remain open.

- 2026-09-13: four S1 red/green cycles correct queued field masks and in-page nested set values. Ten reviewed cases span both APIs and applicable paths. A mechanical extraction reuses the existing document copier, with 310 regressions before/after. Integrated browser, regression, type and bundle checks pass; evidence is bound to the current candidate. No full implementation/release gate is closed.

- 2026-09-13: five S1 red/green cycles preserve in-page masks, nested batch updates and queued Date values. Separate review verifies 24 input scenarios across both APIs and three execution paths. The actual SDK/local-emulator oracle confirms valid Date capture and distinguishes Invalid Date from an out-of-range Date; validation parity remains open. Rebuilt Pyric, CLI and embedded Studio, then passed 181 hosted-fixture, five Studio/SharedWorker, 539 baseline, 233 value, 310 engine, 74 tooling and 102 runtime-chip/asset cases. Strict types, 118-file code form and all browser budgets pass. Full implementation and release gates remain open.

- 2026-09-13: nine S1 red/green cycles establish Timestamp ranges/error precedence, Date refusal before queueing, microsecond storage/rules/query precision and default query ordering. Independent SDK/local-emulator references corrected two earlier fixture expectations and four legacy ordering cases. Separate review passes 54 focused browser cases, 226 full hosted-fixture cases, five Studio/SharedWorker cases, 539 baseline, 233 value, 310 engine, 29 query, 74 tooling and 102 runtime regressions. Strict types, 123-file code form and all browser budgets pass. Full implementation/release gates remain open.

- 2026-09-13: revalidated and recorded the completed Timestamp slice, then completed an S1 red/green cycle for in-page transaction snapshots. Normal snapshot.exists() calls previously threw a TypeError. The modular boundary now reuses existing tagging/value normalization after the transaction read. Six reviewed browser cases and 172 SDK regressions preserve document absence, retry and identity behavior; the full 232-case browser suite, five rebuilt Studio cases, 539 baseline, strict types and 125-file code form pass. No full implementation/release gate is closed. Public transaction/batch typing and converters remain required.

- 2026-09-13: resumed after the requested prompt-only turn. Revalidated three converter cycles and completed two reference-value cycles through normal SDK imports. Review corrected backing-reference query registration and reused the document copier for transaction snapshots. Fresh origin/main still matches HEAD e207262f. Final checks pass 243 hosted-fixture, five rebuilt Studio/SharedWorker, 539 baseline, 187 SDK, 233 value and 310 engine scenarios, strict types, 133-file code form and all browser budgets. Full implementation and release gates remain open.

- 2026-09-13: six S1 red/green cycles preserve factory converter removal/reapplication and converted setDoc/batch/transaction writes. Separate review removes intermediate reference shells and shares conversion within each adapter, retaining capture order and existing batch commit options. Positive public model fixtures compile without assertions or index signatures. Final verification passes 255 hosted-fixture, five rebuilt Studio/SharedWorker, 539 baseline, 187 SDK, 233 value and 310 engine scenarios, strict types, 136-file code form and all browser budgets. Full implementation and release gates remain open.

- 2026-09-13: revalidated the pending in-page input cycle and completed three further S1 cycles for hosted queue-time refusal and invalid converter output. The reviewed 30-case input matrix agrees with ten real-SDK/local-emulator outcomes. A separate mechanical commit extracts the shared predicate; 310 engine tests pass before and after. Final checks pass 285 browser, five rebuilt Studio, 539 baseline, 187 SDK, 233 value and 310 engine scenarios, strict types, 140-file code form and all browser budgets. Source evidence includes committed and uncommitted changes from e207262f. Full implementation and release gates remain open.

- 2026-09-13: three S1/S2 cycles reject non-string setDoc, batch and transaction wire paths. Expanded review exposed a duplicate unchecked transaction loop; both atomic APIs now share a typed application helper. Six real-transport cases verify refusal and continued shared-state access. Final checks pass 291 browser, five rebuilt Studio, 539 baseline, 187 SDK, 233 value and 310 engine cases, strict types, 142-file code form and all browser budgets. Fresh origin/main remains e207262f and is contained by the current branch. Full inbound validation and implementation/release gates remain open.

- 2026-09-13: four S1/S2 cycles reject unknown atomic methods, null descriptors, non-array write lists and non-array transaction read sets. Red evidence exposed partial commits, bypassed read validation and generic unknown errors. The reviewed fourteen-case matrix verifies invalid-argument, no mutation and continued shared-state access through both transports. Review shares the outer array assertion and removes the batch commit cast. Final checks pass 305 browser, five rebuilt Studio, 539 baseline, 187 SDK, 233 value and 310 engine cases, strict types, 143-file code form and all browser budgets. Full inbound validation and implementation/release gates remain open.

- 2026-09-13: two further S1/S2 cycles reject null read entries and array-valued transaction read paths. The latter red committed both writes. Both unchanged assertions and the expanded 24-case atomic/path review pass. Final checks pass 309 browser, five Studio and 539 baseline cases, strict CLI/public fixture types, 143-file code form and all browser budgets. The CLI was rebuilt; unchanged Pyric/Studio artifacts are explicitly reused. Earlier SDK/value/engine results remain historical. Full inbound validation and implementation/release gates remain open.

- 2026-09-13: four S1/S2 cycles reject missing read data/JSON, malformed JSON and scalar document roots before conflict comparison. All reds returned aborted; unchanged assertions now require invalid-argument. Review shares declarative test payloads across transports, and the 34-case atomic/path selection passes. Final checks pass 319 browser, five Studio and 539 baseline cases, strict types, 143-file code form and all browser budgets. The CLI was rebuilt; identical Pyric/Studio artifacts are explicitly reused. Full nested-value validation, lifecycle, persistence, live execution and release gates remain open.

- 2026-09-13: resumed after the prompt-only turn. Reviewed the pending identity-protected listener reconnect cycle and completed an RTDB socket-loss cycle. Seven focused cases verify restoration, lost acknowledgments, deletion and one-shot disconnect intent. Fresh origin/main remains e207262f and is contained by this branch. Final checks pass 321 browser, five rebuilt Studio, 539 baseline and 16 additional bridge cases, strict types, 146-file code form and all browser budgets. Full lifecycle, persistence, live execution and release gates remain open.

- 2026-09-13: three S1 cycles now report RTDB connectivity changes for socket loss and explicit controls, and preserve deleted-app refusal. Separate review covers seven connectivity cases and six existing lifecycle cases. Final checks pass 328 browser, five rebuilt Studio and 539 baseline tests, strict types, 149-file code form and all browser budgets. Metadata reads/parent snapshots, in-page parity, full host offline semantics and the remaining implementation/release gates stay open.

- 2026-09-13: resumed after the prompt-only turn and completed the pending in-page connectivity cycle, alongside the parent metadata cycle. Separate review verifies fourteen connectivity cases across three runtimes and six other lifecycle cases. Two actual SDK offline probes distinguish get from onValue/onlyOnce and establish a filtered metadata expectation. Rebuilt Pyric, CLI and Studio; final checks pass 335 browser, five Studio, 539 baseline and 508 RTDB cases, strict types, 151-file code form and all browser budgets. Fresh origin/main remains e207262f and is contained by this branch. Filtered metadata queries are the next concrete gap; full implementation and release gates remain open.

- 2026-09-13: four S1 cycles fix filtered parent metadata and unchanged selections across worker and in-page adapters. Two separate mechanical commits extract the existing projection and place its types with that owner; existing RTDB regressions pass and the type move emits identical JavaScript. Review covers twelve query cases across three runtimes. Added a dedicated RTDB listener bundle budget and CI check without changing engine exclusions. Final checks pass 347 browser, five Studio, 539 baseline, 508 RTDB and 13 boundary-tool cases, strict types, 155-file code form and all five browser budgets. Current evidence binds to HEAD feb8992c and the rebuilt artifacts. Full lifecycle, persistence, live execution and release gates remain open.

- 2026-09-13: merged origin/main 548e50a7 at a99a06a5; Git reapplied tracked work and every pre-merge untracked file retained its digest. Four state-replacement TDD cycles and the five-case review expose and fix stale disconnect intent, unrecognized imports, and lost reset rules. Final verification passes 352 hosted-fixture cases, five Studio cases, the isolated upstream Flow browser case, 539 worker/bridge tests, 508 RTDB tests, 17 upstream Flow regressions, strict types, the 160-file code-form check and all five bundle budgets. Evidence binds to the merged revision and rebuilt artifacts; full integration and release gates remain open.

- 2026-09-13: nine import/relay TDD cycles now have matched red/green snapshots. Import preflight rejects the reviewed corrupt shapes before replacement; SharedWorker returns validation failures instead of timing out. Hosted remote operations and subscriptions reuse the existing queue, with independent auth and explicit event refusal. Final checks pass 366 browser, five rebuilt Studio and 587 isolated regression cases, strict types, the 164-file code-form check and all five browser budgets. Evidence records the corrected remote-auth and write-payload fixture assumptions separately. Full integration and release gates remain open.

- 2026-09-13: resumed after the requested prompt-only turn. Three MCP/CLI TDD cycles now have matched red/green snapshots. MCP and service CLI calls use the browser host sandbox; CLI lens selection survives subsequent commands. A copied-pointer test exposed a successful wrong-project write and now verifies refusal before mutation. Review includes seeding before browsers, shared reads, rules refusal, subdirectories and the default SharedWorker refusal. Final checks pass 374 browser, five Studio and 629 isolated regression cases, strict types, 169-file code form and all five bundle budgets. The CLI is rebuilt; unchanged Pyric/Studio artifacts are explicitly verified and reused. Fresh origin/main remains 548e50a7 and is contained by HEAD. Identity/control parity, authoritative persistence, full lifecycle, observations, live execution and release gates remain open.

- 2026-09-13: the preceding prompt-only turn made no implementation progress. Revalidated the pending ownership work and completed the Bun compatibility and simultaneous-start cycles. The initial native addon aborted Bun; replacing it with built-in SQLite restored the package manifest and lockfile without adding dependencies. BEGIN EXCLUSIVE then let both simultaneous starters refuse; BEGIN IMMEDIATE passes the unchanged assertion and twenty explicit repetitions. Three ownership red/green pairs are preserved, including the corrected historical exit-code fixture. Review passes nine CLI ownership scenarios on Node 22.18.0 and 22.15.0, Bun contention/recovery, canonical aliases and failed startup, plus two copied-standalone cases. Final verification passes 383 browser, five Studio, 629 baseline and 44 additional serve regression cases, strict types, 174-file code form and all five browser budgets. Fresh origin/main 548e50a7 remains contained by HEAD a99a06a5. This is partial 4A evidence; other writer entry points, authoritative application persistence, full lifecycle, observations, live execution and release gates remain open.

- 2026-09-13: resumed after a prompt-only turn. Revalidated two stdio MCP red/green pairs, corrected misleading discovery comments and verified packaged stdio behavior. Final checks pass 389 hosted-fixture and five Studio/SharedWorker scenarios, 656 regressions, six minimum-Node stdio cases and three copied-standalone cases. Strict types, 178-file code form and all browser budgets pass. Current source/artifact evidence is refreshed; full admission, persistence, lifetime, live execution and release gates remain open. The next MCP failure slice must prove that a lost reply reports uncertainty without inviting a blind retry after a committed mutation.

- 2026-09-13: completed two S1/S2/S3 red/green cycles for stdio lost acknowledgments and request deadlines after non-idempotent adds. Both errors now state uncertainty and possible commitment without encouraging blind retries. Review shares the wording and preserves relay behavior. Eight stdio cases, three rebuilt standalone cases, 27 selector/address regressions, strict CLI/fixture types and 178-file code form pass. Earlier broad checks remain historical; current evidence is bound to the two-file change scope. Full admission, resource, persistence, live execution and release gates remain open.

- 2026-09-13: nine persistence TDD cycles now cover acknowledged SDK/MCP/CLI writes across process termination, corrupt/unsupported-state refusal, hosted file diagnostics, explicit SDK/MCP committed-state errors, and refusal of subsequent Firestore SDK mutations while reads remain available. Separate review reuses the existing state store/codec, moves preflight before allocation, preserves SharedWorker policy and removes a touched suppression. Final checks pass 399 browser, five Studio, nine minimum-Node, three copied-standalone, 629 worker/bridge/codec and 66 state/session cases, strict types, 181-file code form and five browser budgets. Full mutation admission/health/recovery, all-service durability, writer ownership, lifecycle and release gates remain open.

- 2026-09-13: six service CLI TDD cycles now distinguish committed persistence failure, refused later mutations, available reads with explicit memory health, preserved rules refusals, and memory-only identity switches that continue to govern reads. The runtime reuses declared method effects and the canonical identity operation. Separate review clarified durable-state naming and result assembly. Current checks pass 32 affected integration cases, six minimum-Node cases, three copied-standalone cases, 80 regressions, strict types and 182-file code form. All 405 scenarios are collected; the preceding full-suite runs remain historical. MCP policy, full service/control durability, partial failures, recovery and full implementation/release gates remain open.

- 2026-09-13: resumed after the prompt-only turn, which made no implementation progress. Recovered pending MCP verification; missing terminal handles required fresh checks. Chromium sandbox launch failures were retained separately and the same commands passed with required permissions. Four MCP red/green pairs and a partial-import characterization now have current review evidence. Tool-family effects retain advertised contracts; reads remain available and declared later mutations refuse. Current checks pass 37 integration, five minimum-Node, five Studio, three copied-standalone and 175 regression cases, strict types, 195-file code form and five browser budgets. The complete inventory collects 410. CLI partial failures, full classification/admission/recovery/durability and implementation/release gates remain open.

- 2026-09-13: independently verified the MCP candidate, then completed a CLI partial-batch red/green cycle. A first write could succeed before a later operation failed; the failed result previously skipped the host flush. Both operator paths now share result flushing, preserving the original error alongside any persistence warning. Separate review verifies partial state after SIGKILL/restart and refusal of later mutations after disk failure. The stale-build green attempt and npm-cache build failure are retained separately; only rebuilt-artifact runs count. Current checks pass 40 affected integration, nine minimum-Node, five Studio, three copied-standalone and 175 regression cases, strict types and 195-file code form. All 413 scenarios are collected. Browser contributors and unchanged package artifacts are verified for reuse. Complete classification, remaining admission/durability/recovery, lifecycle, observations, live execution and release gates remain open.

- 2026-09-13: recovered the completed 48-case browser terminal. Missing minimum-Node/Studio handles were confirmed before rerunning; both pass. The broad regression command initially could not bind loopback servers, then all 629 cases passed with loopback access. Seven SDK persistence red/green pairs, a SharedWorker characterization and rebuilt artifact checks are recorded. The Storage restart failure prompted an actual persistence foundation; it was not relabeled as a passing characterization. Concurrent snapshots, reset generations, multi-service atomicity, all buckets, remaining SDK admission and complete release gates stay open.

- 2026-09-13: independently verified the seven SDK persistence pairs, then reproduced overlapping-upload data loss at S1/S3. A CLI preload delays platform Blob binary I/O without replacing internal host collaborators; SDK reads after both acknowledgments and restart returned the older value. The unchanged assertion passes after complete explicit flushes are queued. Separate review makes the fault-release signal safe during cleanup. Current checks pass 49 integration, nine minimum-Node, three copied-standalone and 629 regression cases, strict types and 202-file code form. All 422 scenarios collect. The prior artifact/regression evidence is reused only for unchanged inputs. Reset generations, coherent and atomic all-service state, remaining admission, lifecycle/live and release gates remain open.

- 2026-09-13: the previous goal turn made verified progress. Revalidated its current evidence before a separate fixture extraction and two reset TDD cycles. Reset acknowledged before an old Storage save finished, allowing a cleared object to return after restart; it now awaits authoritative persistence. Unhealthy persistence also allowed reset to clear memory; admission now refuses first. Review preserves default SharedWorker reset/reload/upload behavior and removes a redundant cast. Current checks pass 67 integration/state-replacement, 12 minimum-Node, five Studio, three copied-standalone and 629 regression cases, strict types and 204-file code form. All 425 scenarios collect. Complete generation ownership, multi-service atomicity, remaining controls/writers, lifecycle/live and release gates remain open.

- 2026-09-13: the previous goal turn made verified progress; its reset evidence was revalidated before this cycle. A valid legacy HTTP state replay replaced a newer acknowledged SDK value only on disk, so restart returned stale data. One current-capability S1/S2/S3 red/green cycle now protects the host's store; reads remain available and browser writes receive 423 before body collection. Review keeps default SharedWorker persistence handoff working and applies the existing named-condition policy to the changed routing factory. The full 427-case hosted suite, 14 minimum-Node, five Studio, three copied-standalone, 629 worker/bridge and 117 namespace/session regressions pass, with strict types and 206-file code form. Separate CLI/Vite writers, full lifecycle/persistence semantics, live execution and release gates remain open.

- 2026-09-13: three process/SDK TDD cycles extend canonical state ownership to CLI browser persistence and Vite, and bind Vite teardown to the originating server. A real Vite restart exposed premature replacement teardown; the unchanged public ownership/SDK assertions now pass. Focused verification and the current-input audit are recorded in the evidence ledger. The full 432-case inventory was collected, not rerun.

- 2026-09-13: independently revalidated the previous candidate, then completed two process/MCP TDD cycles for fallback and one-shot service ownership. Current evidence includes normal MCP attachment, selected-project isolation and the copied standalone in-process path. Shared runtime/storage migration and full feature gates remain open.

- 2026-09-14: hosted-hardening task 3 value fidelity is verified at S1/S2/S3. Retained red/green evidence covers Timestamp loss, literal-marker map restore/restart loss, malformed-value refusal before reset, and escaped field names in branch diff. SharedWorker/in-page parity, legacy checkpoints, mixed persistence buckets and actual changed-value promotion have public-boundary review evidence. Current source/emitted hashes retain the 42-browser, eight-minimum-Node and 107-regression results; three additional review cases and four final minimum-Node checks pass, as do strict types, fifteen-file code form and client/live browser boundaries. Details and input hashes live in docs/hosted-hardening-progress.md and ignored/hardening/checkpoint-values/verified-inputs.json. General atomicity/concurrent capture, arbitrary buckets, downgrade compatibility, applicable final packaging and the broader goal remain open. No push retry was attempted.

- 2026-09-14: hardening task 4 restoration diagnostics passed its S1/S3 red/green cycle. Readiness reported 0/0 while the SDK read two persisted documents; the shared summary now handles bucketed records and controller Auth metadata. Review covers human text/JSON, fresh and Auth-only state, legacy state/store regression behavior, seeds, resets and all runtime paths. Current evidence: 18 browser cases (35.6s), 34 regressions, two minimum-Node cases (6.7s), strict CLI/fixture types and five-file code form. Input hashes and reports are archived under ignored/hardening/restoration-diagnostics. Packaging remains at the final join; subsequent queue tasks and the broader goal remain open.

- 2026-09-14: hardening task 8 protocol admission and buffered-frame refusal passed three S1/S2/S3 red/green cycles. Unsupported consumer/peer handshakes refuse before admission or replacement; a closing socket cannot dispatch buffered attach/write frames. The peer fixture's unauthenticated follow-up was corrected and its red rerun before green. Review covers 24 malformed-version handshakes across worker-port, legacy and SharedWorker peers, plus buffered invalid-grant/JSON refusal. Final checks: 37 browser scenarios on Node 22.15.0 (1.4m), 31 isolated bridge/session regressions, strict CLI/fixture types and two-file code form, all passing. Evidence is in ignored/hardening/protocol-admission and docs/hosted-hardening-progress.md. Reply versions, remaining payloads/bounds, package compatibility and the broader goal remain open. No push retry was attempted.

- 2026-09-14: hardening task 8 legacy relay envelopes passed five S1/S2/S3 red/green cycles (operation/subscription IDs, operation/subscription bodies and unsubscribe IDs). Review consolidates envelope validation in a 75-line module and tests 40 malformed frames within five host lifecycles, with SDK no-mutation checks and a fresh public remote consumer. Current checks: 41 affected browser scenarios (1.6m), eight valid-relay/checkpoint scenarios (14.1s), both on Node 22.15.0; 31 isolated bridge/session regressions; strict CLI/fixture types; three-file code form, all passing. Evidence is in ignored/hardening/relay-frames and docs/hosted-hardening-progress.md. Other frame fields, detailed service payloads, reply versions, bounds and later queue items remain open. No push retry was attempted.

- 2026-09-14: hardening task 8 consumer-side acknowledgment versions passed browser and public-remote S1/S2/S3 red/green cycles. Incompatible host versions now reject before queued SDK writes or remote readiness. Review covers eight malformed versions per consumer type and a real remote-listener process that exits naturally after unsubscribe. After correcting constant-binding narrowing and code-form findings, current checks pass 31 browser scenarios on Node 22.15.0 (1.0m), 47 regressions in five isolated files, strict CLI/fixture types, six-file code form and browser socket/client budgets (7,366/16,384 and 54,943/98,304). The initial restricted-loopback regression failure is retained separately. Evidence is in ignored/hardening/host-protocol and docs/hosted-hardening-progress.md. Peer acknowledgment versions, remaining frame fields, service arguments, bounds and package compatibility remain open. No push retry was attempted.

- 2026-09-14: hardening task 8 peer acknowledgment versions pass a corrected S1/S2/S3 red/green cycle. An incompatible host disconnects the browser peer while SharedWorker SDK access remains usable and a healthy peer can take over. Earlier callback/close-code/proxy-observation mistakes are retained as setup failures. Review covers eight malformed versions, named decisions, URL policy extraction and valid legacy fixture handshakes. Final evidence: 24 browser cases on Node 22.15.0 (43.7s), 37 isolated regressions, strict CLI/browser/relay-fixture types, four-file source form and a browser peer build with no external imports. No new bundle budget is claimed. Details and hashes are in ignored/hardening/peer-protocol and docs/hosted-hardening-progress.md. Remaining frame/payload/bounds checks and later queue items stay open; no push retry was attempted.

- 2026-09-14: hardening task 8 hosted acknowledgment capability validation passes two S1/S2/S3 red/green cycles. Non-array capabilities stranded a queued SDK write; a mixed array still admitted it. The browser now requires a string list containing worker-port before readiness. Eleven capability cases share the existing version-refusal fixture. Final checks pass 40 combined browser cases on Node 22.15.0 (1.3m), eleven isolated connection/RTDB/session regressions, strict CLI/fixture types, two-file source form and existing socket/client browser budgets (7,425/16,384 and 54,943/98,304). Reports and hashes are in ignored/hardening/host-capabilities and docs/hosted-hardening-progress.md. Remaining frame/payload/bounds checks and later queue items remain open. No push retry was attempted.

- 2026-09-14: hardening task 8 immediate buffered peer commands are characterised at S1/S2/S3 without a production change. The valid-protocol control observes the wire-injected document through the SDK; the incompatible control verifies refusal and continued local SharedWorker writes. Ten peer browser cases pass on minimum Node (17.9s), with strict fixture types and one-file source form. This is Chromium buffered-wire evidence, not a claim about every delayed callback or browser engine. Current input/artifact identities retain applicable prior evidence. Details are in ignored/hardening/peer-refused-work and docs/hosted-hardening-progress.md.

- 2026-09-14: hardening task 8 remote identity-lens validation passes five S1/S2/S3 red/green cycles: containers, modes, impersonation identity, request IDs and target-session IDs. Shared Auth-state validation owns UID/tenant/token semantics; the existing bridge envelope boundary refuses before dispatch. Two runtime fixtures cover 62 malformed frames and supported lens controls. Final checks pass 19 browser cases on minimum Node (1.2m), 42 regressions in six isolated files, strict CLI/fixture types and two-file source form. No browser leaf or shared Auth implementation changed. Reports and input/artifact identities are in ignored/hardening/lens-frames and docs/hosted-hardening-progress.md. Other protocol/payload/bounds and later queue gates stay open; no push retry was attempted.


### Hardening item 8: mounted inbound message bounds

The serve/Vite mount enforces the existing 12 MiB encoded-message contract before dispatch, including fragmented messages. Original red/green evidence and reviewed hosted/SharedWorker boundary and 8 MiB Storage controls are recorded in `docs/hosted-hardening-progress.md`. Final checks pass 18 browser cases (36.7s, Node 22.15.0), 49 regressions in five isolated files, strict types, seven-file source form and current browser budgets. A separate Vite fixture repair runs the host on Node and fails on teardown errors; the original Bun close timeout is not a passing result. Standalone input, outbound bounds, depth/payload validation and later queue items remain open.


### Hardening item 8: standalone inbound message bounds

The standalone CLI now applies the same 12 MiB encoded-message limit as the serve/Vite mount. The unchanged public-wire red/green fixture retains a healthy MCP-to-peer-to-SDK write and clean CLI exit. Final review passes 14 runtime cases (25.2s, Node 22.15.0), 25 regressions in five isolated files, strict types and three-file source form. The touched server function's casts and conditions were reviewed without introducing a resource owner. Standalone peer admission/schema validation, outbound/depth limits and broader lifecycle gates remain open; the existing idle test's health assertion does not establish resource disposal.


### Hardening item 8: standalone protocol admission

Standalone peer handshakes use the same version policy as mounted handshakes and discard buffered work after refusal begins. Original public CLI/MCP/SDK red/green evidence and eight invalid-version controls are retained. Final checks pass 18 runtime cases (32.2s, Node 22.15.0), 36 regressions in four files, strict types and four-file source form. This shares validation only; standalone consumer capabilities and connection ownership are unchanged. Other peer fields, outer JSON/envelopes, detailed payloads and outbound/depth limits remain open.


### Hardening item 8: peer handshake field validation

Shared envelope validation now refuses malformed peer identity, tool lists and optional capability lists before standalone or mounted registration. Three original public red/green pairs and 42 malformed-handshake controls retain healthy MCP-to-SDK writes, reject buffered registration and preserve supported valid handshakes. Final verification passes 24 runtime cases (54.9s, Node 22.15.0), 39 regressions in five files, strict types and five-file source form. The old malformed-list coercion regression now expects explicit refusal. Standalone outer JSON/envelopes, other fields, detailed payloads, outbound/depth bounds and later queue gates remain open.


### Hardening item 8: shared outer parsing

Standalone malformed JSON and unrecognised envelopes now refuse before buffered work. Both connection handlers reuse one outer parser while retaining their lifetime policy. Two original public red/green pairs and eleven invalid-input controls preserve healthy MCP-to-SDK writes. Final checks pass 27 runtime cases (1.0m, Node 22.15.0), 39 regressions in five files, strict types and seven-file source form, including every caller of the extracted test fixture. Known-field/service validation, outbound frames, nesting bounds and later queue gates remain open.

- 2026-09-14: hardening task 8 write-depth validation passes an unchanged public-wire red/green regression. Shared write preparation refuses above 64 encoded containers before value/sentinel decoding. Hosted and default SharedWorker review covers maps, mixed arrays, escaped marker maps, exact boundaries and set/update/add/batch/transaction preservation. A Playwright deep-argument transfer timeout was corrected in the fixture only and is archived separately. Final checks pass 120 affected browser cases on Node 22.15.0 (3.0m), 16 regressions across five isolated files, strict production/fixture types, five-file source form and all five browser budgets. Evidence: ignored/hardening/document-depth/verified-inputs.json and docs/hosted-hardening-progress.md. Other decode paths, outbound bounds and later queue items remain open. No push retry was attempted.

- 2026-09-14: task 8 transaction read depth passes hosted/SharedWorker red-green cycles: over-deep serialized reads previously reported aborted; one existing depth-validator call now refuses them with invalid-argument before recursive decoding. Review proves one callback attempt for refusal and successful reads/commits at 63/64 encoded containers. Forty-five affected browser cases pass on Node 22.15.0 (1.2m), with 65 regressions in three isolated files, strict CLI/fixture types and five-file source form. Previous browser-boundary evidence is reused after checking unchanged contributors/configuration and Pyric artifact identity. Evidence: ignored/hardening/transaction-read-depth/verified-inputs.json. Query/reply/import depth, remaining method payloads, outbound bounds and later queue items remain open. No push retry was attempted.

- 2026-09-14: task 8 query operand depth passes public SDK filter/cursor red-green cycles. The shared host resolver refuses operands above 64 containers before decoding; review covers ordinary/escaped maps, mixed arrays, both boundary sides, all cursor directions and count/listener recovery. A missing served getCountFromServer binding was separately reproduced and fixed using the existing selected implementation (0536b8ad); counts and post-sign-out Rules denial pass in all three runtimes. Final checks pass 35 browser scenarios on Node 22.15.0 (50.5s), 63 regressions across four isolated files, strict CLI/fixture types and six-file source form. Five leaf boundary reports are reused after input/contributor verification. Evidence: ignored/hardening/query-value-depth/verified-inputs.json. Descriptor/composite validation, reply/import depth, remaining payloads, outbound bounds and later queue items remain open. No push retry was attempted.

- 2026-09-14: hardening task 8 mounted consumer output passes public SDK/wire read and snapshot red/green cycles. Oversized replies now return correlated resource-exhausted errors before sending; oversized snapshot observations omit only optional samples while preserving metadata. Review verifies exact UTF-8 boundaries and healthy clients after refusal. Final evidence: 19 affected browser scenarios on Node 22.15.0 (1.2m), 36 isolated regressions in four files, strict CLI/fixture types, five-file code form and whitespace checks, all passing. Five browser budget reports are reused after contributor/artifact verification. Evidence is in ignored/hardening/outbound-frame-limit and docs/hosted-hardening-progress.md. Other outbound paths, detailed validation, queue/history bounds and later tasks remain open. No push retry was attempted.

- 2026-09-14: task 8 unsupported Firestore target kinds pass a public-wire red/green cycle. An unknown kind previously executed as a query; the shared resolver now refuses explicitly. Review proves 112 refusals across read/count/aggregate/subscription paths and hosted/SharedWorker runtimes, followed by valid work. Final checks pass 30 browser scenarios (54.4s), 63 isolated regressions, strict CLI/fixture types and two-file code form. Five browser budgets are reused with verified unchanged contributors/artifacts. Evidence is in ignored/hardening/query-target-refusal and docs/hosted-hardening-progress.md. Complete target/constraint validation, composite bounds and later queue work remain open; no push retry was attempted.

- 2026-09-14: task 8 invalid query order directions pass a served SDK red/green cycle. Unsupported directions previously executed as ascending; the shared query builder now refuses with the existing invalid-argument error type. Review covers 81 refusals and 27 valid query/count/listener cases across hosted, SharedWorker and in-page. Final checks pass 33 browser scenarios (1.0m), 153 regressions in ten isolated files, Pyric/CLI/fixture types, two-file code form and five fresh browser budgets. Evidence is in ignored/hardening/query-direction-refusal and docs/hosted-hardening-progress.md. Other constraint validation and later queue work remain open; no push retry was attempted.
