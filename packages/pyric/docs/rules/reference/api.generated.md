<!-- Generated from the package export declaration via TypeDoc. Do not edit by hand; run bun run docs:api:generate. -->

# pyric/rules

## Classes

### RulesAssertionError

Thrown by `assertCase` / a runner case's `run()` when the simulated
decision did not match the case's expectation. The message is the
rendered trace from `explainCase`, so a test runner surfaces the "why"
without extra wiring.

#### Extends

- `Error`

#### Constructors

##### Constructor

> **new RulesAssertionError**(`message`): [`RulesAssertionError`](#rulesassertionerror)

###### Parameters

###### message

`string`

###### Returns

[`RulesAssertionError`](#rulesassertionerror)

###### Overrides

`Error.constructor`

#### Properties

##### cause?

> `optional` **cause**: `unknown`

The cause of the error.

###### Inherited from

`Error.cause`

##### message

> **message**: `string`

###### Inherited from

`Error.message`

##### name

> **name**: `string`

###### Inherited from

`Error.name`

##### stack?

> `optional` **stack**: `string`

###### Inherited from

`Error.stack`

##### stackTraceLimit

> `static` **stackTraceLimit**: `number`

The `Error.stackTraceLimit` property specifies the number of stack frames
collected by a stack trace (whether generated by `new Error().stack` or
`Error.captureStackTrace(obj)`).

The default value is `10` but may be set to any valid JavaScript number. Changes
will affect any stack trace captured _after_ the value has been changed.

If set to a non-number value, or set to a negative number, stack traces will
not capture any frames.

###### Inherited from

`Error.stackTraceLimit`

#### Methods

##### captureStackTrace()

###### Call Signature

> `static` **captureStackTrace**(`targetObject`, `constructorOpt?`): `void`

Creates a `.stack` property on `targetObject`, which when accessed returns
a string representing the location in the code at which
`Error.captureStackTrace()` was called.

```js
const myObject = {};
Error.captureStackTrace(myObject);
myObject.stack;  // Similar to `new Error().stack`
```

The first line of the trace will be prefixed with
`${myObject.name}: ${myObject.message}`.

The optional `constructorOpt` argument accepts a function. If given, all frames
above `constructorOpt`, including `constructorOpt`, will be omitted from the
generated stack trace.

The `constructorOpt` argument is useful for hiding implementation
details of error generation from the user. For instance:

```js
function a() {
  b();
}

function b() {
  c();
}

function c() {
  // Create an error without stack trace to avoid calculating the stack trace twice.
  const { stackTraceLimit } = Error;
  Error.stackTraceLimit = 0;
  const error = new Error();
  Error.stackTraceLimit = stackTraceLimit;

  // Capture the stack trace above function b
  Error.captureStackTrace(error, b); // Neither function c, nor b is included in the stack trace
  throw error;
}

a();
```

###### Parameters

###### targetObject

`object`

###### constructorOpt?

`Function`

###### Returns

`void`

###### Inherited from

`Error.captureStackTrace`

###### Call Signature

> `static` **captureStackTrace**(`targetObject`, `constructorOpt?`): `void`

Create .stack property on a target object

###### Parameters

###### targetObject

`object`

###### constructorOpt?

`Function`

###### Returns

`void`

###### Inherited from

`Error.captureStackTrace`

##### isError()

> `static` **isError**(`value`): `value is Error`

Check if a value is an instance of Error

###### Parameters

###### value

`unknown`

The value to check

###### Returns

`value is Error`

True if the value is an instance of Error, false otherwise

###### Inherited from

`Error.isError`

##### prepareStackTrace()

> `static` **prepareStackTrace**(`err`, `stackTraces`): `any`

###### Parameters

###### err

`Error`

###### stackTraces

`CallSite`[]

###### Returns

`any`

###### See

https://v8.dev/docs/stack-trace-api#customizing-stack-traces

###### Inherited from

`Error.prepareStackTrace`

***

### RulesCompileError

Thrown by `firestoreRules(source)` / `rtdbRules(...)` when the source
cannot compile. Carries the compile-blocking issues on `.issues` so a
caller can surface them without re-parsing.

#### Extends

- `Error`

#### Constructors

##### Constructor

> **new RulesCompileError**(`message`, `issues`): [`RulesCompileError`](#rulescompileerror)

###### Parameters

###### message

`string`

###### issues

[`RuleIssue`](#ruleissue)[]

###### Returns

[`RulesCompileError`](#rulescompileerror)

###### Overrides

`Error.constructor`

#### Properties

##### cause?

> `optional` **cause**: `unknown`

The cause of the error.

###### Inherited from

`Error.cause`

##### issues

> `readonly` **issues**: [`RuleIssue`](#ruleissue)[]

##### message

> **message**: `string`

###### Inherited from

`Error.message`

##### name

> **name**: `string`

###### Inherited from

`Error.name`

##### stack?

> `optional` **stack**: `string`

###### Inherited from

`Error.stack`

##### stackTraceLimit

> `static` **stackTraceLimit**: `number`

The `Error.stackTraceLimit` property specifies the number of stack frames
collected by a stack trace (whether generated by `new Error().stack` or
`Error.captureStackTrace(obj)`).

The default value is `10` but may be set to any valid JavaScript number. Changes
will affect any stack trace captured _after_ the value has been changed.

If set to a non-number value, or set to a negative number, stack traces will
not capture any frames.

###### Inherited from

`Error.stackTraceLimit`

#### Methods

##### captureStackTrace()

###### Call Signature

> `static` **captureStackTrace**(`targetObject`, `constructorOpt?`): `void`

Creates a `.stack` property on `targetObject`, which when accessed returns
a string representing the location in the code at which
`Error.captureStackTrace()` was called.

```js
const myObject = {};
Error.captureStackTrace(myObject);
myObject.stack;  // Similar to `new Error().stack`
```

The first line of the trace will be prefixed with
`${myObject.name}: ${myObject.message}`.

The optional `constructorOpt` argument accepts a function. If given, all frames
above `constructorOpt`, including `constructorOpt`, will be omitted from the
generated stack trace.

The `constructorOpt` argument is useful for hiding implementation
details of error generation from the user. For instance:

```js
function a() {
  b();
}

function b() {
  c();
}

function c() {
  // Create an error without stack trace to avoid calculating the stack trace twice.
  const { stackTraceLimit } = Error;
  Error.stackTraceLimit = 0;
  const error = new Error();
  Error.stackTraceLimit = stackTraceLimit;

  // Capture the stack trace above function b
  Error.captureStackTrace(error, b); // Neither function c, nor b is included in the stack trace
  throw error;
}

a();
```

###### Parameters

###### targetObject

`object`

###### constructorOpt?

`Function`

###### Returns

`void`

###### Inherited from

`Error.captureStackTrace`

###### Call Signature

> `static` **captureStackTrace**(`targetObject`, `constructorOpt?`): `void`

Create .stack property on a target object

###### Parameters

###### targetObject

`object`

###### constructorOpt?

`Function`

###### Returns

`void`

###### Inherited from

`Error.captureStackTrace`

##### isError()

> `static` **isError**(`value`): `value is Error`

Check if a value is an instance of Error

###### Parameters

###### value

`unknown`

The value to check

###### Returns

`value is Error`

True if the value is an instance of Error, false otherwise

###### Inherited from

`Error.isError`

##### prepareStackTrace()

> `static` **prepareStackTrace**(`err`, `stackTraces`): `any`

###### Parameters

###### err

`Error`

###### stackTraces

`CallSite`[]

###### Returns

`any`

###### See

https://v8.dev/docs/stack-trace-api#customizing-stack-traces

###### Inherited from

`Error.prepareStackTrace`

***

### RulesUnsupportedError

Thrown by `assertCase` / a runner case's `run()` when the simulator
abstained — it hit a feature it does not implement, so neither a pass nor
a genuine failure can be asserted. Distinct from
[RulesAssertionError](#rulesassertionerror) so a runner can choose to skip rather than
fail on a known simulator gap.

#### Extends

- `Error`

#### Constructors

##### Constructor

> **new RulesUnsupportedError**(`message`): [`RulesUnsupportedError`](#rulesunsupportederror)

###### Parameters

###### message

`string`

###### Returns

[`RulesUnsupportedError`](#rulesunsupportederror)

###### Overrides

`Error.constructor`

#### Properties

##### cause?

> `optional` **cause**: `unknown`

The cause of the error.

###### Inherited from

`Error.cause`

##### message

> **message**: `string`

###### Inherited from

`Error.message`

##### name

> **name**: `string`

###### Inherited from

`Error.name`

##### stack?

> `optional` **stack**: `string`

###### Inherited from

`Error.stack`

##### stackTraceLimit

> `static` **stackTraceLimit**: `number`

The `Error.stackTraceLimit` property specifies the number of stack frames
collected by a stack trace (whether generated by `new Error().stack` or
`Error.captureStackTrace(obj)`).

The default value is `10` but may be set to any valid JavaScript number. Changes
will affect any stack trace captured _after_ the value has been changed.

If set to a non-number value, or set to a negative number, stack traces will
not capture any frames.

###### Inherited from

`Error.stackTraceLimit`

#### Methods

##### captureStackTrace()

###### Call Signature

> `static` **captureStackTrace**(`targetObject`, `constructorOpt?`): `void`

Creates a `.stack` property on `targetObject`, which when accessed returns
a string representing the location in the code at which
`Error.captureStackTrace()` was called.

```js
const myObject = {};
Error.captureStackTrace(myObject);
myObject.stack;  // Similar to `new Error().stack`
```

The first line of the trace will be prefixed with
`${myObject.name}: ${myObject.message}`.

The optional `constructorOpt` argument accepts a function. If given, all frames
above `constructorOpt`, including `constructorOpt`, will be omitted from the
generated stack trace.

The `constructorOpt` argument is useful for hiding implementation
details of error generation from the user. For instance:

```js
function a() {
  b();
}

function b() {
  c();
}

function c() {
  // Create an error without stack trace to avoid calculating the stack trace twice.
  const { stackTraceLimit } = Error;
  Error.stackTraceLimit = 0;
  const error = new Error();
  Error.stackTraceLimit = stackTraceLimit;

  // Capture the stack trace above function b
  Error.captureStackTrace(error, b); // Neither function c, nor b is included in the stack trace
  throw error;
}

a();
```

###### Parameters

###### targetObject

`object`

###### constructorOpt?

`Function`

###### Returns

`void`

###### Inherited from

`Error.captureStackTrace`

###### Call Signature

> `static` **captureStackTrace**(`targetObject`, `constructorOpt?`): `void`

Create .stack property on a target object

###### Parameters

###### targetObject

`object`

###### constructorOpt?

`Function`

###### Returns

`void`

###### Inherited from

`Error.captureStackTrace`

##### isError()

> `static` **isError**(`value`): `value is Error`

Check if a value is an instance of Error

###### Parameters

###### value

`unknown`

The value to check

###### Returns

`value is Error`

True if the value is an instance of Error, false otherwise

###### Inherited from

`Error.isError`

##### prepareStackTrace()

> `static` **prepareStackTrace**(`err`, `stackTraces`): `any`

###### Parameters

###### err

`Error`

###### stackTraces

`CallSite`[]

###### Returns

`any`

###### See

https://v8.dev/docs/stack-trace-api#customizing-stack-traces

###### Inherited from

`Error.prepareStackTrace`

## Interfaces

### CaseResult

The outcome of running one Firestore case through `simulate`. Never a
 thrown error — a denied or abstained case is data, not an exception.

#### Properties

##### case

> **case**: [`FirestoreCase`](#firestorecase)

The case that produced this result.

##### decision

> **decision**: `"ALLOW"` \| `"DENY"` \| `"UNSUPPORTED"`

The engine's absolute verdict, independent of expectation.

##### description

> **description**: `string`

##### expectation

> **expectation**: `"ALLOW"` \| `"DENY"`

##### notes

> **notes**: `string`[]

Top-level diagnostic strings.

##### passed

> **passed**: `boolean`

`true` when `decision` matched `expectation`.

##### pathResolution?

> `optional` **pathResolution**: [`PathResolutionTrace`](#pathresolutiontrace)

Which match blocks the resolver considered and where each fell apart.

##### trace

> **trace**: [`RuleEvaluation`](#ruleevaluation)[]

Per-rule evaluation entries in source order.

##### unsupported

> **unsupported**: `boolean`

`true` when the simulator abstained on a feature it does not
 implement — neither a pass nor a genuine failure.

***

### EvaluatedRuleInfo

The DECIDING rule's source position + sub-expression trace, projected from a
TestResult — for BOTH verdicts: the `allow` rule that granted an
ALLOW, or the rule responsible for a DENY. Additive companion to
renderLegacyDebugMessages: that flattens the per-rule trace to
strings (dropping `line` and `expressionTrace`); this preserves the
structured detail a UI needs to point at the exact source line and step
through the evaluation ("show the work"). Position/trace fields are optional
so a partial trace projects honestly.

#### Properties

##### expression?

> `optional` **expression**: `string`

Pretty-printed condition text of the deciding rule.

##### expressionTrace?

> `optional` **expressionTrace**: [`ExprTraceEntry`](#exprtraceentry)[]

Per-sub-expression evaluation trace of the deciding rule.

##### line?

> `optional` **line**: `number`

1-indexed source line of the deciding `allow` rule.

##### verdict

> **verdict**: `"allow"` \| `"deny"`

The verdict the deciding rule produced for the op.

***

### Explanation

The structured account of why one Firestore case resolved as it did.

#### Properties

##### deciding?

> `optional` **deciding**: [`EvaluatedRuleInfo`](#evaluatedruleinfo)

The deciding `allow` rule (line, condition text, sub-expression
 trace), when one was evaluated. Absent on default-deny / abstain.

##### decision

> **decision**: `"ALLOW"` \| `"DENY"` \| `"UNSUPPORTED"`

##### expectation

> **expectation**: `"ALLOW"` \| `"DENY"`

##### notes

> **notes**: `string`[]

##### passed

> **passed**: `boolean`

##### pathResolution?

> `optional` **pathResolution**: [`PathResolutionTrace`](#pathresolutiontrace)

##### trace

> **trace**: [`RuleEvaluation`](#ruleevaluation)[]

##### unsupported

> **unsupported**: `boolean`

***

### ExprTraceEntry

One entry in the per-rule expression trace recorded by `TraceRecorder`.

Trace entries are emitted in evaluation order. The tree shape is
reconstructable via `parent`: root entries have `parent: null`, and
each child entry's `parent` is the array index of its evaluating
ancestor. This is denser than nested objects for transport (a tool
call returns a single flat array) and easier for the agent to scan
top-to-bottom.

`value` carries the evaluation result. For nodes that threw, see
`error`; for short-circuit skips, `skipped: true` is set instead.

#### Properties

##### error?

> `optional` **error**: `string`

Error message when the expression threw. The wrapper still re-throws;
 this captures the diagnostic for the caller.

##### inlinedFrom?

> `optional` **inlinedFrom**: `object`

Set on entries recorded *inside* a user-defined function's body
 (after parameter binding). Carries the enclosing function's name
 so the agent can filter the trace by frame —
 `entries.filter(e => e.inlinedFrom?.name === 'isOwner')` — without
 walking the parent chain to find the surrounding `functionCall`.

 Parameter expressions are *not* tagged: they evaluate in the
 caller's scope, before the frame is entered. The `functionCall`
 entry itself is recorded in the caller's frame too, so a
 top-level `isOwner()` shows `inlinedFrom: undefined` on the
 `functionCall`, but `'isOwner'` on its body children.

###### name

> **name**: `string`

##### kind

> **kind**: `"literal"` \| `"identifier"` \| `"memberAccess"` \| `"methodCall"` \| `"bracketAccess"` \| `"sliceAccess"` \| `"binaryOp"` \| `"unaryOp"` \| `"ternary"` \| `"inExpr"` \| `"isExpr"` \| `"listLiteral"` \| `"mapLiteral"` \| `"pathLiteral"` \| `"functionCall"`

AST node type — useful for filtering by structural kind.

##### letBinding?

> `optional` **letBinding**: `object`

Set on entries that record a `let` binding evaluation inside a
 user-defined function. `value` holds the bound value.

###### name

> **name**: `string`

##### parent

> **parent**: `number`

Index of the parent entry in the trace array; null at the root.

##### skipped?

> `optional` **skipped**: `boolean`

True for an `&&` / `||` operand that was *not* evaluated due to
 short-circuit. The recorder emits a placeholder entry so the trace
 shape mirrors the surface AST.

##### source

> **source**: `string`

Pretty-printed source of the expression (via `assembleExpression`).

##### value?

> `optional` **value**: `unknown`

Evaluation result. Undefined when `skipped` or `error` is set.

***

### FirestoreCase

One Firestore rules case: a single request plus the outcome it should
produce. Structurally identical to the engine's `TestCase` — re-exported
here under the public name so callers never reach into the engine seam.

#### Properties

##### auth?

> `optional` **auth**: `object`

Auth context; `null`/omitted for unauthenticated.

###### token?

> `optional` **token**: `Record`\<`string`, `unknown`\>

###### uid

> **uid**: `string`

##### data?

> `optional` **data**: `Record`\<`string`, `unknown`\>

`request.resource.data` for write operations.

##### description

> **description**: `string`

Human-readable description of what this case verifies.

##### expectation

> **expectation**: `"ALLOW"` \| `"DENY"`

Expected outcome.

##### functionMocks?

> `optional` **functionMocks**: `object`[]

Mock `get()` / `exists()` calls the rules make.

###### function

> **function**: `"get"` \| `"exists"`

###### path

> **path**: `string`

###### result

> **result**: `boolean` \| `Record`\<`string`, `unknown`\>

##### method

> **method**: `"get"` \| `"list"` \| `"create"` \| `"update"` \| `"delete"`

Firestore method under test.

##### path

> **path**: `string`

Document path, e.g. `"users/alice"`.

##### query?

> `optional` **query**: `object`

`request.query` payload (list ops only): limit/offset/orderBy.

###### limit?

> `optional` **limit**: `number`

###### offset?

> `optional` **offset**: `number`

###### orderBy?

> `optional` **orderBy**: `string`

##### requestTime?

> `optional` **requestTime**: `string`

Override for `request.time` (ISO-8601). Defaults to wallclock.

##### resource?

> `optional` **resource**: `Record`\<`string`, `unknown`\>

Existing document data (`resource.data`).

##### writeMode?

> `optional` **writeMode**: \{ `kind`: `"create"`; \} \| \{ `kind`: `"set"`; `merge`: `boolean`; \} \| \{ `kind`: `"update"`; \} \| \{ `kind`: `"delete"`; \}

Explicit write semantics — controls update-merge and getAfter()
 projection. Omit to treat `data` as the full after-state.

***

### FirestoreRuleset

#### Methods

##### explain()

> **explain**(`oneCase`): [`Explanation`](#explanation)

The structured account of why one case resolved as it did.

###### Parameters

###### oneCase

[`FirestoreCase`](#firestorecase)

###### Returns

[`Explanation`](#explanation)

##### lint()

> **lint**(): [`RuleIssue`](#ruleissue)[]

Structural, security, and budget findings on the compiled ruleset.
 No parse errors — the source already compiled.

###### Returns

[`RuleIssue`](#ruleissue)[]

##### simulate()

> **simulate**(`cases`): [`SimulationSummary`](#simulationsummary)

Run every case. Never throws on a rule outcome: a denied or abstained
 case is reported in the returned summary.

###### Parameters

###### cases

[`FirestoreCase`](#firestorecase)[]

###### Returns

[`SimulationSummary`](#simulationsummary)

##### toJSON()

> **toJSON**(): `FirestoreRules`

The parsed ruleset as plain data (the AST).

###### Returns

`FirestoreRules`

***

### PathDef

Definition of rules for a single database path.

#### Properties

##### children?

> `optional` **children**: `Record`\<`string`, [`PathDef`](#pathdef)\>

##### fieldConstraints?

> `optional` **fieldConstraints**: `Record`\<`string`, [`Expr`](#expr)[]\>

##### indexOn?

> `optional` **indexOn**: `string`[]

##### read?

> `optional` **read**: `string`

##### schema?

> `optional` **schema**: `ZodObject`\<`any`\>

##### validate?

> `optional` **validate**: `string`

##### write?

> `optional` **write**: `string`

***

### PathResolutionEntry

One `match` block the simulator considered while resolving the
request path. Together, `PathResolutionTrace.attempts` forms a
complete picture of "what did the resolver try, and where did
each attempt fall apart?" — useful for the agent when a request
lands in the default-deny path because no block matched.

Recorded only by the local simulator; the production Test API
client doesn't expose path-resolution internals.

#### Properties

##### bindings

> **bindings**: `Record`\<`string`, `string`\>

Wildcard / recursive bindings the block produced (even when
 the overall match failed — the partial bindings are still
 diagnostic).

##### blockPath

> **blockPath**: `string`

Raw match path as written in the source, e.g.
 `'/users/{uid}/messages/{mId}'`.

##### line?

> `optional` **line**: `number`

Source line of the `match` keyword. Absent when the parser
 didn't populate `loc` (programmatically-constructed blocks).

##### matched

> **matched**: `boolean`

True for a block that fully resolved (no remaining request
 segments AND every nested match either completed or wasn't
 needed). MULTIPLE entries may be `matched: true` in one trace:
 a request path can match several overlapping `match` blocks
 (e.g. `/docs/{doc}` and a sibling `/{document=**}`), and every
 matching block's allows OR-combine. Container blocks whose
 children completed the resolution are also flagged matched.

##### matchedSegments

> **matchedSegments**: `number`

How many of the block's path segments matched against the
 request path before the resolver gave up or completed.

##### reason?

> `optional` **reason**: `"literal-mismatch"` \| `"request-shorter"` \| `"no-matching-child"`

Why the resolver moved on. Absent when `matched: true`.
  - `'literal-mismatch'` — a literal segment in the block
    didn't match the corresponding request segment.
  - `'request-shorter'` — block path had more segments than
    the request supplied (e.g. block is `/a/{b}/c`, request is `/a/x`).
  - `'no-matching-child'` — block matched its own segments but
    remaining request segments weren't covered by any child block.

##### totalSegments

> **totalSegments**: `number`

Total segments in the block's path pattern.

***

### PathResolutionTrace

#### Properties

##### attempts

> **attempts**: [`PathResolutionEntry`](#pathresolutionentry)[]

One entry per match block the resolver considered, in the
 order it tried them. `attempts[i].matched: true` marks a
 block that fully resolved; one or more per trace, since
 overlapping blocks all match and OR-combine.

##### requestPath

> **requestPath**: `string`

The request path that was resolved, verbatim from `TestCase.path`.

***

### RtdbCase

One Realtime Database rules case. `expectation` is required so a `simulate`
run can partition cases into passed/failed the same way Firestore does —
the RTDB simulator otherwise returns only a raw allow/deny with no notion
of an expectation.

#### Properties

##### auth?

> `optional` **auth**: `string` \| \{ `token?`: `Record`\<`string`, `unknown`\>; `uid`: `string`; \}

Auth context; a bare uid string, a full identity, or `null`.

##### data?

> `optional` **data**: `Record`\<`string`, `unknown`\>

Existing tree data the rule reads (`data`).

##### description?

> `optional` **description**: `string`

Human-readable description of what this case verifies.

##### expectation

> **expectation**: `"ALLOW"` \| `"DENY"`

Expected outcome.

##### newData?

> `optional` **newData**: `unknown`

Proposed write value (`newData`), for write/validate cases.

##### operation

> **operation**: `"validate"` \| `"read"` \| `"write"`

RTDB rule kind under test.

##### path

> **path**: `string`

Absolute, root-relative tree path, e.g. `"/users/alice"`.

***

### RtdbCaseResult

The outcome of running one RTDB case through `simulate`.

#### Properties

##### case

> **case**: [`RtdbCase`](#rtdbcase)

##### decision

> **decision**: `"ALLOW"` \| `"DENY"` \| `"UNSUPPORTED"`

##### description?

> `optional` **description**: `string`

##### expectation

> **expectation**: `"ALLOW"` \| `"DENY"`

##### matchedPath

> **matchedPath**: `string`

The tree path whose rule decided the request.

##### matchedRule

> **matchedRule**: `string`

Which rule kind (`.read` / `.write` / `.validate`) decided.

##### passed

> **passed**: `boolean`

##### reason

> **reason**: `string`

Engine-provided reason string.

##### unsupported

> **unsupported**: `boolean`

***

### RtdbExplanation

#### Properties

##### decision

> **decision**: `"ALLOW"` \| `"DENY"` \| `"UNSUPPORTED"`

##### expectation

> **expectation**: `"ALLOW"` \| `"DENY"`

##### matchedPath

> **matchedPath**: `string`

##### matchedRule

> **matchedRule**: `string`

##### passed

> **passed**: `boolean`

##### reason

> **reason**: `string`

##### unsupported

> **unsupported**: `boolean`

***

### RtdbRulesCheckResult

#### Properties

##### errors

> **errors**: [`RtdbRulesFinding`](#rtdbrulesfinding)[]

##### ok

> **ok**: `boolean`

##### warnings

> **warnings**: [`RtdbRulesFinding`](#rtdbrulesfinding)[]

***

### RtdbRulesDefinition

#### Properties

##### paths

> **paths**: `Record`\<`string`, [`PathDef`](#pathdef)\> \| (`ctx`) => `void`

***

### RtdbRulesDocument

The authored RTDB rules artifact `defineRtdbRules` returns.

Deliberately INERT on the public surface: it exposes no methods. It is a
value you author and hand to `rtdbRules()`, which is the one analysis
surface (`lint` / `simulate` / `explain` / `toJSON`). The brand is
type-level only; nothing exists at runtime.

#### Properties

##### \[RTDB\_RULES\_DOCUMENT\_BRAND\]?

> `readonly` `optional` **\[RTDB\_RULES\_DOCUMENT\_BRAND\]**: `never`

***

### RtdbRuleset

#### Methods

##### explain()

> **explain**(`oneCase`): [`RtdbExplanation`](#rtdbexplanation)

The structured account of why one case resolved as it did.

###### Parameters

###### oneCase

[`RtdbCase`](#rtdbcase)

###### Returns

[`RtdbExplanation`](#rtdbexplanation)

##### lint()

> **lint**(): [`RuleIssue`](#ruleissue)[]

Structural findings on the compiled ruleset (from `check()`).

###### Returns

[`RuleIssue`](#ruleissue)[]

##### simulate()

> **simulate**(`cases`): [`RtdbSimulationSummary`](#rtdbsimulationsummary)

Run every case. Never throws on a rule outcome.

###### Parameters

###### cases

[`RtdbCase`](#rtdbcase)[]

###### Returns

[`RtdbSimulationSummary`](#rtdbsimulationsummary)

##### toJSON()

> **toJSON**(): [`RtdbRulesJson`](#rtdbrulesjson)

The compiled `rules.json`.

###### Returns

[`RtdbRulesJson`](#rtdbrulesjson)

***

### RtdbRulesFinding

#### Properties

##### code

> **code**: `string`

##### message

> **message**: `string`

##### path

> **path**: `string`

##### rule

> **rule**: [`RtdbRulesFindingRule`](#rtdbrulesfindingrule-1)

***

### RtdbSimulationSummary

#### Properties

##### cases

> **cases**: [`RtdbCaseResult`](#rtdbcaseresult)[]

##### failed

> **failed**: `number`

##### passed

> **passed**: `number`

##### unsupported

> **unsupported**: `number`

***

### RuleEvaluation

Per-rule evaluation entry produced by the local simulator. Each entry
corresponds to one `allow` declaration the simulator evaluated, in
source order.

Populated only by the internal simulator (which has the parsed
AST in hand); the production Test API client (`TestFirestoreRulesHandler`)
returns an empty `trace` and surfaces the wire text on `TestResult.notes`.

#### Properties

##### conditionText?

> `optional` **conditionText**: `string`

Pretty-printed condition expression. Use this verbatim in agent-facing
 summaries — derived from the AST via `printExpression`, so it survives
 comment removal and whitespace re-flow in the source.

##### expressionTrace?

> `optional` **expressionTrace**: [`ExprTraceEntry`](#exprtraceentry)[]

Per-sub-expression evaluation trace for this rule's condition.
Flat, in evaluation order; reconstruct the tree via the `parent`
index on each entry. Populated by the local simulator when the
caller enables tracing — currently always on for
the simulator so agents can see *why*
a rule's condition resolved as it did (which disjunct was true,
which `let` binding the value flowed through, which method call
threw). Absent on entries that came from the production Test API
client (no AST visibility).

##### line?

> `optional` **line**: `number`

1-indexed source line of the `allow` keyword. Populated when the
 rule's `loc` was set by the parser.

##### matchPath?

> `optional` **matchPath**: `string`

Source-rendered path of the `match` block this rule belongs to, e.g.
`'/docs/{docId}'` or `'/{document=**}'`. Populated when the request
path matches MORE THAN ONE overlapping `match` block: allows OR-combine
across every matching block (production semantics — there is no
first-match-wins), so a DENY trace can carry entries from several
blocks. This field keeps them unambiguous — which block did this rule
live in. Absent for the common single-block case.

##### message?

> `optional` **message**: `string`

Human-readable diagnostic — populated for `UNSUPPORTED` (which sim
 surface is missing) and `ERROR` (which runtime error caused the
 rule to abort).

##### operations

> **operations**: (`"get"` \| `"list"` \| `"create"` \| `"update"` \| `"delete"` \| `"read"` \| `"write"`)[]

Operations declared on the allow rule (`read`, `write`, `get`, ...).

##### ruleIndex

> **ruleIndex**: `number`

Position of the `allow` declaration within its match block, 0-indexed
 in source order.

##### verdict

> **verdict**: `"ALLOW"` \| `"DENY"` \| `"UNSUPPORTED"` \| `"ERROR"`

Outcome for this single rule. The TestResult's overall `decision` is
derived from the trace under OR semantics (any `'ALLOW'` ⇒ ALLOW,
else any `'UNSUPPORTED'` ⇒ UNSUPPORTED, else DENY).

***

### RuleIssue

#### Properties

##### code

> **code**: `string`

Stable machine code, e.g. `'PARSE_ERROR'`, `'SEC-4'`, `'GET_BUDGET'`.

##### fix?

> `optional` **fix**: `string`

Suggested remediation, verbatim, when the producing stage offers one.

##### line?

> `optional` **line**: `number`

1-indexed source line, when known.

##### message

> **message**: `string`

Human-readable description.

##### origin

> **origin**: [`RuleIssueOrigin`](#ruleissueorigin-1)

##### path?

> `optional` **path**: `string`

Rules path the issue applies to, when known (e.g. `'/users/{uid}'`).

##### severity

> **severity**: [`RuleIssueSeverity`](#ruleissueseverity-1)

***

### RulesetContext

Context passed to the callback overload of ruleset().

#### Properties

##### path()

> **path**: (`path`, `def`) => `void`

###### Parameters

###### path

`string`

###### def

[`PathDef`](#pathdef)

###### Returns

`void`

***

### SchemaRulesResult

#### Properties

##### children

> **children**: `Record`\<`string`, \{ `children?`: `Record`\<`string`, \{ `validate`: [`Expr`](#expr); \}\>; `validate`: [`Expr`](#expr); \}\>

##### validate

> **validate**: `string`

***

### SimulationSummary

Aggregate of a `simulate(cases)` run. Counts partition the cases:
 `passed + failed + unsupported === cases.length`.

#### Properties

##### cases

> **cases**: [`CaseResult`](#caseresult)[]

##### failed

> **failed**: `number`

##### passed

> **passed**: `number`

##### unsupported

> **unsupported**: `number`

## Type Aliases

### Expr

> **Expr** = `string`

Type alias for RTDB rule expression strings.

***

### FirestoreMethod

> **FirestoreMethod** = *typeof* `FIRESTORE_METHODS`\[`number`\]

***

### RtdbRulesFindingRule

> **RtdbRulesFindingRule** = `".read"` \| `".write"` \| `".validate"` \| `"ruleset"`

***

### RtdbRulesJson

> **RtdbRulesJson** = `object`

#### Properties

##### rules

> **rules**: `Record`\<`string`, `unknown`\>

***

### RtdbRulesSimulationAuth

> **RtdbRulesSimulationAuth** = `string` \| \{ `token?`: `Record`\<`string`, `unknown`\>; `uid`: `string`; \} \| `null`

***

### RtdbRulesSimulationInput

> **RtdbRulesSimulationInput** = `Omit`\<`SimulationInput`, `"auth"` \| `"mockData"`\> & `object`

#### Type Declaration

##### auth?

> `optional` **auth**: [`RtdbRulesSimulationAuth`](#rtdbrulessimulationauth)

##### data?

> `optional` **data**: `Record`\<`string`, `unknown`\>

##### mockData?

> `optional` **mockData**: `Record`\<`string`, `unknown`\>

***

### RuleIssueOrigin

> **RuleIssueOrigin** = `"parse"` \| `"validate"` \| `"lint"`

The stage that produced the issue.
  - `parse`    — the source did not parse; nothing downstream ran.
  - `validate` — a structural/security finding on a parsed ruleset.
  - `lint`     — a budget/quality/hallucination warning.

***

### RuleIssueSeverity

> **RuleIssueSeverity** = `"error"` \| `"warning"` \| `"info"`

Ordered by decreasing urgency. `info` is advisory.

***

### Segment

> **Segment** = `string` \| \{ `$`: `string`; \}

A path segment: string for literal, { $: name } for path variable.

## Variables

### all()

> `const` **all**: (...`exprs`) => [`Expr`](#expr)

All conditions must be true (AND).

#### Parameters

##### exprs

...[`Expr`](#expr)[]

#### Returns

[`Expr`](#expr)

***

### allow()

> `const` **allow**: () => [`Expr`](#expr)

Always allow (true). Readable alias for always().

#### Returns

[`Expr`](#expr)

***

### always()

> `const` **always**: () => [`Expr`](#expr)

Always allow (true).

#### Returns

[`Expr`](#expr)

***

### any()

> `const` **any**: (...`exprs`) => [`Expr`](#expr)

At least one condition must be true (OR).

#### Parameters

##### exprs

...[`Expr`](#expr)[]

#### Returns

[`Expr`](#expr)

***

### AUTH\_UID

> `const` **AUTH\_UID**: [`Segment`](#segment)

auth.uid as a comparison value (unquoted in expressions)

***

### authenticated()

> `const` **authenticated**: () => [`Expr`](#expr)

#### Returns

[`Expr`](#expr)

***

### dataExists()

> `const` **dataExists**: (`path?`) => [`Expr`](#expr)

Check if data exists at current node or a child path

#### Parameters

##### path?

`string`

#### Returns

[`Expr`](#expr)

***

### dataParentVal()

> `const` **dataParentVal**: (`depth`, `field`) => [`Expr`](#expr)

Navigate up from data snapshot, then read a child field's value

#### Parameters

##### depth

`number`

##### field

`string`

#### Returns

[`Expr`](#expr)

***

### dataVal()

> `const` **dataVal**: (`path?`) => [`Expr`](#expr)

Read data value at current node or a child path (pre-write state)

#### Parameters

##### path?

`string`

#### Returns

[`Expr`](#expr)

***

### deny()

> `const` **deny**: () => [`Expr`](#expr)

Always deny (false).

#### Returns

[`Expr`](#expr)

***

### eq()

> `const` **eq**: (`left`, `right`) => [`Expr`](#expr)

Strict equality: left === right (right is a literal value or runtime ref)

#### Parameters

##### left

[`Expr`](#expr)

##### right

`CompareValue`

#### Returns

[`Expr`](#expr)

***

### expr()

> `const` **expr**: (`raw`) => [`Expr`](#expr)

Create an Expr from a raw expression string.

#### Parameters

##### raw

`string`

#### Returns

[`Expr`](#expr)

***

### fieldEnum()

> `const` **fieldEnum**: (`field`, `values`) => [`Expr`](#expr)

Field must be one of the allowed string values

#### Parameters

##### field

`string`

##### values

`string`[]

#### Returns

[`Expr`](#expr)

***

### fieldIsBoolean()

> `const` **fieldIsBoolean**: (`field`) => [`Expr`](#expr)

Field must be a boolean

#### Parameters

##### field

`string`

#### Returns

[`Expr`](#expr)

***

### fieldIsNumber()

> `const` **fieldIsNumber**: (`field`) => [`Expr`](#expr)

Field must be a number

#### Parameters

##### field

`string`

#### Returns

[`Expr`](#expr)

***

### fieldIsString()

> `const` **fieldIsString**: (`field`) => [`Expr`](#expr)

Field must be a string

#### Parameters

##### field

`string`

#### Returns

[`Expr`](#expr)

***

### fieldOwnerOnly()

> `const` **fieldOwnerOnly**: (`field`) => [`Expr`](#expr)

Only the field owner (auth.uid === data.child(field).val()) can access

#### Parameters

##### field

`string`

#### Returns

[`Expr`](#expr)

***

### gt()

> `const` **gt**: (`left`, `right`) => [`Expr`](#expr)

Greater than: left > right

#### Parameters

##### left

[`Expr`](#expr)

##### right

`number`

#### Returns

[`Expr`](#expr)

***

### hasChild()

> `const` **hasChild**: (`field`) => [`Expr`](#expr)

Incoming data must have a specific child field

#### Parameters

##### field

`string`

#### Returns

[`Expr`](#expr)

***

### hasChildren()

> `const` **hasChildren**: () => [`Expr`](#expr)

Incoming data must be an object with at least one child

#### Returns

[`Expr`](#expr)

***

### hasRole()

> `const` **hasRole**: (`segments`, `role`) => [`Expr`](#expr)

Cross-path role check via root lookup

#### Parameters

##### segments

[`Segment`](#segment)[]

##### role

`string`

#### Returns

[`Expr`](#expr)

***

### immutable()

> `const` **immutable**: (`field`) => [`Expr`](#expr)

Field can be set on creation but never changed after

#### Parameters

##### field

`string`

#### Returns

[`Expr`](#expr)

***

### immutableSelf()

> `const` **immutableSelf**: () => [`Expr`](#expr)

This node's own value can be set on creation but never changed

#### Returns

[`Expr`](#expr)

***

### isMember()

> `const` **isMember**: (`listName`, `pathVarName`) => [`Expr`](#expr)

Cross-path membership check: root.child(list).child($var).child(auth.uid).exists()

#### Parameters

##### listName

`string`

##### pathVarName

`string`

#### Returns

[`Expr`](#expr)

***

### isNew()

> `const` **isNew**: () => [`Expr`](#expr)

Data at this path doesn't exist yet (creation check)

#### Returns

[`Expr`](#expr)

***

### lte()

> `const` **lte**: (`left`, `right`) => [`Expr`](#expr)

Less than or equal: left <= right

#### Parameters

##### left

[`Expr`](#expr)

##### right

`number`

#### Returns

[`Expr`](#expr)

***

### neq()

> `const` **neq**: (`left`, `right`) => [`Expr`](#expr)

Strict inequality: left !== right

#### Parameters

##### left

[`Expr`](#expr)

##### right

`CompareValue`

#### Returns

[`Expr`](#expr)

***

### newDataExists()

> `const` **newDataExists**: (`path?`) => [`Expr`](#expr)

Check if incoming data exists at current node or a child path

#### Parameters

##### path?

`string`

#### Returns

[`Expr`](#expr)

***

### newDataIs()

> `const` **newDataIs**: (`type`) => [`Expr`](#expr)

Check incoming data type at current node

#### Parameters

##### type

`"String"` | `"Number"` | `"Boolean"`

#### Returns

[`Expr`](#expr)

***

### newDataParentExists()

> `const` **newDataParentExists**: (`depth`, `field`) => [`Expr`](#expr)

Navigate up from newData snapshot, then check if a child field exists

#### Parameters

##### depth

`number`

##### field

`string`

#### Returns

[`Expr`](#expr)

***

### newDataParentVal()

> `const` **newDataParentVal**: (`depth`, `field`) => [`Expr`](#expr)

Navigate up from newData snapshot, then read a child field's value

#### Parameters

##### depth

`number`

##### field

`string`

#### Returns

[`Expr`](#expr)

***

### newDataVal()

> `const` **newDataVal**: (`path?`) => [`Expr`](#expr)

Read incoming data value at current node or a child path (post-write state)

#### Parameters

##### path?

`string`

#### Returns

[`Expr`](#expr)

***

### not()

> `const` **not**: (`e`) => [`Expr`](#expr)

Negate a condition.

#### Parameters

##### e

[`Expr`](#expr)

#### Returns

[`Expr`](#expr)

***

### ownerOrNew()

> `const` **ownerOrNew**: (`field`) => [`Expr`](#expr)

Anyone authenticated can create; only the field owner can edit

#### Parameters

##### field

`string`

#### Returns

[`Expr`](#expr)

***

### ownField()

> `const` **ownField**: (`field`) => [`Expr`](#expr)

Field-based ownership: auth.uid matches a value stored in a data field

#### Parameters

##### field

`string`

#### Returns

[`Expr`](#expr)

***

### ownPath()

> `const` **ownPath**: (`pathVar`) => [`Expr`](#expr)

Path-based ownership: auth.uid matches a URL path variable

#### Parameters

##### pathVar

`string`

#### Returns

[`Expr`](#expr)

***

### pathOwnerOnly()

> `const` **pathOwnerOnly**: (`pathVar`) => [`Expr`](#expr)

Only the path owner (auth.uid === $pathVar) can access

#### Parameters

##### pathVar

`string`

#### Returns

[`Expr`](#expr)

***

### required()

> `const` **required**: (...`fields`) => [`Expr`](#expr)

All specified fields must be present in the incoming data

#### Parameters

##### fields

...`string`[]

#### Returns

[`Expr`](#expr)

***

### rootEquals()

> `const` **rootEquals**: (`segments`, `value`) => [`Expr`](#expr)

Check if a path's value equals a specific string (via root)

#### Parameters

##### segments

[`Segment`](#segment)[]

##### value

`string`

#### Returns

[`Expr`](#expr)

***

### rootExists()

> `const` **rootExists**: (`segments`) => [`Expr`](#expr)

Check if a path exists in the database (via root)

#### Parameters

##### segments

[`Segment`](#segment)[]

#### Returns

[`Expr`](#expr)

***

### transition()

> `const` **transition**: (`field`, `allowed`) => [`Expr`](#expr)

State machine: only allowed transitions on a field

#### Parameters

##### field

`string`

##### allowed

\[`string`, `string`\][]

#### Returns

[`Expr`](#expr)

## Functions

### assertCase()

#### Call Signature

> **assertCase**(`result`): `void`

Throw when a case result did not pass. A simulator abstention throws
[RulesUnsupportedError](#rulesunsupportederror); a genuine expectation mismatch throws
[RulesAssertionError](#rulesassertionerror). Both carry the [explainCase](#explaincase) trace as
their message. Returns `void` on a passing result.

##### Parameters

###### result

[`CaseResult`](#caseresult) | [`RtdbCaseResult`](#rtdbcaseresult)

##### Returns

`void`

#### Call Signature

> **assertCase**(`ruleset`, `oneCase`): `void`

Simulate one case against a ruleset and throw on a miss — the runner
form: `for (const c of cases) test(c.description, () => assertCase(ruleset, c))`.

##### Parameters

###### ruleset

[`FirestoreRuleset`](#firestoreruleset)

###### oneCase

[`FirestoreCase`](#firestorecase)

##### Returns

`void`

#### Call Signature

> **assertCase**(`ruleset`, `oneCase`): `void`

Throw when a case result did not pass. A simulator abstention throws
[RulesUnsupportedError](#rulesunsupportederror); a genuine expectation mismatch throws
[RulesAssertionError](#rulesassertionerror). Both carry the [explainCase](#explaincase) trace as
their message. Returns `void` on a passing result.

##### Parameters

###### ruleset

[`RtdbRuleset`](#rtdbruleset)

###### oneCase

[`RtdbCase`](#rtdbcase)

##### Returns

`void`

#### Call Signature

> **assertCase**(`source`, `oneCase`): `void`

Convenience: compile Firestore source and assert one case against it.

##### Parameters

###### source

`string`

###### oneCase

[`FirestoreCase`](#firestorecase)

##### Returns

`void`

***

### bytes()

> **bytes**(`input`): `Bytes`

A `bytes` value.
  - string     → UTF-8 encoded
  - Uint8Array → used verbatim

#### Parameters

##### input

`string` | `Uint8Array`\<`ArrayBufferLike`\>

#### Returns

`Bytes`

***

### defineRtdbRules()

> **defineRtdbRules**(`definition`): [`RtdbRulesDocument`](#rtdbrulesdocument)

#### Parameters

##### definition

[`RtdbRulesDefinition`](#rtdbrulesdefinition)

#### Returns

[`RtdbRulesDocument`](#rtdbrulesdocument)

***

### duration()

> **duration**(`value`, `unit?`): `Duration`

A `duration` value. `unit` is one of the Firestore duration units
(`'w' | 'd' | 'h' | 'm' | 's' | 'ms' | 'ns'`); defaults to seconds.

#### Parameters

##### value

`number`

##### unit?

`string`

#### Returns

`Duration`

***

### explainCase()

> **explainCase**(`result`): `string`

Render a case result as a human-readable trace. The single sanctioned
trace renderer — used as the message of the error `assertCase` throws, and
available directly for logging a result without asserting.

#### Parameters

##### result

[`CaseResult`](#caseresult) | [`RtdbCaseResult`](#rtdbcaseresult)

#### Returns

`string`

***

### firestoreRules()

> **firestoreRules**(`source`): [`FirestoreRuleset`](#firestoreruleset)

Compile Firestore rules source into a deep, safe-by-default handle.

#### Parameters

##### source

`string`

#### Returns

[`FirestoreRuleset`](#firestoreruleset)

#### Throws

when the source does not parse. The thrown
  error carries the compile-blocking issues on `.issues`.

***

### flip()

> **flip**(`marks`): `string`

Turn flip: validates a turn field alternates between marks.
First mark is the initial value on creation.
Supports 2+ players with circular rotation.

#### Parameters

##### marks

`string`[]

ordered list of marks (e.g., ["X", "O"])

#### Returns

`string`

***

### latlng()

> **latlng**(`lat`, `lng`): `LatLng`

A `latlng` geographic point.

#### Parameters

##### lat

`number`

##### lng

`number`

#### Returns

`LatLng`

***

### lint()

> **lint**(`source`): [`RuleIssue`](#ruleissue)[]

Lint Firestore rules source. Accepts anything — including empty or
syntactically broken source — and always returns an issue list.

#### Parameters

##### source

`string`

#### Returns

[`RuleIssue`](#ruleissue)[]

***

### reference()

> **reference**(`path`): `Reference`

A `reference` to a document, by its path (e.g. `"users/alice"`).

#### Parameters

##### path

`string`

#### Returns

`Reference`

***

### rtdbRules()

> **rtdbRules**(`input`): [`RtdbRuleset`](#rtdbruleset)

Build a deep handle on a Realtime Database ruleset from a definition, a
compiled document, or compiled `{ rules }` JSON.

#### Parameters

##### input

`RtdbRulesInput`

#### Returns

[`RtdbRuleset`](#rtdbruleset)

***

### ruleset()

> **ruleset**(`input`): `RtdbNode`

Compile a declarative rules definition into an environment-independent tree.

#### Parameters

##### input

`Record`\<`string`, [`PathDef`](#pathdef)\> | (`ctx`) => `void`

#### Returns

`RtdbNode`

***

### schemaRules()

> **schemaRules**(`schema`, `fieldConstraints?`): [`SchemaRulesResult`](#schemarulesresult)

Generate RTDB validate rules from a Zod object schema.
Optional fieldConstraints are AND-composed with the schema type check.

#### Parameters

##### schema

`ZodObject`\<`any`\>

##### fieldConstraints?

`Record`\<`string`, [`Expr`](#expr)[]\>

#### Returns

[`SchemaRulesResult`](#schemarulesresult)

***

### serverTimestamp()

> **serverTimestamp**(): `object`

The server-timestamp sentinel — the case-data equivalent of
`FieldValue.serverTimestamp()`. The simulator resolves it to the request
time, so a rule comparing `data.createdAt == request.time` sees a match.

#### Returns

`object`

##### \_\_type

> `readonly` **\_\_type**: `"serverTimestamp"`

***

### timestamp()

> **timestamp**(`input`): `Timestamp`

A Firestore `timestamp` value.
  - number → milliseconds since the epoch
  - string → ISO-8601
  - object → explicit `{ seconds, nanos }`

#### Parameters

##### input

`string` | `number` | \{ `nanos?`: `number`; `seconds`: `number`; \}

#### Returns

`Timestamp`

***

### turnGuard()

> **turnGuard**(`turnField`, `players`, `statusField?`, `playingValue?`): `string`

Turn enforcement: only the current turn's player can write.
Uses data (pre-write) for the turn check — NOT newData.

#### Parameters

##### turnField

`string`

the field that stores whose turn it is (e.g., "currentTurn")

##### players

`Record`\<`string`, `string`\>

map of mark → player field (e.g., { X: "playerX", O: "playerO" })

##### statusField?

`string`

optional field that must equal playingValue for moves to be allowed

##### playingValue?

`string`

the value of statusField during active play (e.g., "playing")

#### Returns

`string`

***

### vector()

> **vector**(`values`): `Vector`

A `vector` value from its numeric components.

#### Parameters

##### values

readonly `number`[]

#### Returns

`Vector`

***

### winCheckHelper()

> **winCheckHelper**(`mark`, `lines`, `boardPath?`): `string`

Win check helper: validates a boolean field against winning lines on a board.
If true, at least one winning line must exist. If false, no winning line can exist.
Uses the "client claims, rules verify" pattern.

#### Parameters

##### mark

`string`

the player mark to check (e.g., "X")

##### lines

`number`[][]

array of winning line coordinates (e.g., [[0,1,2], [3,4,5], ...])

##### boardPath?

`string`

the path to the board relative to the parent (default "board")

#### Returns

`string`
