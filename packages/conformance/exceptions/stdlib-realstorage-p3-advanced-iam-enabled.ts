import type { ObservationException } from './types.ts';

export const exception: ObservationException = {
  reason: 'Real-resource production discovery evidence for Storage-to-Firestore return types, evaluation order, path boundaries, helper composition, and independence from Firestore rules. It does not claim local evaluator conformance.',
  until: 'The corresponding Storage evaluator behavior and evidence rows are implemented and replay this production capture locally.',
};
