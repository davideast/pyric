import type { ObservationException } from './types.ts';

export const exception: ObservationException = {
  reason: 'Real-resource production discovery evidence for the default-versus-named Firestore database boundary in Storage Rules. It does not claim local evaluator conformance.',
  until: 'The Storage evaluator models the qualified database boundary and replays this production capture locally.',
};
