import type { ObservationException } from './types.ts';

export const exception: ObservationException = {
  reason: 'Discovery evidence for the module transport boundary; it informs the portable compiler contract rather than claiming a new local evaluator behavior.',
  until: 'The module transport contract receives a dedicated registry row.',
};
