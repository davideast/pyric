import type { ObservationException } from './types.ts';

export const exception: ObservationException = {
  reason:
    'Upstream listener-shape observation for the agent-tool deny-list; it intentionally references the deny-listed listener family rather than a single implemented matrix row.',
};
