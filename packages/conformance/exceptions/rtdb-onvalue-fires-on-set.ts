import type { ObservationException } from './types.ts';

export const exception: ObservationException = {
  reason:
    'Upstream listener-shape observation retained for the agent-tool boundary; it references the listener family rather than a single implemented matrix row.',
};
