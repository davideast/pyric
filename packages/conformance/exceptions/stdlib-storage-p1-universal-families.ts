import type { ObservationException } from './types.ts';

export const exception: ObservationException = {
  reason: 'Candidate common-language discovery; production acceptance does not become a Pyric conformance claim until each family is implemented and replayed locally.',
  until: 'Common stdlib candidates are selected or explicitly declined.',
};
