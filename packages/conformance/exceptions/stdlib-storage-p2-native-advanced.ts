import type { ObservationException } from './types.ts';

export const exception: ObservationException = {
  reason: 'Candidate Storage-native discovery; MapDiff/update/hash-shaped production observations remain non-claims until mirror replay and real-resource population evidence exist.',
  until: 'Storage-native module candidates are selected or explicitly declined.',
};
