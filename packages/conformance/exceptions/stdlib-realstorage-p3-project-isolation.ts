import type { ObservationException } from './types.ts';

export const exception: ObservationException = {
  reason: 'Real-resource production discovery evidence that Storage-to-Firestore lookups resolve within the bucket-owning project. It does not claim local evaluator conformance.',
  until: 'The Storage evaluator models project ownership and replays this two-project production capture locally.',
};
