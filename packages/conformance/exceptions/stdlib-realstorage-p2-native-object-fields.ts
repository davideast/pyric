import type { ObservationException } from './types.ts';

export const exception: ObservationException = {
  reason: 'Real-resource production discovery evidence for server-populated Storage object fields and their update/overwrite relationships. It does not claim local evaluator conformance.',
  until: 'The corresponding Storage evaluator behavior and evidence rows replay this production capture locally.',
};
