import type { ObservationException } from './types.ts';

export const exception: ObservationException = {
  reason: 'Bounded production characterization of immediate Storage authorization after Firestore membership changes. It is evidence, not a consistency-latency guarantee or local conformance claim.',
  until: 'A published consistency contract and corresponding local replay are intentionally adopted.',
};
