{
  "schema": "pyric.conformance.surface.v1",
  "order": 2,
  "kind": "mirror",
  "censusSurface": "auth",
  "upstream": "firebase/auth",
  "mirrors": [
    "pyric/auth"
  ],
  "observationPrefixes": [
    "auth-",
    "admin-app-"
  ],
  "coverage": true,
  "scopeNote": "fetchSignInMethodsForEmail remains a public gap despite its upstream deprecation and security disposition. Account linking, re-authentication, and email-link/action-code APIs are built. MFA, phone, reCAPTCHA, and their exported types remain public gaps and stay in the denominator.",
  "captureRigs": [
    "oracle-run",
    "admin-app"
  ],
  "registry": "auth",
  "dispositions": [
    {
      "tier": "out-of-scope",
      "reason": "Deprecated upstream as a security retraction, not a cosmetic one: the shipped @firebase/auth declaration states it \"returns an empty list when Email Enumeration Protection is enabled, irrespective of the number of authentication methods available\", and that \"migrating off of this method is recommended as a security best-practice\". Enumeration protection is on by default, so against a modern project the production function always returns []. A mirror that reproduced the pre-deprecation behavior would be MORE capable than prod and would silently mislead agent code that branches on the result; a mirror that reproduced the post-deprecation behavior would be a no-op. Same criterion applied to Imagen in firebase/ai — an API whose production counterpart is retiring is out of scope, because mirroring it freezes dead behavior.",
      "symbols": [
        "fetchSignInMethodsForEmail"
      ]
    },
    {
      "tier": "deferred",
      "reason": "Phone number mutation is deferred alongside the rest of the phone-auth family — buildable via a mocked SMS/verification-code resolver, the same shape as the OAuth resolver.",
      "symbols": [
        "updatePhoneNumber"
      ]
    },
    {
      "tier": "deferred",
      "reason": "MFA / phone / reCAPTCHA is deferred, not out of scope — reCAPTCHA/SMS are external infrastructure Pyric can mock with the OAuth resolver pattern, and TOTP is pure algorithm work.",
      "symbols": [
        "multiFactor",
        "getMultiFactorResolver",
        "FactorId",
        "PhoneAuthProvider",
        "PhoneAuthCredential",
        "PhoneMultiFactorGenerator",
        "TotpMultiFactorGenerator",
        "TotpSecret",
        "signInWithPhoneNumber",
        "linkWithPhoneNumber",
        "reauthenticateWithPhoneNumber",
        "RecaptchaVerifier",
        "initializeRecaptchaConfig"
      ]
    }
  ]
}
