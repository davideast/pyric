{
  "schema": "pyric.conformance.surface.v2",
  "kind": "mirror",
  "developerSurface": "auth",
  "censusSurface": "auth",
  "upstream": "firebase/auth",
  "mirrors": [
    "pyric/auth"
  ],
  "privateRuntimeExports": [],
  "observationPrefixes": [
    "auth-",
    "admin-app-"
  ],
  "coverage": true,
  "captureRigs": [
    "oracle-run",
    "admin-app"
  ],
  "registry": "auth",
  "dispositions": [
    {
      "id": "auth.email-enumeration",
      "availability": "out-of-scope",
      "reasonCode": "upstream-deprecated",
      "summary": "Deprecated upstream as a security retraction, not a cosmetic one: the shipped @firebase/auth declaration states it \"returns an empty list when Email Enumeration Protection is enabled, irrespective of the number of authentication methods available\", and that \"migrating off of this method is recommended as a security best-practice\". Enumeration protection is on by default, so against a modern project the production function always returns []. A mirror that reproduced the pre-deprecation behavior would be MORE capable than prod and would silently mislead agent code that branches on the result; a mirror that reproduced the post-deprecation behavior would be a no-op. Same criterion applied to Imagen in firebase/ai — an API whose production counterpart is retiring is out of scope, because mirroring it freezes dead behavior.",
      "evidenceRefs": ["registry:auth#175", "upstream:firebase/auth"],
      "symbols": [
        "fetchSignInMethodsForEmail"
      ]
    },
    {
      "id": "auth.phone-mutation",
      "availability": "deferred",
      "reasonCode": "implementation-deferred",
      "summary": "Phone number mutation is deferred alongside the rest of the phone-auth family — buildable via a mocked SMS/verification-code resolver, the same shape as the OAuth resolver.",
      "evidenceRefs": ["registry:auth#181"],
      "symbols": [
        "updatePhoneNumber"
      ]
    },
    {
      "id": "auth.mfa-phone-recaptcha",
      "availability": "deferred",
      "reasonCode": "implementation-deferred",
      "summary": "MFA / phone / reCAPTCHA is deferred, not out of scope — reCAPTCHA/SMS are external infrastructure Pyric can mock with the OAuth resolver pattern, and TOTP is pure algorithm work.",
      "evidenceRefs": ["registry:auth#180"],
      "symbols": [
        "multiFactor",
        "getMultiFactorResolver",
        "FactorId",
        "PhoneAuthProvider",
        "PhoneAuthCredential",
        "PhoneMultiFactorGenerator",
        "TotpMultiFactorGenerator",
        "TotpSecret",
        "signInWithPhoneNumber",
        "linkWithPhoneNumber",
        "reauthenticateWithPhoneNumber",
        "RecaptchaVerifier",
        "initializeRecaptchaConfig"
      ]
    }
  ]
}
