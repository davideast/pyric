{
  "name": "rules-firestore-string-literals-and-regex",
  "matrixRow": "firestore-rules#175",
  "rowIds": [
    "firestore-rules#175"
  ],
  "description": "Firestore Rules Test API verdicts for corpus pack \"string-literals-and-regex\" (Class B). Pre-fix the simulator forwarded raw `\\\\.` to RegExp without unescaping; production-style `.matches('...\\\\.com')` denied silently.",
  "observedAt": "2026-07-10T22:41:17.909Z",
  "fbSdkVersion": "12.13.0",
  "projectId": "digame-mas",
  "behavior": {
    "matches('.*@acme\\\\.com') vs alice@acme.com → ALLOW": "ALLOW",
    "matches('.*@acme\\\\.com') vs bob@other.com → DENY": "DENY",
    "matches('.*@acme.com') vs alice@acme.com → ALLOW (no-escape control)": "ALLOW",
    "!matches('.*\\t.*') vs name without tab → ALLOW (tab escape literal)": "ALLOW"
  }
}
