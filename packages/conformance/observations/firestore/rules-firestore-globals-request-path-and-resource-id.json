{
  "name": "rules-firestore-globals-request-path-and-resource-id",
  "matrixRow": "firestore#166",
  "rowIds": [
    "firestore#166"
  ],
  "description": "Firestore Rules Test API verdicts for corpus pack \"globals-request-path-and-resource-id\" (Item 6). Sim must populate request.path (Path), request.query (Map), resource.id (String), resource.__name__ (Path). Pre-fix: all undefined; rules using them silently denied.",
  "observedAt": "2026-07-10T22:41:18.980Z",
  "fbSdkVersion": "12.13.0",
  "projectId": "digame-mas",
  "behavior": {
    "request.path is path ALLOW": "ALLOW",
    "request.path equals literal ALLOW": "ALLOW",
    "request.query empty map ALLOW": "DENY",
    "resource.id on create → DENY (resource is null pre-write)": "DENY",
    "resource.__name__ on create → DENY (resource is null pre-write)": "DENY",
    "wrong resource.id DENY": "DENY"
  }
}
