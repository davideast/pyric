{
  "id": "experiment/owner-doc-write",
  "skill": "firebase-security-rules",
  "description": "Owners create/update only their own /users/{uid} doc; any signed-in user may read.",
  "prompt": "Write Firestore rules so a signed-in user can create and update only their own /users/{uid} document. Reads of /users/{uid} are allowed for any signed-in user. Unauthenticated access is denied entirely.",
  "successSpec": {
    "name": "experiment/conformance",
    "args": {
      "cases": [
        { "method": "get", "path": "users/alice", "auth": { "uid": "bob" }, "expect": "ALLOW" },
        { "method": "update", "path": "users/alice", "auth": { "uid": "alice" }, "expect": "ALLOW" },
        { "method": "update", "path": "users/alice", "auth": { "uid": "bob" }, "expect": "DENY" },
        { "method": "create", "path": "users/alice", "auth": null, "expect": "DENY" },
        { "method": "get", "path": "users/alice", "auth": null, "expect": "DENY" }
      ]
    }
  }
}
