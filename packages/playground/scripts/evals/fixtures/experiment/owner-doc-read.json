{
  "id": "experiment/owner-doc-read",
  "skill": "firebase-security-rules",
  "description": "Authenticated users may read only their own /users/{uid} document.",
  "prompt": "Write Firestore security rules so a signed-in user can read their own document at /users/{uid}, where uid equals their auth uid. No other user can read it, and unauthenticated requests are denied.",
  "successSpec": {
    "name": "experiment/conformance",
    "args": {
      "cases": [
        { "method": "get", "path": "users/alice", "auth": { "uid": "alice" }, "expect": "ALLOW" },
        { "method": "get", "path": "users/alice", "auth": { "uid": "mallory" }, "expect": "DENY" },
        { "method": "get", "path": "users/alice", "auth": null, "expect": "DENY" }
      ]
    }
  }
}
