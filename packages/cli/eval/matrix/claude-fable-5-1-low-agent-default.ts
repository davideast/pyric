import type { EvalRow } from '../types.js';

const row: EvalRow = {
  id: 'claude-fable-5-1-low-agent-default',
  cli: 'claude',
  model: 'claude-fable-5-1',
  effort: 'low',
  condition: 'agent-default',
  seeds: [1, 2, 3],
};

export default row;
