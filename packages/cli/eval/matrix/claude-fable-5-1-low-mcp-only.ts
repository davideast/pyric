import type { EvalRow } from '../types.js';

const row: EvalRow = {
  id: 'claude-fable-5-1-low-mcp-only',
  cli: 'claude',
  model: 'claude-fable-5-1',
  effort: 'low',
  condition: 'mcp-only',
  seeds: [1, 2, 3],
};

export default row;
