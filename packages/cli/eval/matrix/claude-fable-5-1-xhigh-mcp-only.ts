import type { EvalRow } from '../types.js';

const row: EvalRow = {
  id: 'claude-fable-5-1-xhigh-mcp-only',
  cli: 'claude',
  model: 'claude-fable-5-1',
  effort: 'xhigh',
  condition: 'mcp-only',
  seeds: [1, 2, 3],
};

export default row;
