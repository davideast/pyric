import type { EvalRow } from '../types.js';

const row: EvalRow = {
  id: 'gpt-6-astra-low',
  cli: 'codex',
  model: 'gpt-6-astra',
  effort: 'low',
  condition: 'agent-default',
  seeds: [1, 2, 3],
};

export default row;
