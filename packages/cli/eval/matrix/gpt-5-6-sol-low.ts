import type { EvalRow } from '../types.js';

const row: EvalRow = {
  id: 'gpt-5-6-sol-low',
  cli: 'codex',
  model: 'gpt-5.6-sol',
  effort: 'low',
  condition: 'agent-default',
  seeds: [1, 2, 3],
};

export default row;
