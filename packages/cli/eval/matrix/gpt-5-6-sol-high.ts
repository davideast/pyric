import type { EvalRow } from '../types.js';

const row: EvalRow = {
  id: 'gpt-5-6-sol-high',
  cli: 'codex',
  model: 'gpt-5.6-sol',
  effort: 'high',
  condition: 'agent-default',
  seeds: [1, 2, 3],
};

export default row;
