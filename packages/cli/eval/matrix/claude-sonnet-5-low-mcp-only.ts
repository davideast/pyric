import type { EvalRow } from '../types.js';

const row: EvalRow = {
  id: 'claude-sonnet-5-low-mcp-only',
  cli: 'claude',
  model: 'claude-sonnet-5',
  effort: 'low',
  condition: 'mcp-only',
  seeds: [1, 2, 3],
};

export default row;
