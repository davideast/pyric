import type { EvalRow } from '../types.js';

const row: EvalRow = {
  id: 'gpt-5-6-terra-high',
  cli: 'codex',
  model: 'gpt-5.6-terra',
  effort: 'high',
  condition: 'agent-default',
  seeds: [1, 2, 3],
};

export default row;
