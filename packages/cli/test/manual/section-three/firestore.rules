rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /counters/{id} { allow read, write: if request.auth != null; }
    match /atomic/{id} {
      allow read: if true;
      allow write: if request.auth != null && id != 'blocked';
    }
  }
}
