/** The Firebase Cloud Messaging service tool. */
import type { ToolRecord } from '../method-types.js';

export default {
  order: 45,
  intro:
    'Firebase Cloud Messaging in the sandbox: send messages as the FCM server would, manage topic subscriptions, and read back the tokens and deliveries the sandbox recorded.',
} satisfies ToolRecord;
