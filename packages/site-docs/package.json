{
  "name": "@pyric/site-docs",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "port": "bun scripts/port-content.ts",
    "build": "bun run port && astro build",
    "preview": "astro preview",
    "verify": "bun scripts/verify-dist.ts",
    "audit:rhythm": "bun scripts/audit-rhythm.ts",
    "test": "bun run build && bun scripts/verify-dist.ts && bun scripts/audit-rhythm.ts"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "github-slugger": "^2.0.0",
    "playwright": "^1.60.0",
    "typescript": "^5.7.0"
  }
}
